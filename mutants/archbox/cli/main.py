"""Main entry point for archbox CLI."""

from __future__ import annotations

import argparse
import sys
from collections.abc import Callable
from typing import Annotated, ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"]  # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg=None):  # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os  # type: ignore

    mutant_under_test = os.environ["MUTANT_UNDER_TEST"]  # type: ignore
    if mutant_under_test == "fail":  # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException  # type: ignore

        raise MutmutProgrammaticFailException("Failed programmatically")  # type: ignore
    elif mutant_under_test == "stats":  # type: ignore
        from mutmut.__main__ import record_trampoline_hit  # type: ignore

        record_trampoline_hit(orig.__module__ + "." + orig.__name__)  # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    prefix = orig.__module__ + "." + orig.__name__ + "__mutmut_"  # type: ignore
    if not mutant_under_test.startswith(prefix):  # type: ignore
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    mutant_name = mutant_under_test.rpartition(".")[-1]  # type: ignore
    if self_arg is not None:  # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)  # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)  # type: ignore
    return result  # type: ignore


def build_parser() -> argparse.ArgumentParser:
    args = []  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_build_parser__mutmut_orig, x_build_parser__mutmut_mutants, args, kwargs, None
    )


def x_build_parser__mutmut_orig() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_1() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = None
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_2() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog=None,
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_3() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description=None,
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_4() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_5() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_6() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="XXarchboxXX",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_7() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="ARCHBOX",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_8() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="XXArchBox - ARCH/GARCH volatility models for financial time seriesXX",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_9() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="archbox - arch/garch volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_10() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ARCHBOX - ARCH/GARCH VOLATILITY MODELS FOR FINANCIAL TIME SERIES",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_11() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        None,
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_12() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action=None,
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_13() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help=None,
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_14() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_15() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_16() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_17() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "XX--versionXX",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_18() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--VERSION",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_19() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="XXstore_trueXX",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_20() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="STORE_TRUE",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_21() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="XXShow version and exitXX",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_22() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_23() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="SHOW VERSION AND EXIT",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_24() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = None

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_25() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest=None, help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_26() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help=None)

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_27() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_28() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(
        dest="command",
    )

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_29() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="XXcommandXX", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_30() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="COMMAND", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_31() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="XXAvailable commandsXX")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_32() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_33() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="AVAILABLE COMMANDS")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_34() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = None
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_35() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser(None, help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_36() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help=None)
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_37() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser(help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_38() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser(
        "estimate",
    )
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_39() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("XXestimateXX", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_40() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("ESTIMATE", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_41() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="XXEstimate a volatility modelXX")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_42() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_43() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="ESTIMATE A VOLATILITY MODEL")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_44() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        None,
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_45() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=None,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_46() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=None,
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_47() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help=None,
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_48() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_49() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_50() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_51() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_52() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "XX--modelXX",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_53() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--MODEL",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_54() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=False,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_55() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "XXgarchXX",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_56() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "GARCH",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_57() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "XXegarchXX",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_58() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "EGARCH",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_59() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "XXgjrXX",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_60() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "GJR",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_61() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "XXaparchXX",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_62() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "APARCH",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_63() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "XXfigarchXX",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_64() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "FIGARCH",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_65() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "XXigarchXX",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_66() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "IGARCH",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_67() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "XXgarch-mXX",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_68() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "GARCH-M",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_69() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "XXcomponentXX",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_70() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "COMPONENT",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_71() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "XXhar-rvXX",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_72() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "HAR-RV",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_73() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="XXModel typeXX",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_74() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_75() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="MODEL TYPE",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_76() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument(None, required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_77() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=None, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_78() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help=None)
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_79() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument(required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_80() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_81() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument(
        "--data",
        required=True,
    )
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_82() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("XX--dataXX", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_83() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--DATA", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_84() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=False, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_85() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="XXPath to CSV data fileXX")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_86() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="path to csv data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_87() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="PATH TO CSV DATA FILE")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_88() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument(None, default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_89() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default=None, help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_90() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help=None)
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_91() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument(default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_92() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_93() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument(
        "--column",
        default="returns",
    )
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_94() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("XX--columnXX", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_95() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--COLUMN", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_96() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="XXreturnsXX", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_97() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="RETURNS", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_98() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="XXColumn name for returnsXX")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_99() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_100() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="COLUMN NAME FOR RETURNS")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_101() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument(None, type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_102() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=None, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_103() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=None, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_104() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help=None)
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_105() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument(type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_106() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_107() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_108() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument(
        "--p",
        type=int,
        default=1,
    )
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_109() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("XX--pXX", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_110() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--P", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_111() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=2, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_112() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="XXGARCH lag orderXX")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_113() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="garch lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_114() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH LAG ORDER")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_115() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument(None, type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_116() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=None, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_117() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=None, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_118() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help=None)
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_119() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument(type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_120() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_121() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_122() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument(
        "--q",
        type=int,
        default=1,
    )
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_123() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("XX--qXX", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_124() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--Q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_125() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=2, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_126() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="XXARCH lag orderXX")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_127() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="arch lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_128() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH LAG ORDER")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_129() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        None,
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_130() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default=None,
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_131() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=None,
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_132() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help=None,
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_133() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_134() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_135() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_136() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_137() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "XX--distXX",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_138() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--DIST",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_139() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="XXnormalXX",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_140() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="NORMAL",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_141() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["XXnormalXX", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_142() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["NORMAL", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_143() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "XXstudent-tXX", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_144() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "STUDENT-T", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_145() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "XXskewed-tXX", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_146() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "SKEWED-T", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_147() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "XXgedXX"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_148() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "GED"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_149() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="XXConditional distributionXX",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_150() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_151() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="CONDITIONAL DISTRIBUTION",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_152() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        None,
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_153() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default=None,
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_154() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=None,
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_155() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help=None,
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_156() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_157() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_158() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_159() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_160() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "XX--meanXX",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_161() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--MEAN",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_162() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="XXconstantXX",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_163() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="CONSTANT",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_164() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["XXconstantXX", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_165() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["CONSTANT", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_166() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "XXzeroXX"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_167() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "ZERO"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_168() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="XXMean modelXX",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_169() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_170() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="MEAN MODEL",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_171() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument(None, action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_172() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action=None, help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_173() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help=None)
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_174() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument(action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_175() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_176() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument(
        "--variance-targeting",
        action="store_true",
    )
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_177() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("XX--variance-targetingXX", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_178() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--VARIANCE-TARGETING", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_179() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="XXstore_trueXX", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_180() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="STORE_TRUE", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_181() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="XXUse variance targetingXX")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_182() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_183() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="USE VARIANCE TARGETING")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_184() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument(None, default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_185() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default=None, help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_186() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help=None)

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_187() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument(default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_188() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_189() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument(
        "--output",
        default="results.json",
    )

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_190() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("XX--outputXX", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_191() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--OUTPUT", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_192() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="XXresults.jsonXX", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_193() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="RESULTS.JSON", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_194() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="XXOutput file pathXX")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_195() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_196() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="OUTPUT FILE PATH")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_197() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = None
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_198() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser(None, help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_199() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help=None)
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_200() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser(help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_201() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser(
        "risk",
    )
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_202() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("XXriskXX", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_203() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("RISK", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_204() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="XXCompute risk measures (VaR, ES)XX")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_205() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="compute risk measures (var, es)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_206() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="COMPUTE RISK MEASURES (VAR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_207() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        None,
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_208() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=None,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_209() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=None,
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_210() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help=None,
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_211() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_212() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_213() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_214() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_215() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "XX--modelXX",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_216() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--MODEL",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_217() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=False,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_218() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "XXgarchXX",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_219() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "GARCH",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_220() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "XXegarchXX",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_221() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "EGARCH",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_222() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "XXgjrXX",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_223() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "GJR",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_224() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "XXaparchXX",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_225() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "APARCH",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_226() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "XXfigarchXX",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_227() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "FIGARCH",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_228() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "XXigarchXX",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_229() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "IGARCH",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_230() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "XXgarch-mXX",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_231() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "GARCH-M",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_232() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "XXcomponentXX",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_233() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "COMPONENT",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_234() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="XXModel typeXX",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_235() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_236() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="MODEL TYPE",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_237() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument(None, required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_238() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=None, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_239() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help=None)
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_240() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument(required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_241() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_242() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument(
        "--data",
        required=True,
    )
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_243() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("XX--dataXX", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_244() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--DATA", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_245() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=False, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_246() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="XXPath to CSV data fileXX")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_247() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="path to csv data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_248() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="PATH TO CSV DATA FILE")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_249() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument(None, default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_250() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default=None, help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_251() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help=None)
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_252() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument(default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_253() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_254() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument(
        "--column",
        default="returns",
    )
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_255() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("XX--columnXX", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_256() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--COLUMN", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_257() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="XXreturnsXX", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_258() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="RETURNS", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_259() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="XXColumn name for returnsXX")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_260() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_261() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="COLUMN NAME FOR RETURNS")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_262() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument(None, type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_263() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=None, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_264() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=None, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_265() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help=None)
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_266() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument(type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_267() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_268() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_269() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument(
        "--p",
        type=int,
        default=1,
    )
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_270() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("XX--pXX", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_271() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--P", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_272() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=2, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_273() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="XXGARCH lag orderXX")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_274() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="garch lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_275() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH LAG ORDER")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_276() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument(None, type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_277() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=None, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_278() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=None, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_279() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help=None)
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_280() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument(type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_281() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_282() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_283() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument(
        "--q",
        type=int,
        default=1,
    )
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_284() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("XX--qXX", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_285() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--Q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_286() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=2, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_287() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="XXARCH lag orderXX")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_288() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="arch lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_289() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH LAG ORDER")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_290() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        None,
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_291() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default=None,
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_292() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=None,
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_293() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help=None,
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_294() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_295() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_296() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_297() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_298() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "XX--distXX",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_299() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--DIST",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_300() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="XXnormalXX",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_301() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="NORMAL",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_302() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["XXnormalXX", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_303() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["NORMAL", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_304() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "XXstudent-tXX", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_305() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "STUDENT-T", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_306() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "XXskewed-tXX", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_307() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "SKEWED-T", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_308() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "XXgedXX"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_309() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "GED"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_310() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="XXConditional distributionXX",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_311() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_312() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="CONDITIONAL DISTRIBUTION",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_313() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        None,
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_314() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default=None,
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_315() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=None,
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_316() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help=None,
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_317() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_318() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_319() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_320() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_321() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "XX--var-methodXX",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_322() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--VAR-METHOD",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_323() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="XXparametricXX",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_324() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="PARAMETRIC",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_325() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["XXparametricXX", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_326() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["PARAMETRIC", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_327() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "XXhistoricalXX", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_328() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "HISTORICAL", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_329() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "XXfiltered-hsXX", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_330() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "FILTERED-HS", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_331() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "XXmonte-carloXX"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_332() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "MONTE-CARLO"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_333() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="XXVaR computation methodXX",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_334() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="var computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_335() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VAR COMPUTATION METHOD",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_336() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument(None, type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_337() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=None, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_338() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=None, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_339() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help=None)
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_340() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument(type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_341() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_342() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_343() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument(
        "--alpha",
        type=float,
        default=0.05,
    )
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_344() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("XX--alphaXX", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_345() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--ALPHA", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_346() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=1.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_347() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="XXSignificance levelXX")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_348() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_349() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="SIGNIFICANCE LEVEL")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_350() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument(None, default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_351() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default=None, help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_352() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help=None)

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_353() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument(default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_354() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_355() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument(
        "--output",
        default="risk.json",
    )

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_356() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("XX--outputXX", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_357() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--OUTPUT", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_358() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="XXrisk.jsonXX", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_359() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="RISK.JSON", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_360() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="XXOutput file pathXX")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_361() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_362() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="OUTPUT FILE PATH")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_363() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = None
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_364() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser(None, help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_365() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help=None)
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_366() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser(help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_367() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser(
        "backtest",
    )
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_368() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("XXbacktestXX", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_369() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("BACKTEST", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_370() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="XXBacktest VaR modelXX")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_371() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="backtest var model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_372() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="BACKTEST VAR MODEL")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_373() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        None,
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_374() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=None,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_375() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=None,
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_376() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help=None,
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_377() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_378() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_379() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_380() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_381() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "XX--modelXX",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_382() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--MODEL",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_383() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=False,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_384() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "XXgarchXX",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_385() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "GARCH",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_386() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "XXegarchXX",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_387() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "EGARCH",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_388() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "XXgjrXX",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_389() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "GJR",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_390() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "XXaparchXX",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_391() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "APARCH",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_392() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "XXfigarchXX",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_393() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "FIGARCH",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_394() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "XXigarchXX",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_395() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "IGARCH",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_396() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "XXgarch-mXX",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_397() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "GARCH-M",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_398() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "XXcomponentXX",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_399() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "COMPONENT",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_400() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="XXModel typeXX",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_401() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_402() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="MODEL TYPE",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_403() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument(None, required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_404() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=None, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_405() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help=None)
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_406() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument(required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_407() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_408() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument(
        "--data",
        required=True,
    )
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_409() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("XX--dataXX", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_410() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--DATA", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_411() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=False, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_412() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="XXPath to CSV data fileXX")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_413() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="path to csv data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_414() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="PATH TO CSV DATA FILE")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_415() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument(None, default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_416() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default=None, help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_417() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help=None)
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_418() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument(default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_419() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_420() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument(
        "--column",
        default="returns",
    )
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_421() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("XX--columnXX", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_422() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--COLUMN", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_423() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="XXreturnsXX", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_424() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="RETURNS", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_425() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="XXColumn name for returnsXX")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_426() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_427() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="COLUMN NAME FOR RETURNS")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_428() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument(None, type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_429() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=None, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_430() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=None, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_431() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help=None)
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_432() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument(type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_433() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_434() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_435() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument(
        "--p",
        type=int,
        default=1,
    )
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_436() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("XX--pXX", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_437() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--P", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_438() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=2, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_439() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="XXGARCH lag orderXX")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_440() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="garch lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_441() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH LAG ORDER")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_442() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument(None, type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_443() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=None, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_444() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=None, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_445() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help=None)
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_446() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument(type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_447() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_448() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_449() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument(
        "--q",
        type=int,
        default=1,
    )
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_450() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("XX--qXX", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_451() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--Q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_452() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=2, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_453() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="XXARCH lag orderXX")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_454() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="arch lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_455() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH LAG ORDER")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_456() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        None,
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_457() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default=None,
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_458() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=None,
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_459() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help=None,
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_460() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_461() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_462() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_463() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_464() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "XX--distXX",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_465() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--DIST",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_466() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="XXnormalXX",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_467() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="NORMAL",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_468() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["XXnormalXX", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_469() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["NORMAL", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_470() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "XXstudent-tXX", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_471() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "STUDENT-T", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_472() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "XXskewed-tXX", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_473() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "SKEWED-T", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_474() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "XXgedXX"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_475() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "GED"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_476() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="XXConditional distributionXX",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_477() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_478() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="CONDITIONAL DISTRIBUTION",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_479() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument(None, type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_480() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=None, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_481() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=None, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_482() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help=None)
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_483() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument(type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_484() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_485() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_486() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument(
        "--alpha",
        type=float,
        default=0.05,
    )
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_487() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("XX--alphaXX", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_488() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--ALPHA", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_489() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=1.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_490() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="XXSignificance levelXX")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_491() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_492() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="SIGNIFICANCE LEVEL")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_493() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument(None, type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_494() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=None, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_495() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=None, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_496() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help=None)
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_497() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument(type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_498() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_499() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_500() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument(
        "--window",
        type=int,
        default=250,
    )
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_501() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("XX--windowXX", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_502() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--WINDOW", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_503() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=251, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_504() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="XXRolling window sizeXX")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_505() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_506() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="ROLLING WINDOW SIZE")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_507() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument(None, default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_508() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default=None, help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_509() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help=None)

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_510() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument(default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_511() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_512() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument(
        "--output",
        default="backtest.json",
    )

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_513() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("XX--outputXX", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_514() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--OUTPUT", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_515() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="XXbacktest.jsonXX", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_516() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="BACKTEST.JSON", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_517() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="XXOutput file pathXX")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_518() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_519() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="OUTPUT FILE PATH")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_520() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = None
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_521() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser(None, help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_522() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help=None)
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_523() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser(help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_524() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser(
        "regime",
    )
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_525() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("XXregimeXX", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_526() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("REGIME", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_527() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="XXEstimate regime-switching modelXX")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_528() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_529() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="ESTIMATE REGIME-SWITCHING MODEL")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_530() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        None,
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_531() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=None,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_532() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=None,
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_533() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help=None,
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_534() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_535() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_536() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_537() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_538() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "XX--modelXX",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_539() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--MODEL",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_540() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=False,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_541() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["XXms-meanXX", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_542() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["MS-MEAN", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_543() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "XXms-arXX", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_544() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "MS-AR", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_545() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "XXms-varXX", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_546() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "MS-VAR", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_547() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "XXms-garchXX"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_548() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "MS-GARCH"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_549() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="XXRegime model typeXX",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_550() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_551() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="REGIME MODEL TYPE",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_552() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument(None, required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_553() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=None, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_554() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help=None)
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_555() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument(required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_556() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_557() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument(
        "--data",
        required=True,
    )
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_558() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("XX--dataXX", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_559() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--DATA", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_560() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=False, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_561() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="XXPath to CSV data fileXX")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_562() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="path to csv data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_563() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="PATH TO CSV DATA FILE")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_564() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument(None, default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_565() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default=None, help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_566() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help=None)
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_567() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument(default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_568() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_569() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument(
        "--column",
        default="returns",
    )
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_570() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("XX--columnXX", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_571() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--COLUMN", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_572() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="XXreturnsXX", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_573() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="RETURNS", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_574() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="XXColumn name for returnsXX")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_575() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_576() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="COLUMN NAME FOR RETURNS")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_577() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument(None, type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_578() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=None, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_579() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=None, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_580() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help=None)
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_581() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument(type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_582() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_583() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_584() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument(
        "--k-regimes",
        type=int,
        default=2,
    )
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_585() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("XX--k-regimesXX", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_586() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--K-REGIMES", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_587() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=3, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_588() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="XXNumber of regimesXX")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_589() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_590() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="NUMBER OF REGIMES")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_591() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument(None, type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_592() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=None, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_593() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=None, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_594() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help=None)
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_595() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument(type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_596() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_597() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_598() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument(
        "--order",
        type=int,
        default=1,
    )
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_599() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("XX--orderXX", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_600() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--ORDER", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_601() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=2, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_602() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="XXAR orderXX")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_603() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="ar order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_604() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR ORDER")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_605() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        None,
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_606() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default=None,
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_607() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=None,
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_608() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help=None,
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_609() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_610() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_611() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_612() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_613() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "XX--methodXX",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_614() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--METHOD",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_615() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="XXemXX",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_616() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="EM",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_617() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["XXemXX", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_618() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["EM", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_619() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "XXmleXX"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_620() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "MLE"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_621() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="XXEstimation methodXX",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_622() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_623() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="ESTIMATION METHOD",
    )
    reg.add_argument("--output", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_624() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument(None, default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_625() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default=None, help="Output file path")

    return parser


def x_build_parser__mutmut_626() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help=None)

    return parser


def x_build_parser__mutmut_627() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument(default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_628() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", help="Output file path")

    return parser


def x_build_parser__mutmut_629() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument(
        "--output",
        default="regime.json",
    )

    return parser


def x_build_parser__mutmut_630() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("XX--outputXX", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_631() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--OUTPUT", default="regime.json", help="Output file path")

    return parser


def x_build_parser__mutmut_632() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="XXregime.jsonXX", help="Output file path")

    return parser


def x_build_parser__mutmut_633() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="REGIME.JSON", help="Output file path")

    return parser


def x_build_parser__mutmut_634() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="XXOutput file pathXX")

    return parser


def x_build_parser__mutmut_635() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="output file path")

    return parser


def x_build_parser__mutmut_636() -> argparse.ArgumentParser:
    """Build the main argument parser with subparsers."""
    parser = argparse.ArgumentParser(
        prog="archbox",
        description="ArchBox - ARCH/GARCH volatility models for financial time series",
    )
    parser.add_argument(
        "--version",
        action="store_true",
        help="Show version and exit",
    )

    subparsers = parser.add_subparsers(dest="command", help="Available commands")

    # --- estimate ---
    est = subparsers.add_parser("estimate", help="Estimate a volatility model")
    est.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
            "har-rv",
        ],
        help="Model type",
    )
    est.add_argument("--data", required=True, help="Path to CSV data file")
    est.add_argument("--column", default="returns", help="Column name for returns")
    est.add_argument("--p", type=int, default=1, help="GARCH lag order")
    est.add_argument("--q", type=int, default=1, help="ARCH lag order")
    est.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    est.add_argument(
        "--mean",
        default="constant",
        choices=["constant", "zero"],
        help="Mean model",
    )
    est.add_argument("--variance-targeting", action="store_true", help="Use variance targeting")
    est.add_argument("--output", default="results.json", help="Output file path")

    # --- risk ---
    risk = subparsers.add_parser("risk", help="Compute risk measures (VaR, ES)")
    risk.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    risk.add_argument("--data", required=True, help="Path to CSV data file")
    risk.add_argument("--column", default="returns", help="Column name for returns")
    risk.add_argument("--p", type=int, default=1, help="GARCH lag order")
    risk.add_argument("--q", type=int, default=1, help="ARCH lag order")
    risk.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    risk.add_argument(
        "--var-method",
        default="parametric",
        choices=["parametric", "historical", "filtered-hs", "monte-carlo"],
        help="VaR computation method",
    )
    risk.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    risk.add_argument("--output", default="risk.json", help="Output file path")

    # --- backtest ---
    bt = subparsers.add_parser("backtest", help="Backtest VaR model")
    bt.add_argument(
        "--model",
        required=True,
        choices=[
            "garch",
            "egarch",
            "gjr",
            "aparch",
            "figarch",
            "igarch",
            "garch-m",
            "component",
        ],
        help="Model type",
    )
    bt.add_argument("--data", required=True, help="Path to CSV data file")
    bt.add_argument("--column", default="returns", help="Column name for returns")
    bt.add_argument("--p", type=int, default=1, help="GARCH lag order")
    bt.add_argument("--q", type=int, default=1, help="ARCH lag order")
    bt.add_argument(
        "--dist",
        default="normal",
        choices=["normal", "student-t", "skewed-t", "ged"],
        help="Conditional distribution",
    )
    bt.add_argument("--alpha", type=float, default=0.05, help="Significance level")
    bt.add_argument("--window", type=int, default=250, help="Rolling window size")
    bt.add_argument("--output", default="backtest.json", help="Output file path")

    # --- regime ---
    reg = subparsers.add_parser("regime", help="Estimate regime-switching model")
    reg.add_argument(
        "--model",
        required=True,
        choices=["ms-mean", "ms-ar", "ms-var", "ms-garch"],
        help="Regime model type",
    )
    reg.add_argument("--data", required=True, help="Path to CSV data file")
    reg.add_argument("--column", default="returns", help="Column name for returns")
    reg.add_argument("--k-regimes", type=int, default=2, help="Number of regimes")
    reg.add_argument("--order", type=int, default=1, help="AR order")
    reg.add_argument(
        "--method",
        default="em",
        choices=["em", "mle"],
        help="Estimation method",
    )
    reg.add_argument("--output", default="regime.json", help="OUTPUT FILE PATH")

    return parser


x_build_parser__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_build_parser__mutmut_1": x_build_parser__mutmut_1,
    "x_build_parser__mutmut_2": x_build_parser__mutmut_2,
    "x_build_parser__mutmut_3": x_build_parser__mutmut_3,
    "x_build_parser__mutmut_4": x_build_parser__mutmut_4,
    "x_build_parser__mutmut_5": x_build_parser__mutmut_5,
    "x_build_parser__mutmut_6": x_build_parser__mutmut_6,
    "x_build_parser__mutmut_7": x_build_parser__mutmut_7,
    "x_build_parser__mutmut_8": x_build_parser__mutmut_8,
    "x_build_parser__mutmut_9": x_build_parser__mutmut_9,
    "x_build_parser__mutmut_10": x_build_parser__mutmut_10,
    "x_build_parser__mutmut_11": x_build_parser__mutmut_11,
    "x_build_parser__mutmut_12": x_build_parser__mutmut_12,
    "x_build_parser__mutmut_13": x_build_parser__mutmut_13,
    "x_build_parser__mutmut_14": x_build_parser__mutmut_14,
    "x_build_parser__mutmut_15": x_build_parser__mutmut_15,
    "x_build_parser__mutmut_16": x_build_parser__mutmut_16,
    "x_build_parser__mutmut_17": x_build_parser__mutmut_17,
    "x_build_parser__mutmut_18": x_build_parser__mutmut_18,
    "x_build_parser__mutmut_19": x_build_parser__mutmut_19,
    "x_build_parser__mutmut_20": x_build_parser__mutmut_20,
    "x_build_parser__mutmut_21": x_build_parser__mutmut_21,
    "x_build_parser__mutmut_22": x_build_parser__mutmut_22,
    "x_build_parser__mutmut_23": x_build_parser__mutmut_23,
    "x_build_parser__mutmut_24": x_build_parser__mutmut_24,
    "x_build_parser__mutmut_25": x_build_parser__mutmut_25,
    "x_build_parser__mutmut_26": x_build_parser__mutmut_26,
    "x_build_parser__mutmut_27": x_build_parser__mutmut_27,
    "x_build_parser__mutmut_28": x_build_parser__mutmut_28,
    "x_build_parser__mutmut_29": x_build_parser__mutmut_29,
    "x_build_parser__mutmut_30": x_build_parser__mutmut_30,
    "x_build_parser__mutmut_31": x_build_parser__mutmut_31,
    "x_build_parser__mutmut_32": x_build_parser__mutmut_32,
    "x_build_parser__mutmut_33": x_build_parser__mutmut_33,
    "x_build_parser__mutmut_34": x_build_parser__mutmut_34,
    "x_build_parser__mutmut_35": x_build_parser__mutmut_35,
    "x_build_parser__mutmut_36": x_build_parser__mutmut_36,
    "x_build_parser__mutmut_37": x_build_parser__mutmut_37,
    "x_build_parser__mutmut_38": x_build_parser__mutmut_38,
    "x_build_parser__mutmut_39": x_build_parser__mutmut_39,
    "x_build_parser__mutmut_40": x_build_parser__mutmut_40,
    "x_build_parser__mutmut_41": x_build_parser__mutmut_41,
    "x_build_parser__mutmut_42": x_build_parser__mutmut_42,
    "x_build_parser__mutmut_43": x_build_parser__mutmut_43,
    "x_build_parser__mutmut_44": x_build_parser__mutmut_44,
    "x_build_parser__mutmut_45": x_build_parser__mutmut_45,
    "x_build_parser__mutmut_46": x_build_parser__mutmut_46,
    "x_build_parser__mutmut_47": x_build_parser__mutmut_47,
    "x_build_parser__mutmut_48": x_build_parser__mutmut_48,
    "x_build_parser__mutmut_49": x_build_parser__mutmut_49,
    "x_build_parser__mutmut_50": x_build_parser__mutmut_50,
    "x_build_parser__mutmut_51": x_build_parser__mutmut_51,
    "x_build_parser__mutmut_52": x_build_parser__mutmut_52,
    "x_build_parser__mutmut_53": x_build_parser__mutmut_53,
    "x_build_parser__mutmut_54": x_build_parser__mutmut_54,
    "x_build_parser__mutmut_55": x_build_parser__mutmut_55,
    "x_build_parser__mutmut_56": x_build_parser__mutmut_56,
    "x_build_parser__mutmut_57": x_build_parser__mutmut_57,
    "x_build_parser__mutmut_58": x_build_parser__mutmut_58,
    "x_build_parser__mutmut_59": x_build_parser__mutmut_59,
    "x_build_parser__mutmut_60": x_build_parser__mutmut_60,
    "x_build_parser__mutmut_61": x_build_parser__mutmut_61,
    "x_build_parser__mutmut_62": x_build_parser__mutmut_62,
    "x_build_parser__mutmut_63": x_build_parser__mutmut_63,
    "x_build_parser__mutmut_64": x_build_parser__mutmut_64,
    "x_build_parser__mutmut_65": x_build_parser__mutmut_65,
    "x_build_parser__mutmut_66": x_build_parser__mutmut_66,
    "x_build_parser__mutmut_67": x_build_parser__mutmut_67,
    "x_build_parser__mutmut_68": x_build_parser__mutmut_68,
    "x_build_parser__mutmut_69": x_build_parser__mutmut_69,
    "x_build_parser__mutmut_70": x_build_parser__mutmut_70,
    "x_build_parser__mutmut_71": x_build_parser__mutmut_71,
    "x_build_parser__mutmut_72": x_build_parser__mutmut_72,
    "x_build_parser__mutmut_73": x_build_parser__mutmut_73,
    "x_build_parser__mutmut_74": x_build_parser__mutmut_74,
    "x_build_parser__mutmut_75": x_build_parser__mutmut_75,
    "x_build_parser__mutmut_76": x_build_parser__mutmut_76,
    "x_build_parser__mutmut_77": x_build_parser__mutmut_77,
    "x_build_parser__mutmut_78": x_build_parser__mutmut_78,
    "x_build_parser__mutmut_79": x_build_parser__mutmut_79,
    "x_build_parser__mutmut_80": x_build_parser__mutmut_80,
    "x_build_parser__mutmut_81": x_build_parser__mutmut_81,
    "x_build_parser__mutmut_82": x_build_parser__mutmut_82,
    "x_build_parser__mutmut_83": x_build_parser__mutmut_83,
    "x_build_parser__mutmut_84": x_build_parser__mutmut_84,
    "x_build_parser__mutmut_85": x_build_parser__mutmut_85,
    "x_build_parser__mutmut_86": x_build_parser__mutmut_86,
    "x_build_parser__mutmut_87": x_build_parser__mutmut_87,
    "x_build_parser__mutmut_88": x_build_parser__mutmut_88,
    "x_build_parser__mutmut_89": x_build_parser__mutmut_89,
    "x_build_parser__mutmut_90": x_build_parser__mutmut_90,
    "x_build_parser__mutmut_91": x_build_parser__mutmut_91,
    "x_build_parser__mutmut_92": x_build_parser__mutmut_92,
    "x_build_parser__mutmut_93": x_build_parser__mutmut_93,
    "x_build_parser__mutmut_94": x_build_parser__mutmut_94,
    "x_build_parser__mutmut_95": x_build_parser__mutmut_95,
    "x_build_parser__mutmut_96": x_build_parser__mutmut_96,
    "x_build_parser__mutmut_97": x_build_parser__mutmut_97,
    "x_build_parser__mutmut_98": x_build_parser__mutmut_98,
    "x_build_parser__mutmut_99": x_build_parser__mutmut_99,
    "x_build_parser__mutmut_100": x_build_parser__mutmut_100,
    "x_build_parser__mutmut_101": x_build_parser__mutmut_101,
    "x_build_parser__mutmut_102": x_build_parser__mutmut_102,
    "x_build_parser__mutmut_103": x_build_parser__mutmut_103,
    "x_build_parser__mutmut_104": x_build_parser__mutmut_104,
    "x_build_parser__mutmut_105": x_build_parser__mutmut_105,
    "x_build_parser__mutmut_106": x_build_parser__mutmut_106,
    "x_build_parser__mutmut_107": x_build_parser__mutmut_107,
    "x_build_parser__mutmut_108": x_build_parser__mutmut_108,
    "x_build_parser__mutmut_109": x_build_parser__mutmut_109,
    "x_build_parser__mutmut_110": x_build_parser__mutmut_110,
    "x_build_parser__mutmut_111": x_build_parser__mutmut_111,
    "x_build_parser__mutmut_112": x_build_parser__mutmut_112,
    "x_build_parser__mutmut_113": x_build_parser__mutmut_113,
    "x_build_parser__mutmut_114": x_build_parser__mutmut_114,
    "x_build_parser__mutmut_115": x_build_parser__mutmut_115,
    "x_build_parser__mutmut_116": x_build_parser__mutmut_116,
    "x_build_parser__mutmut_117": x_build_parser__mutmut_117,
    "x_build_parser__mutmut_118": x_build_parser__mutmut_118,
    "x_build_parser__mutmut_119": x_build_parser__mutmut_119,
    "x_build_parser__mutmut_120": x_build_parser__mutmut_120,
    "x_build_parser__mutmut_121": x_build_parser__mutmut_121,
    "x_build_parser__mutmut_122": x_build_parser__mutmut_122,
    "x_build_parser__mutmut_123": x_build_parser__mutmut_123,
    "x_build_parser__mutmut_124": x_build_parser__mutmut_124,
    "x_build_parser__mutmut_125": x_build_parser__mutmut_125,
    "x_build_parser__mutmut_126": x_build_parser__mutmut_126,
    "x_build_parser__mutmut_127": x_build_parser__mutmut_127,
    "x_build_parser__mutmut_128": x_build_parser__mutmut_128,
    "x_build_parser__mutmut_129": x_build_parser__mutmut_129,
    "x_build_parser__mutmut_130": x_build_parser__mutmut_130,
    "x_build_parser__mutmut_131": x_build_parser__mutmut_131,
    "x_build_parser__mutmut_132": x_build_parser__mutmut_132,
    "x_build_parser__mutmut_133": x_build_parser__mutmut_133,
    "x_build_parser__mutmut_134": x_build_parser__mutmut_134,
    "x_build_parser__mutmut_135": x_build_parser__mutmut_135,
    "x_build_parser__mutmut_136": x_build_parser__mutmut_136,
    "x_build_parser__mutmut_137": x_build_parser__mutmut_137,
    "x_build_parser__mutmut_138": x_build_parser__mutmut_138,
    "x_build_parser__mutmut_139": x_build_parser__mutmut_139,
    "x_build_parser__mutmut_140": x_build_parser__mutmut_140,
    "x_build_parser__mutmut_141": x_build_parser__mutmut_141,
    "x_build_parser__mutmut_142": x_build_parser__mutmut_142,
    "x_build_parser__mutmut_143": x_build_parser__mutmut_143,
    "x_build_parser__mutmut_144": x_build_parser__mutmut_144,
    "x_build_parser__mutmut_145": x_build_parser__mutmut_145,
    "x_build_parser__mutmut_146": x_build_parser__mutmut_146,
    "x_build_parser__mutmut_147": x_build_parser__mutmut_147,
    "x_build_parser__mutmut_148": x_build_parser__mutmut_148,
    "x_build_parser__mutmut_149": x_build_parser__mutmut_149,
    "x_build_parser__mutmut_150": x_build_parser__mutmut_150,
    "x_build_parser__mutmut_151": x_build_parser__mutmut_151,
    "x_build_parser__mutmut_152": x_build_parser__mutmut_152,
    "x_build_parser__mutmut_153": x_build_parser__mutmut_153,
    "x_build_parser__mutmut_154": x_build_parser__mutmut_154,
    "x_build_parser__mutmut_155": x_build_parser__mutmut_155,
    "x_build_parser__mutmut_156": x_build_parser__mutmut_156,
    "x_build_parser__mutmut_157": x_build_parser__mutmut_157,
    "x_build_parser__mutmut_158": x_build_parser__mutmut_158,
    "x_build_parser__mutmut_159": x_build_parser__mutmut_159,
    "x_build_parser__mutmut_160": x_build_parser__mutmut_160,
    "x_build_parser__mutmut_161": x_build_parser__mutmut_161,
    "x_build_parser__mutmut_162": x_build_parser__mutmut_162,
    "x_build_parser__mutmut_163": x_build_parser__mutmut_163,
    "x_build_parser__mutmut_164": x_build_parser__mutmut_164,
    "x_build_parser__mutmut_165": x_build_parser__mutmut_165,
    "x_build_parser__mutmut_166": x_build_parser__mutmut_166,
    "x_build_parser__mutmut_167": x_build_parser__mutmut_167,
    "x_build_parser__mutmut_168": x_build_parser__mutmut_168,
    "x_build_parser__mutmut_169": x_build_parser__mutmut_169,
    "x_build_parser__mutmut_170": x_build_parser__mutmut_170,
    "x_build_parser__mutmut_171": x_build_parser__mutmut_171,
    "x_build_parser__mutmut_172": x_build_parser__mutmut_172,
    "x_build_parser__mutmut_173": x_build_parser__mutmut_173,
    "x_build_parser__mutmut_174": x_build_parser__mutmut_174,
    "x_build_parser__mutmut_175": x_build_parser__mutmut_175,
    "x_build_parser__mutmut_176": x_build_parser__mutmut_176,
    "x_build_parser__mutmut_177": x_build_parser__mutmut_177,
    "x_build_parser__mutmut_178": x_build_parser__mutmut_178,
    "x_build_parser__mutmut_179": x_build_parser__mutmut_179,
    "x_build_parser__mutmut_180": x_build_parser__mutmut_180,
    "x_build_parser__mutmut_181": x_build_parser__mutmut_181,
    "x_build_parser__mutmut_182": x_build_parser__mutmut_182,
    "x_build_parser__mutmut_183": x_build_parser__mutmut_183,
    "x_build_parser__mutmut_184": x_build_parser__mutmut_184,
    "x_build_parser__mutmut_185": x_build_parser__mutmut_185,
    "x_build_parser__mutmut_186": x_build_parser__mutmut_186,
    "x_build_parser__mutmut_187": x_build_parser__mutmut_187,
    "x_build_parser__mutmut_188": x_build_parser__mutmut_188,
    "x_build_parser__mutmut_189": x_build_parser__mutmut_189,
    "x_build_parser__mutmut_190": x_build_parser__mutmut_190,
    "x_build_parser__mutmut_191": x_build_parser__mutmut_191,
    "x_build_parser__mutmut_192": x_build_parser__mutmut_192,
    "x_build_parser__mutmut_193": x_build_parser__mutmut_193,
    "x_build_parser__mutmut_194": x_build_parser__mutmut_194,
    "x_build_parser__mutmut_195": x_build_parser__mutmut_195,
    "x_build_parser__mutmut_196": x_build_parser__mutmut_196,
    "x_build_parser__mutmut_197": x_build_parser__mutmut_197,
    "x_build_parser__mutmut_198": x_build_parser__mutmut_198,
    "x_build_parser__mutmut_199": x_build_parser__mutmut_199,
    "x_build_parser__mutmut_200": x_build_parser__mutmut_200,
    "x_build_parser__mutmut_201": x_build_parser__mutmut_201,
    "x_build_parser__mutmut_202": x_build_parser__mutmut_202,
    "x_build_parser__mutmut_203": x_build_parser__mutmut_203,
    "x_build_parser__mutmut_204": x_build_parser__mutmut_204,
    "x_build_parser__mutmut_205": x_build_parser__mutmut_205,
    "x_build_parser__mutmut_206": x_build_parser__mutmut_206,
    "x_build_parser__mutmut_207": x_build_parser__mutmut_207,
    "x_build_parser__mutmut_208": x_build_parser__mutmut_208,
    "x_build_parser__mutmut_209": x_build_parser__mutmut_209,
    "x_build_parser__mutmut_210": x_build_parser__mutmut_210,
    "x_build_parser__mutmut_211": x_build_parser__mutmut_211,
    "x_build_parser__mutmut_212": x_build_parser__mutmut_212,
    "x_build_parser__mutmut_213": x_build_parser__mutmut_213,
    "x_build_parser__mutmut_214": x_build_parser__mutmut_214,
    "x_build_parser__mutmut_215": x_build_parser__mutmut_215,
    "x_build_parser__mutmut_216": x_build_parser__mutmut_216,
    "x_build_parser__mutmut_217": x_build_parser__mutmut_217,
    "x_build_parser__mutmut_218": x_build_parser__mutmut_218,
    "x_build_parser__mutmut_219": x_build_parser__mutmut_219,
    "x_build_parser__mutmut_220": x_build_parser__mutmut_220,
    "x_build_parser__mutmut_221": x_build_parser__mutmut_221,
    "x_build_parser__mutmut_222": x_build_parser__mutmut_222,
    "x_build_parser__mutmut_223": x_build_parser__mutmut_223,
    "x_build_parser__mutmut_224": x_build_parser__mutmut_224,
    "x_build_parser__mutmut_225": x_build_parser__mutmut_225,
    "x_build_parser__mutmut_226": x_build_parser__mutmut_226,
    "x_build_parser__mutmut_227": x_build_parser__mutmut_227,
    "x_build_parser__mutmut_228": x_build_parser__mutmut_228,
    "x_build_parser__mutmut_229": x_build_parser__mutmut_229,
    "x_build_parser__mutmut_230": x_build_parser__mutmut_230,
    "x_build_parser__mutmut_231": x_build_parser__mutmut_231,
    "x_build_parser__mutmut_232": x_build_parser__mutmut_232,
    "x_build_parser__mutmut_233": x_build_parser__mutmut_233,
    "x_build_parser__mutmut_234": x_build_parser__mutmut_234,
    "x_build_parser__mutmut_235": x_build_parser__mutmut_235,
    "x_build_parser__mutmut_236": x_build_parser__mutmut_236,
    "x_build_parser__mutmut_237": x_build_parser__mutmut_237,
    "x_build_parser__mutmut_238": x_build_parser__mutmut_238,
    "x_build_parser__mutmut_239": x_build_parser__mutmut_239,
    "x_build_parser__mutmut_240": x_build_parser__mutmut_240,
    "x_build_parser__mutmut_241": x_build_parser__mutmut_241,
    "x_build_parser__mutmut_242": x_build_parser__mutmut_242,
    "x_build_parser__mutmut_243": x_build_parser__mutmut_243,
    "x_build_parser__mutmut_244": x_build_parser__mutmut_244,
    "x_build_parser__mutmut_245": x_build_parser__mutmut_245,
    "x_build_parser__mutmut_246": x_build_parser__mutmut_246,
    "x_build_parser__mutmut_247": x_build_parser__mutmut_247,
    "x_build_parser__mutmut_248": x_build_parser__mutmut_248,
    "x_build_parser__mutmut_249": x_build_parser__mutmut_249,
    "x_build_parser__mutmut_250": x_build_parser__mutmut_250,
    "x_build_parser__mutmut_251": x_build_parser__mutmut_251,
    "x_build_parser__mutmut_252": x_build_parser__mutmut_252,
    "x_build_parser__mutmut_253": x_build_parser__mutmut_253,
    "x_build_parser__mutmut_254": x_build_parser__mutmut_254,
    "x_build_parser__mutmut_255": x_build_parser__mutmut_255,
    "x_build_parser__mutmut_256": x_build_parser__mutmut_256,
    "x_build_parser__mutmut_257": x_build_parser__mutmut_257,
    "x_build_parser__mutmut_258": x_build_parser__mutmut_258,
    "x_build_parser__mutmut_259": x_build_parser__mutmut_259,
    "x_build_parser__mutmut_260": x_build_parser__mutmut_260,
    "x_build_parser__mutmut_261": x_build_parser__mutmut_261,
    "x_build_parser__mutmut_262": x_build_parser__mutmut_262,
    "x_build_parser__mutmut_263": x_build_parser__mutmut_263,
    "x_build_parser__mutmut_264": x_build_parser__mutmut_264,
    "x_build_parser__mutmut_265": x_build_parser__mutmut_265,
    "x_build_parser__mutmut_266": x_build_parser__mutmut_266,
    "x_build_parser__mutmut_267": x_build_parser__mutmut_267,
    "x_build_parser__mutmut_268": x_build_parser__mutmut_268,
    "x_build_parser__mutmut_269": x_build_parser__mutmut_269,
    "x_build_parser__mutmut_270": x_build_parser__mutmut_270,
    "x_build_parser__mutmut_271": x_build_parser__mutmut_271,
    "x_build_parser__mutmut_272": x_build_parser__mutmut_272,
    "x_build_parser__mutmut_273": x_build_parser__mutmut_273,
    "x_build_parser__mutmut_274": x_build_parser__mutmut_274,
    "x_build_parser__mutmut_275": x_build_parser__mutmut_275,
    "x_build_parser__mutmut_276": x_build_parser__mutmut_276,
    "x_build_parser__mutmut_277": x_build_parser__mutmut_277,
    "x_build_parser__mutmut_278": x_build_parser__mutmut_278,
    "x_build_parser__mutmut_279": x_build_parser__mutmut_279,
    "x_build_parser__mutmut_280": x_build_parser__mutmut_280,
    "x_build_parser__mutmut_281": x_build_parser__mutmut_281,
    "x_build_parser__mutmut_282": x_build_parser__mutmut_282,
    "x_build_parser__mutmut_283": x_build_parser__mutmut_283,
    "x_build_parser__mutmut_284": x_build_parser__mutmut_284,
    "x_build_parser__mutmut_285": x_build_parser__mutmut_285,
    "x_build_parser__mutmut_286": x_build_parser__mutmut_286,
    "x_build_parser__mutmut_287": x_build_parser__mutmut_287,
    "x_build_parser__mutmut_288": x_build_parser__mutmut_288,
    "x_build_parser__mutmut_289": x_build_parser__mutmut_289,
    "x_build_parser__mutmut_290": x_build_parser__mutmut_290,
    "x_build_parser__mutmut_291": x_build_parser__mutmut_291,
    "x_build_parser__mutmut_292": x_build_parser__mutmut_292,
    "x_build_parser__mutmut_293": x_build_parser__mutmut_293,
    "x_build_parser__mutmut_294": x_build_parser__mutmut_294,
    "x_build_parser__mutmut_295": x_build_parser__mutmut_295,
    "x_build_parser__mutmut_296": x_build_parser__mutmut_296,
    "x_build_parser__mutmut_297": x_build_parser__mutmut_297,
    "x_build_parser__mutmut_298": x_build_parser__mutmut_298,
    "x_build_parser__mutmut_299": x_build_parser__mutmut_299,
    "x_build_parser__mutmut_300": x_build_parser__mutmut_300,
    "x_build_parser__mutmut_301": x_build_parser__mutmut_301,
    "x_build_parser__mutmut_302": x_build_parser__mutmut_302,
    "x_build_parser__mutmut_303": x_build_parser__mutmut_303,
    "x_build_parser__mutmut_304": x_build_parser__mutmut_304,
    "x_build_parser__mutmut_305": x_build_parser__mutmut_305,
    "x_build_parser__mutmut_306": x_build_parser__mutmut_306,
    "x_build_parser__mutmut_307": x_build_parser__mutmut_307,
    "x_build_parser__mutmut_308": x_build_parser__mutmut_308,
    "x_build_parser__mutmut_309": x_build_parser__mutmut_309,
    "x_build_parser__mutmut_310": x_build_parser__mutmut_310,
    "x_build_parser__mutmut_311": x_build_parser__mutmut_311,
    "x_build_parser__mutmut_312": x_build_parser__mutmut_312,
    "x_build_parser__mutmut_313": x_build_parser__mutmut_313,
    "x_build_parser__mutmut_314": x_build_parser__mutmut_314,
    "x_build_parser__mutmut_315": x_build_parser__mutmut_315,
    "x_build_parser__mutmut_316": x_build_parser__mutmut_316,
    "x_build_parser__mutmut_317": x_build_parser__mutmut_317,
    "x_build_parser__mutmut_318": x_build_parser__mutmut_318,
    "x_build_parser__mutmut_319": x_build_parser__mutmut_319,
    "x_build_parser__mutmut_320": x_build_parser__mutmut_320,
    "x_build_parser__mutmut_321": x_build_parser__mutmut_321,
    "x_build_parser__mutmut_322": x_build_parser__mutmut_322,
    "x_build_parser__mutmut_323": x_build_parser__mutmut_323,
    "x_build_parser__mutmut_324": x_build_parser__mutmut_324,
    "x_build_parser__mutmut_325": x_build_parser__mutmut_325,
    "x_build_parser__mutmut_326": x_build_parser__mutmut_326,
    "x_build_parser__mutmut_327": x_build_parser__mutmut_327,
    "x_build_parser__mutmut_328": x_build_parser__mutmut_328,
    "x_build_parser__mutmut_329": x_build_parser__mutmut_329,
    "x_build_parser__mutmut_330": x_build_parser__mutmut_330,
    "x_build_parser__mutmut_331": x_build_parser__mutmut_331,
    "x_build_parser__mutmut_332": x_build_parser__mutmut_332,
    "x_build_parser__mutmut_333": x_build_parser__mutmut_333,
    "x_build_parser__mutmut_334": x_build_parser__mutmut_334,
    "x_build_parser__mutmut_335": x_build_parser__mutmut_335,
    "x_build_parser__mutmut_336": x_build_parser__mutmut_336,
    "x_build_parser__mutmut_337": x_build_parser__mutmut_337,
    "x_build_parser__mutmut_338": x_build_parser__mutmut_338,
    "x_build_parser__mutmut_339": x_build_parser__mutmut_339,
    "x_build_parser__mutmut_340": x_build_parser__mutmut_340,
    "x_build_parser__mutmut_341": x_build_parser__mutmut_341,
    "x_build_parser__mutmut_342": x_build_parser__mutmut_342,
    "x_build_parser__mutmut_343": x_build_parser__mutmut_343,
    "x_build_parser__mutmut_344": x_build_parser__mutmut_344,
    "x_build_parser__mutmut_345": x_build_parser__mutmut_345,
    "x_build_parser__mutmut_346": x_build_parser__mutmut_346,
    "x_build_parser__mutmut_347": x_build_parser__mutmut_347,
    "x_build_parser__mutmut_348": x_build_parser__mutmut_348,
    "x_build_parser__mutmut_349": x_build_parser__mutmut_349,
    "x_build_parser__mutmut_350": x_build_parser__mutmut_350,
    "x_build_parser__mutmut_351": x_build_parser__mutmut_351,
    "x_build_parser__mutmut_352": x_build_parser__mutmut_352,
    "x_build_parser__mutmut_353": x_build_parser__mutmut_353,
    "x_build_parser__mutmut_354": x_build_parser__mutmut_354,
    "x_build_parser__mutmut_355": x_build_parser__mutmut_355,
    "x_build_parser__mutmut_356": x_build_parser__mutmut_356,
    "x_build_parser__mutmut_357": x_build_parser__mutmut_357,
    "x_build_parser__mutmut_358": x_build_parser__mutmut_358,
    "x_build_parser__mutmut_359": x_build_parser__mutmut_359,
    "x_build_parser__mutmut_360": x_build_parser__mutmut_360,
    "x_build_parser__mutmut_361": x_build_parser__mutmut_361,
    "x_build_parser__mutmut_362": x_build_parser__mutmut_362,
    "x_build_parser__mutmut_363": x_build_parser__mutmut_363,
    "x_build_parser__mutmut_364": x_build_parser__mutmut_364,
    "x_build_parser__mutmut_365": x_build_parser__mutmut_365,
    "x_build_parser__mutmut_366": x_build_parser__mutmut_366,
    "x_build_parser__mutmut_367": x_build_parser__mutmut_367,
    "x_build_parser__mutmut_368": x_build_parser__mutmut_368,
    "x_build_parser__mutmut_369": x_build_parser__mutmut_369,
    "x_build_parser__mutmut_370": x_build_parser__mutmut_370,
    "x_build_parser__mutmut_371": x_build_parser__mutmut_371,
    "x_build_parser__mutmut_372": x_build_parser__mutmut_372,
    "x_build_parser__mutmut_373": x_build_parser__mutmut_373,
    "x_build_parser__mutmut_374": x_build_parser__mutmut_374,
    "x_build_parser__mutmut_375": x_build_parser__mutmut_375,
    "x_build_parser__mutmut_376": x_build_parser__mutmut_376,
    "x_build_parser__mutmut_377": x_build_parser__mutmut_377,
    "x_build_parser__mutmut_378": x_build_parser__mutmut_378,
    "x_build_parser__mutmut_379": x_build_parser__mutmut_379,
    "x_build_parser__mutmut_380": x_build_parser__mutmut_380,
    "x_build_parser__mutmut_381": x_build_parser__mutmut_381,
    "x_build_parser__mutmut_382": x_build_parser__mutmut_382,
    "x_build_parser__mutmut_383": x_build_parser__mutmut_383,
    "x_build_parser__mutmut_384": x_build_parser__mutmut_384,
    "x_build_parser__mutmut_385": x_build_parser__mutmut_385,
    "x_build_parser__mutmut_386": x_build_parser__mutmut_386,
    "x_build_parser__mutmut_387": x_build_parser__mutmut_387,
    "x_build_parser__mutmut_388": x_build_parser__mutmut_388,
    "x_build_parser__mutmut_389": x_build_parser__mutmut_389,
    "x_build_parser__mutmut_390": x_build_parser__mutmut_390,
    "x_build_parser__mutmut_391": x_build_parser__mutmut_391,
    "x_build_parser__mutmut_392": x_build_parser__mutmut_392,
    "x_build_parser__mutmut_393": x_build_parser__mutmut_393,
    "x_build_parser__mutmut_394": x_build_parser__mutmut_394,
    "x_build_parser__mutmut_395": x_build_parser__mutmut_395,
    "x_build_parser__mutmut_396": x_build_parser__mutmut_396,
    "x_build_parser__mutmut_397": x_build_parser__mutmut_397,
    "x_build_parser__mutmut_398": x_build_parser__mutmut_398,
    "x_build_parser__mutmut_399": x_build_parser__mutmut_399,
    "x_build_parser__mutmut_400": x_build_parser__mutmut_400,
    "x_build_parser__mutmut_401": x_build_parser__mutmut_401,
    "x_build_parser__mutmut_402": x_build_parser__mutmut_402,
    "x_build_parser__mutmut_403": x_build_parser__mutmut_403,
    "x_build_parser__mutmut_404": x_build_parser__mutmut_404,
    "x_build_parser__mutmut_405": x_build_parser__mutmut_405,
    "x_build_parser__mutmut_406": x_build_parser__mutmut_406,
    "x_build_parser__mutmut_407": x_build_parser__mutmut_407,
    "x_build_parser__mutmut_408": x_build_parser__mutmut_408,
    "x_build_parser__mutmut_409": x_build_parser__mutmut_409,
    "x_build_parser__mutmut_410": x_build_parser__mutmut_410,
    "x_build_parser__mutmut_411": x_build_parser__mutmut_411,
    "x_build_parser__mutmut_412": x_build_parser__mutmut_412,
    "x_build_parser__mutmut_413": x_build_parser__mutmut_413,
    "x_build_parser__mutmut_414": x_build_parser__mutmut_414,
    "x_build_parser__mutmut_415": x_build_parser__mutmut_415,
    "x_build_parser__mutmut_416": x_build_parser__mutmut_416,
    "x_build_parser__mutmut_417": x_build_parser__mutmut_417,
    "x_build_parser__mutmut_418": x_build_parser__mutmut_418,
    "x_build_parser__mutmut_419": x_build_parser__mutmut_419,
    "x_build_parser__mutmut_420": x_build_parser__mutmut_420,
    "x_build_parser__mutmut_421": x_build_parser__mutmut_421,
    "x_build_parser__mutmut_422": x_build_parser__mutmut_422,
    "x_build_parser__mutmut_423": x_build_parser__mutmut_423,
    "x_build_parser__mutmut_424": x_build_parser__mutmut_424,
    "x_build_parser__mutmut_425": x_build_parser__mutmut_425,
    "x_build_parser__mutmut_426": x_build_parser__mutmut_426,
    "x_build_parser__mutmut_427": x_build_parser__mutmut_427,
    "x_build_parser__mutmut_428": x_build_parser__mutmut_428,
    "x_build_parser__mutmut_429": x_build_parser__mutmut_429,
    "x_build_parser__mutmut_430": x_build_parser__mutmut_430,
    "x_build_parser__mutmut_431": x_build_parser__mutmut_431,
    "x_build_parser__mutmut_432": x_build_parser__mutmut_432,
    "x_build_parser__mutmut_433": x_build_parser__mutmut_433,
    "x_build_parser__mutmut_434": x_build_parser__mutmut_434,
    "x_build_parser__mutmut_435": x_build_parser__mutmut_435,
    "x_build_parser__mutmut_436": x_build_parser__mutmut_436,
    "x_build_parser__mutmut_437": x_build_parser__mutmut_437,
    "x_build_parser__mutmut_438": x_build_parser__mutmut_438,
    "x_build_parser__mutmut_439": x_build_parser__mutmut_439,
    "x_build_parser__mutmut_440": x_build_parser__mutmut_440,
    "x_build_parser__mutmut_441": x_build_parser__mutmut_441,
    "x_build_parser__mutmut_442": x_build_parser__mutmut_442,
    "x_build_parser__mutmut_443": x_build_parser__mutmut_443,
    "x_build_parser__mutmut_444": x_build_parser__mutmut_444,
    "x_build_parser__mutmut_445": x_build_parser__mutmut_445,
    "x_build_parser__mutmut_446": x_build_parser__mutmut_446,
    "x_build_parser__mutmut_447": x_build_parser__mutmut_447,
    "x_build_parser__mutmut_448": x_build_parser__mutmut_448,
    "x_build_parser__mutmut_449": x_build_parser__mutmut_449,
    "x_build_parser__mutmut_450": x_build_parser__mutmut_450,
    "x_build_parser__mutmut_451": x_build_parser__mutmut_451,
    "x_build_parser__mutmut_452": x_build_parser__mutmut_452,
    "x_build_parser__mutmut_453": x_build_parser__mutmut_453,
    "x_build_parser__mutmut_454": x_build_parser__mutmut_454,
    "x_build_parser__mutmut_455": x_build_parser__mutmut_455,
    "x_build_parser__mutmut_456": x_build_parser__mutmut_456,
    "x_build_parser__mutmut_457": x_build_parser__mutmut_457,
    "x_build_parser__mutmut_458": x_build_parser__mutmut_458,
    "x_build_parser__mutmut_459": x_build_parser__mutmut_459,
    "x_build_parser__mutmut_460": x_build_parser__mutmut_460,
    "x_build_parser__mutmut_461": x_build_parser__mutmut_461,
    "x_build_parser__mutmut_462": x_build_parser__mutmut_462,
    "x_build_parser__mutmut_463": x_build_parser__mutmut_463,
    "x_build_parser__mutmut_464": x_build_parser__mutmut_464,
    "x_build_parser__mutmut_465": x_build_parser__mutmut_465,
    "x_build_parser__mutmut_466": x_build_parser__mutmut_466,
    "x_build_parser__mutmut_467": x_build_parser__mutmut_467,
    "x_build_parser__mutmut_468": x_build_parser__mutmut_468,
    "x_build_parser__mutmut_469": x_build_parser__mutmut_469,
    "x_build_parser__mutmut_470": x_build_parser__mutmut_470,
    "x_build_parser__mutmut_471": x_build_parser__mutmut_471,
    "x_build_parser__mutmut_472": x_build_parser__mutmut_472,
    "x_build_parser__mutmut_473": x_build_parser__mutmut_473,
    "x_build_parser__mutmut_474": x_build_parser__mutmut_474,
    "x_build_parser__mutmut_475": x_build_parser__mutmut_475,
    "x_build_parser__mutmut_476": x_build_parser__mutmut_476,
    "x_build_parser__mutmut_477": x_build_parser__mutmut_477,
    "x_build_parser__mutmut_478": x_build_parser__mutmut_478,
    "x_build_parser__mutmut_479": x_build_parser__mutmut_479,
    "x_build_parser__mutmut_480": x_build_parser__mutmut_480,
    "x_build_parser__mutmut_481": x_build_parser__mutmut_481,
    "x_build_parser__mutmut_482": x_build_parser__mutmut_482,
    "x_build_parser__mutmut_483": x_build_parser__mutmut_483,
    "x_build_parser__mutmut_484": x_build_parser__mutmut_484,
    "x_build_parser__mutmut_485": x_build_parser__mutmut_485,
    "x_build_parser__mutmut_486": x_build_parser__mutmut_486,
    "x_build_parser__mutmut_487": x_build_parser__mutmut_487,
    "x_build_parser__mutmut_488": x_build_parser__mutmut_488,
    "x_build_parser__mutmut_489": x_build_parser__mutmut_489,
    "x_build_parser__mutmut_490": x_build_parser__mutmut_490,
    "x_build_parser__mutmut_491": x_build_parser__mutmut_491,
    "x_build_parser__mutmut_492": x_build_parser__mutmut_492,
    "x_build_parser__mutmut_493": x_build_parser__mutmut_493,
    "x_build_parser__mutmut_494": x_build_parser__mutmut_494,
    "x_build_parser__mutmut_495": x_build_parser__mutmut_495,
    "x_build_parser__mutmut_496": x_build_parser__mutmut_496,
    "x_build_parser__mutmut_497": x_build_parser__mutmut_497,
    "x_build_parser__mutmut_498": x_build_parser__mutmut_498,
    "x_build_parser__mutmut_499": x_build_parser__mutmut_499,
    "x_build_parser__mutmut_500": x_build_parser__mutmut_500,
    "x_build_parser__mutmut_501": x_build_parser__mutmut_501,
    "x_build_parser__mutmut_502": x_build_parser__mutmut_502,
    "x_build_parser__mutmut_503": x_build_parser__mutmut_503,
    "x_build_parser__mutmut_504": x_build_parser__mutmut_504,
    "x_build_parser__mutmut_505": x_build_parser__mutmut_505,
    "x_build_parser__mutmut_506": x_build_parser__mutmut_506,
    "x_build_parser__mutmut_507": x_build_parser__mutmut_507,
    "x_build_parser__mutmut_508": x_build_parser__mutmut_508,
    "x_build_parser__mutmut_509": x_build_parser__mutmut_509,
    "x_build_parser__mutmut_510": x_build_parser__mutmut_510,
    "x_build_parser__mutmut_511": x_build_parser__mutmut_511,
    "x_build_parser__mutmut_512": x_build_parser__mutmut_512,
    "x_build_parser__mutmut_513": x_build_parser__mutmut_513,
    "x_build_parser__mutmut_514": x_build_parser__mutmut_514,
    "x_build_parser__mutmut_515": x_build_parser__mutmut_515,
    "x_build_parser__mutmut_516": x_build_parser__mutmut_516,
    "x_build_parser__mutmut_517": x_build_parser__mutmut_517,
    "x_build_parser__mutmut_518": x_build_parser__mutmut_518,
    "x_build_parser__mutmut_519": x_build_parser__mutmut_519,
    "x_build_parser__mutmut_520": x_build_parser__mutmut_520,
    "x_build_parser__mutmut_521": x_build_parser__mutmut_521,
    "x_build_parser__mutmut_522": x_build_parser__mutmut_522,
    "x_build_parser__mutmut_523": x_build_parser__mutmut_523,
    "x_build_parser__mutmut_524": x_build_parser__mutmut_524,
    "x_build_parser__mutmut_525": x_build_parser__mutmut_525,
    "x_build_parser__mutmut_526": x_build_parser__mutmut_526,
    "x_build_parser__mutmut_527": x_build_parser__mutmut_527,
    "x_build_parser__mutmut_528": x_build_parser__mutmut_528,
    "x_build_parser__mutmut_529": x_build_parser__mutmut_529,
    "x_build_parser__mutmut_530": x_build_parser__mutmut_530,
    "x_build_parser__mutmut_531": x_build_parser__mutmut_531,
    "x_build_parser__mutmut_532": x_build_parser__mutmut_532,
    "x_build_parser__mutmut_533": x_build_parser__mutmut_533,
    "x_build_parser__mutmut_534": x_build_parser__mutmut_534,
    "x_build_parser__mutmut_535": x_build_parser__mutmut_535,
    "x_build_parser__mutmut_536": x_build_parser__mutmut_536,
    "x_build_parser__mutmut_537": x_build_parser__mutmut_537,
    "x_build_parser__mutmut_538": x_build_parser__mutmut_538,
    "x_build_parser__mutmut_539": x_build_parser__mutmut_539,
    "x_build_parser__mutmut_540": x_build_parser__mutmut_540,
    "x_build_parser__mutmut_541": x_build_parser__mutmut_541,
    "x_build_parser__mutmut_542": x_build_parser__mutmut_542,
    "x_build_parser__mutmut_543": x_build_parser__mutmut_543,
    "x_build_parser__mutmut_544": x_build_parser__mutmut_544,
    "x_build_parser__mutmut_545": x_build_parser__mutmut_545,
    "x_build_parser__mutmut_546": x_build_parser__mutmut_546,
    "x_build_parser__mutmut_547": x_build_parser__mutmut_547,
    "x_build_parser__mutmut_548": x_build_parser__mutmut_548,
    "x_build_parser__mutmut_549": x_build_parser__mutmut_549,
    "x_build_parser__mutmut_550": x_build_parser__mutmut_550,
    "x_build_parser__mutmut_551": x_build_parser__mutmut_551,
    "x_build_parser__mutmut_552": x_build_parser__mutmut_552,
    "x_build_parser__mutmut_553": x_build_parser__mutmut_553,
    "x_build_parser__mutmut_554": x_build_parser__mutmut_554,
    "x_build_parser__mutmut_555": x_build_parser__mutmut_555,
    "x_build_parser__mutmut_556": x_build_parser__mutmut_556,
    "x_build_parser__mutmut_557": x_build_parser__mutmut_557,
    "x_build_parser__mutmut_558": x_build_parser__mutmut_558,
    "x_build_parser__mutmut_559": x_build_parser__mutmut_559,
    "x_build_parser__mutmut_560": x_build_parser__mutmut_560,
    "x_build_parser__mutmut_561": x_build_parser__mutmut_561,
    "x_build_parser__mutmut_562": x_build_parser__mutmut_562,
    "x_build_parser__mutmut_563": x_build_parser__mutmut_563,
    "x_build_parser__mutmut_564": x_build_parser__mutmut_564,
    "x_build_parser__mutmut_565": x_build_parser__mutmut_565,
    "x_build_parser__mutmut_566": x_build_parser__mutmut_566,
    "x_build_parser__mutmut_567": x_build_parser__mutmut_567,
    "x_build_parser__mutmut_568": x_build_parser__mutmut_568,
    "x_build_parser__mutmut_569": x_build_parser__mutmut_569,
    "x_build_parser__mutmut_570": x_build_parser__mutmut_570,
    "x_build_parser__mutmut_571": x_build_parser__mutmut_571,
    "x_build_parser__mutmut_572": x_build_parser__mutmut_572,
    "x_build_parser__mutmut_573": x_build_parser__mutmut_573,
    "x_build_parser__mutmut_574": x_build_parser__mutmut_574,
    "x_build_parser__mutmut_575": x_build_parser__mutmut_575,
    "x_build_parser__mutmut_576": x_build_parser__mutmut_576,
    "x_build_parser__mutmut_577": x_build_parser__mutmut_577,
    "x_build_parser__mutmut_578": x_build_parser__mutmut_578,
    "x_build_parser__mutmut_579": x_build_parser__mutmut_579,
    "x_build_parser__mutmut_580": x_build_parser__mutmut_580,
    "x_build_parser__mutmut_581": x_build_parser__mutmut_581,
    "x_build_parser__mutmut_582": x_build_parser__mutmut_582,
    "x_build_parser__mutmut_583": x_build_parser__mutmut_583,
    "x_build_parser__mutmut_584": x_build_parser__mutmut_584,
    "x_build_parser__mutmut_585": x_build_parser__mutmut_585,
    "x_build_parser__mutmut_586": x_build_parser__mutmut_586,
    "x_build_parser__mutmut_587": x_build_parser__mutmut_587,
    "x_build_parser__mutmut_588": x_build_parser__mutmut_588,
    "x_build_parser__mutmut_589": x_build_parser__mutmut_589,
    "x_build_parser__mutmut_590": x_build_parser__mutmut_590,
    "x_build_parser__mutmut_591": x_build_parser__mutmut_591,
    "x_build_parser__mutmut_592": x_build_parser__mutmut_592,
    "x_build_parser__mutmut_593": x_build_parser__mutmut_593,
    "x_build_parser__mutmut_594": x_build_parser__mutmut_594,
    "x_build_parser__mutmut_595": x_build_parser__mutmut_595,
    "x_build_parser__mutmut_596": x_build_parser__mutmut_596,
    "x_build_parser__mutmut_597": x_build_parser__mutmut_597,
    "x_build_parser__mutmut_598": x_build_parser__mutmut_598,
    "x_build_parser__mutmut_599": x_build_parser__mutmut_599,
    "x_build_parser__mutmut_600": x_build_parser__mutmut_600,
    "x_build_parser__mutmut_601": x_build_parser__mutmut_601,
    "x_build_parser__mutmut_602": x_build_parser__mutmut_602,
    "x_build_parser__mutmut_603": x_build_parser__mutmut_603,
    "x_build_parser__mutmut_604": x_build_parser__mutmut_604,
    "x_build_parser__mutmut_605": x_build_parser__mutmut_605,
    "x_build_parser__mutmut_606": x_build_parser__mutmut_606,
    "x_build_parser__mutmut_607": x_build_parser__mutmut_607,
    "x_build_parser__mutmut_608": x_build_parser__mutmut_608,
    "x_build_parser__mutmut_609": x_build_parser__mutmut_609,
    "x_build_parser__mutmut_610": x_build_parser__mutmut_610,
    "x_build_parser__mutmut_611": x_build_parser__mutmut_611,
    "x_build_parser__mutmut_612": x_build_parser__mutmut_612,
    "x_build_parser__mutmut_613": x_build_parser__mutmut_613,
    "x_build_parser__mutmut_614": x_build_parser__mutmut_614,
    "x_build_parser__mutmut_615": x_build_parser__mutmut_615,
    "x_build_parser__mutmut_616": x_build_parser__mutmut_616,
    "x_build_parser__mutmut_617": x_build_parser__mutmut_617,
    "x_build_parser__mutmut_618": x_build_parser__mutmut_618,
    "x_build_parser__mutmut_619": x_build_parser__mutmut_619,
    "x_build_parser__mutmut_620": x_build_parser__mutmut_620,
    "x_build_parser__mutmut_621": x_build_parser__mutmut_621,
    "x_build_parser__mutmut_622": x_build_parser__mutmut_622,
    "x_build_parser__mutmut_623": x_build_parser__mutmut_623,
    "x_build_parser__mutmut_624": x_build_parser__mutmut_624,
    "x_build_parser__mutmut_625": x_build_parser__mutmut_625,
    "x_build_parser__mutmut_626": x_build_parser__mutmut_626,
    "x_build_parser__mutmut_627": x_build_parser__mutmut_627,
    "x_build_parser__mutmut_628": x_build_parser__mutmut_628,
    "x_build_parser__mutmut_629": x_build_parser__mutmut_629,
    "x_build_parser__mutmut_630": x_build_parser__mutmut_630,
    "x_build_parser__mutmut_631": x_build_parser__mutmut_631,
    "x_build_parser__mutmut_632": x_build_parser__mutmut_632,
    "x_build_parser__mutmut_633": x_build_parser__mutmut_633,
    "x_build_parser__mutmut_634": x_build_parser__mutmut_634,
    "x_build_parser__mutmut_635": x_build_parser__mutmut_635,
    "x_build_parser__mutmut_636": x_build_parser__mutmut_636,
}
x_build_parser__mutmut_orig.__name__ = "x_build_parser"


def main(argv: list[str] | None = None) -> int:
    args = [argv]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(x_main__mutmut_orig, x_main__mutmut_mutants, args, kwargs, None)


def x_main__mutmut_orig(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_1(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = None
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_2(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = None

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_3(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(None)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_4(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        print(None)
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_5(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 1

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_6(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is not None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_7(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 1

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_8(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command != "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_9(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "XXestimateXX":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_10(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "ESTIMATE":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_11(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(None)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_12(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command != "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_13(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "XXriskXX":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_14(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "RISK":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_15(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(None)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_16(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command != "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_17(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "XXbacktestXX":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_18(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "BACKTEST":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_19(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(None)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_20(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command != "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_21(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "XXregimeXX":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_22(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "REGIME":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_23(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(None)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_24(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 2
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_25(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception:
        print(None, file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_26(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=None)
        return 1

    return 0


def x_main__mutmut_27(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception:
        print(file=sys.stderr)
        return 1

    return 0


def x_main__mutmut_28(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(
            f"Error: {e}",
        )
        return 1

    return 0


def x_main__mutmut_29(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 2

    return 0


def x_main__mutmut_30(argv: list[str] | None = None) -> int:
    """Main entry point for the CLI.

    Parameters
    ----------
    argv : list[str], optional
        Command-line arguments. If None, uses sys.argv.

    Returns
    -------
    int
        Exit code (0 for success, 1 for error).
    """
    parser = build_parser()
    args = parser.parse_args(argv)

    if args.version:
        from archbox.__version__ import __version__

        print(f"archbox {__version__}")
        return 0

    if args.command is None:
        parser.print_help()
        return 0

    try:
        if args.command == "estimate":
            from archbox.cli.estimate import run_estimate

            run_estimate(args)
        elif args.command == "risk":
            from archbox.cli.risk import run_risk

            run_risk(args)
        elif args.command == "backtest":
            from archbox.cli.backtest import run_backtest

            run_backtest(args)
        elif args.command == "regime":
            from archbox.cli.regime import run_regime

            run_regime(args)
        else:
            parser.print_help()
            return 1
    except Exception as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1

    return 1


x_main__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_main__mutmut_1": x_main__mutmut_1,
    "x_main__mutmut_2": x_main__mutmut_2,
    "x_main__mutmut_3": x_main__mutmut_3,
    "x_main__mutmut_4": x_main__mutmut_4,
    "x_main__mutmut_5": x_main__mutmut_5,
    "x_main__mutmut_6": x_main__mutmut_6,
    "x_main__mutmut_7": x_main__mutmut_7,
    "x_main__mutmut_8": x_main__mutmut_8,
    "x_main__mutmut_9": x_main__mutmut_9,
    "x_main__mutmut_10": x_main__mutmut_10,
    "x_main__mutmut_11": x_main__mutmut_11,
    "x_main__mutmut_12": x_main__mutmut_12,
    "x_main__mutmut_13": x_main__mutmut_13,
    "x_main__mutmut_14": x_main__mutmut_14,
    "x_main__mutmut_15": x_main__mutmut_15,
    "x_main__mutmut_16": x_main__mutmut_16,
    "x_main__mutmut_17": x_main__mutmut_17,
    "x_main__mutmut_18": x_main__mutmut_18,
    "x_main__mutmut_19": x_main__mutmut_19,
    "x_main__mutmut_20": x_main__mutmut_20,
    "x_main__mutmut_21": x_main__mutmut_21,
    "x_main__mutmut_22": x_main__mutmut_22,
    "x_main__mutmut_23": x_main__mutmut_23,
    "x_main__mutmut_24": x_main__mutmut_24,
    "x_main__mutmut_25": x_main__mutmut_25,
    "x_main__mutmut_26": x_main__mutmut_26,
    "x_main__mutmut_27": x_main__mutmut_27,
    "x_main__mutmut_28": x_main__mutmut_28,
    "x_main__mutmut_29": x_main__mutmut_29,
    "x_main__mutmut_30": x_main__mutmut_30,
}
x_main__mutmut_orig.__name__ = "x_main"


if __name__ == "__main__":
    sys.exit(main())
