"""News Impact Curve visualization for archbox.

The News Impact Curve (NIC) shows how past shocks (eps_{t-1}) affect
current conditional variance (sigma^2_t). For symmetric models (GARCH),
the NIC is a parabola. For asymmetric models (EGARCH, GJR), negative
shocks generate more volatility than positive shocks of the same magnitude.
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Annotated, Any, ClassVar

import matplotlib.pyplot as plt
import numpy as np
from matplotlib.figure import Figure
from numpy.typing import NDArray

from archbox.visualization.themes import Theme, get_theme

MutantDict = Annotated[dict[str, Callable], "Mutant"]  # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg=None):  # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os  # type: ignore

    mutant_under_test = os.environ["MUTANT_UNDER_TEST"]  # type: ignore
    if mutant_under_test == "fail":  # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException  # type: ignore

        raise MutmutProgrammaticFailException("Failed programmatically")  # type: ignore
    elif mutant_under_test == "stats":  # type: ignore
        from mutmut.__main__ import record_trampoline_hit  # type: ignore

        record_trampoline_hit(orig.__module__ + "." + orig.__name__)  # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    prefix = orig.__module__ + "." + orig.__name__ + "__mutmut_"  # type: ignore
    if not mutant_under_test.startswith(prefix):  # type: ignore
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    mutant_name = mutant_under_test.rpartition(".")[-1]  # type: ignore
    if self_arg is not None:  # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)  # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)  # type: ignore
    return result  # type: ignore


def plot_news_impact(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    args = [results, n_points, n_sigma, theme, figsize, title]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_plot_news_impact__mutmut_orig, x_plot_news_impact__mutmut_mutants, args, kwargs, None
    )


def x_plot_news_impact__mutmut_orig(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_1(
    results: Any,
    n_points: int = 201,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_2(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 4.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_3(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "XXprofessionalXX",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_4(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "PROFESSIONAL",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_5(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = None

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_6(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(None)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_7(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = None
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_8(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(None):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_9(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = None

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_10(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize and theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_11(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(None, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_12(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, None):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_13(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr("news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_14(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(
            results,
        ):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_15(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "XXnews_impactXX"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_16(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "NEWS_IMPACT"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_17(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = None
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_18(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=None, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_19(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=None)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_20(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_21(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(
                n_points=n_points,
            )
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_22(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = None

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_23(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(None, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_24(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, None, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_25(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, None)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_26(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_27(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_28(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(
                results,
                n_points,
            )

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_29(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = None

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_30(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(None, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_31(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, None, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_32(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=None)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_33(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_34(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_35(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(
            1,
            1,
        )

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_36(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(2, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_37(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 2, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_38(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            None,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_39(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            None,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_40(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=None,
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_41(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=None,
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_42(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=None,
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_43(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_44(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_45(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_46(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_47(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_48(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get(None, "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_49(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", None),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_50(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_51(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get(
                "primary",
            ),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_52(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("XXprimaryXX", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_53(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("PRIMARY", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_54(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "XX#1f4e79XX"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_55(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1F4E79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_56(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get(None, 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_57(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", None),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_58(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get(1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_59(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get(
                "primary",
            ),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_60(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("XXprimaryXX", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_61(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("PRIMARY", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_62(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 2.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_63(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(None, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_64(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, None, "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_65(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", None),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_66(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=("model_name").Model,
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_67(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=results.Model,
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_68(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=results.model_name,
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_69(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "XXmodel_nameXX", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_70(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "MODEL_NAME", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_71(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "XXModelXX"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_72(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_73(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "MODEL"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_74(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") or len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_75(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(None, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_76(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, None) and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_77(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr("params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_78(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if (
            hasattr(
                results,
            )
            and len(results.params) > 0
        ):
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_79(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "XXparamsXX") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_80(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "PARAMS") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_81(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) >= 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_82(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 1:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_83(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = None
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_84(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[1] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_85(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(None, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_86(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, None) else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_87(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr("params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_88(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = (
                results.params[0]
                if hasattr(
                    results,
                )
                else np.mean(sigma2)
            )
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_89(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "XXparamsXX") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_90(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "PARAMS") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_91(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(None)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_92(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = None
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_93(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega - np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_94(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) / eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_95(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) * np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_96(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(None) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_97(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(None) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_98(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 - 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_99(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range * 2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_100(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**3 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_101(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = (
                omega + np.mean(sigma2) / np.max(eps_range**2 + 1.000000000001) * eps_range**2
            )
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_102(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range * 2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_103(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**3
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_104(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                None,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_105(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                None,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_106(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=None,
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_107(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=None,
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_108(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle=None,
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_109(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=None,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_110(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label=None,
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_111(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_112(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_113(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_114(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_115(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_116(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_117(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_118(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get(None, "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_119(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", None),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_120(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_121(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get(
                    "grid",
                ),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_122(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("XXgridXX", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_123(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("GRID", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_124(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "XX#ccccccXX"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_125(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#CCCCCC"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_126(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get(None, 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_127(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", None),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_128(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get(0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_129(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get(
                    "secondary",
                ),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_130(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("XXsecondaryXX", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_131(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("SECONDARY", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_132(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 1.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_133(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="XX--XX",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_134(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=1.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_135(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="XXSymmetric ReferenceXX",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_136(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="symmetric reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_137(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="SYMMETRIC REFERENCE",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_138(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=None,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_139(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=None,
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_140(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=None,
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_141(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=None,
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_142(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_143(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_144(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_145(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_146(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=1,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_147(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get(None, "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_148(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", None),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_149(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_150(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get(
                "grid",
            ),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_151(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("XXgridXX", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_152(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("GRID", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_153(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "XX#ccccccXX"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_154(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#CCCCCC"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_155(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(None, 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_156(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", None),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_157(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_158(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(
                "grid",
            ),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_159(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("XXgridXX", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_160(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("GRID", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_161(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 1.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_162(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle="XX:XX",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_163(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(None)
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_164(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"XX$\varepsilon_{t-1}$XX")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_165(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\vAREPSILON_{T-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_166(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(None)
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_167(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"XX$\sigma^2_t$XX")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_168(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sIGMA^2_T$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_169(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(None)
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_170(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title and "News Impact Curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_171(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "XXNews Impact CurveXX")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_172(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "news impact curve")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_173(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "NEWS IMPACT CURVE")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_174(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc=None, framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_175(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=None)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_176(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_177(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(
            loc="upper center",
        )

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_178(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="XXupper centerXX", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_179(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="UPPER CENTER", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact__mutmut_180(
    results: Any,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot the News Impact Curve.

    Parameters
    ----------
    results : ArchResults
        Fitted model results. Must have a `news_impact` method or
        sufficient parameters to compute the NIC.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations (from -n_sigma*sigma to +n_sigma*sigma).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        # Compute NIC data
        if hasattr(results, "news_impact"):
            eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
        else:
            # Fallback: compute from parameters
            eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        ax.plot(
            eps_range,
            sigma2,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=getattr(results, "model_name", "Model"),
        )

        # Add symmetric reference (parabola)
        if hasattr(results, "params") and len(results.params) > 0:
            omega = results.params[0] if hasattr(results, "params") else np.mean(sigma2)
            sym_sigma2 = omega + np.mean(sigma2) / np.max(eps_range**2 + 1e-12) * eps_range**2
            ax.plot(
                eps_range,
                sym_sigma2,
                color=theme.colors.get("grid", "#cccccc"),
                linewidth=theme.line_widths.get("secondary", 0.8),
                linestyle="--",
                alpha=0.5,
                label="Symmetric Reference",
            )

        # Highlight asymmetry region
        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve")
        ax.legend(loc="upper center", framealpha=1.8)

        fig.tight_layout()
        return fig


x_plot_news_impact__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_plot_news_impact__mutmut_1": x_plot_news_impact__mutmut_1,
    "x_plot_news_impact__mutmut_2": x_plot_news_impact__mutmut_2,
    "x_plot_news_impact__mutmut_3": x_plot_news_impact__mutmut_3,
    "x_plot_news_impact__mutmut_4": x_plot_news_impact__mutmut_4,
    "x_plot_news_impact__mutmut_5": x_plot_news_impact__mutmut_5,
    "x_plot_news_impact__mutmut_6": x_plot_news_impact__mutmut_6,
    "x_plot_news_impact__mutmut_7": x_plot_news_impact__mutmut_7,
    "x_plot_news_impact__mutmut_8": x_plot_news_impact__mutmut_8,
    "x_plot_news_impact__mutmut_9": x_plot_news_impact__mutmut_9,
    "x_plot_news_impact__mutmut_10": x_plot_news_impact__mutmut_10,
    "x_plot_news_impact__mutmut_11": x_plot_news_impact__mutmut_11,
    "x_plot_news_impact__mutmut_12": x_plot_news_impact__mutmut_12,
    "x_plot_news_impact__mutmut_13": x_plot_news_impact__mutmut_13,
    "x_plot_news_impact__mutmut_14": x_plot_news_impact__mutmut_14,
    "x_plot_news_impact__mutmut_15": x_plot_news_impact__mutmut_15,
    "x_plot_news_impact__mutmut_16": x_plot_news_impact__mutmut_16,
    "x_plot_news_impact__mutmut_17": x_plot_news_impact__mutmut_17,
    "x_plot_news_impact__mutmut_18": x_plot_news_impact__mutmut_18,
    "x_plot_news_impact__mutmut_19": x_plot_news_impact__mutmut_19,
    "x_plot_news_impact__mutmut_20": x_plot_news_impact__mutmut_20,
    "x_plot_news_impact__mutmut_21": x_plot_news_impact__mutmut_21,
    "x_plot_news_impact__mutmut_22": x_plot_news_impact__mutmut_22,
    "x_plot_news_impact__mutmut_23": x_plot_news_impact__mutmut_23,
    "x_plot_news_impact__mutmut_24": x_plot_news_impact__mutmut_24,
    "x_plot_news_impact__mutmut_25": x_plot_news_impact__mutmut_25,
    "x_plot_news_impact__mutmut_26": x_plot_news_impact__mutmut_26,
    "x_plot_news_impact__mutmut_27": x_plot_news_impact__mutmut_27,
    "x_plot_news_impact__mutmut_28": x_plot_news_impact__mutmut_28,
    "x_plot_news_impact__mutmut_29": x_plot_news_impact__mutmut_29,
    "x_plot_news_impact__mutmut_30": x_plot_news_impact__mutmut_30,
    "x_plot_news_impact__mutmut_31": x_plot_news_impact__mutmut_31,
    "x_plot_news_impact__mutmut_32": x_plot_news_impact__mutmut_32,
    "x_plot_news_impact__mutmut_33": x_plot_news_impact__mutmut_33,
    "x_plot_news_impact__mutmut_34": x_plot_news_impact__mutmut_34,
    "x_plot_news_impact__mutmut_35": x_plot_news_impact__mutmut_35,
    "x_plot_news_impact__mutmut_36": x_plot_news_impact__mutmut_36,
    "x_plot_news_impact__mutmut_37": x_plot_news_impact__mutmut_37,
    "x_plot_news_impact__mutmut_38": x_plot_news_impact__mutmut_38,
    "x_plot_news_impact__mutmut_39": x_plot_news_impact__mutmut_39,
    "x_plot_news_impact__mutmut_40": x_plot_news_impact__mutmut_40,
    "x_plot_news_impact__mutmut_41": x_plot_news_impact__mutmut_41,
    "x_plot_news_impact__mutmut_42": x_plot_news_impact__mutmut_42,
    "x_plot_news_impact__mutmut_43": x_plot_news_impact__mutmut_43,
    "x_plot_news_impact__mutmut_44": x_plot_news_impact__mutmut_44,
    "x_plot_news_impact__mutmut_45": x_plot_news_impact__mutmut_45,
    "x_plot_news_impact__mutmut_46": x_plot_news_impact__mutmut_46,
    "x_plot_news_impact__mutmut_47": x_plot_news_impact__mutmut_47,
    "x_plot_news_impact__mutmut_48": x_plot_news_impact__mutmut_48,
    "x_plot_news_impact__mutmut_49": x_plot_news_impact__mutmut_49,
    "x_plot_news_impact__mutmut_50": x_plot_news_impact__mutmut_50,
    "x_plot_news_impact__mutmut_51": x_plot_news_impact__mutmut_51,
    "x_plot_news_impact__mutmut_52": x_plot_news_impact__mutmut_52,
    "x_plot_news_impact__mutmut_53": x_plot_news_impact__mutmut_53,
    "x_plot_news_impact__mutmut_54": x_plot_news_impact__mutmut_54,
    "x_plot_news_impact__mutmut_55": x_plot_news_impact__mutmut_55,
    "x_plot_news_impact__mutmut_56": x_plot_news_impact__mutmut_56,
    "x_plot_news_impact__mutmut_57": x_plot_news_impact__mutmut_57,
    "x_plot_news_impact__mutmut_58": x_plot_news_impact__mutmut_58,
    "x_plot_news_impact__mutmut_59": x_plot_news_impact__mutmut_59,
    "x_plot_news_impact__mutmut_60": x_plot_news_impact__mutmut_60,
    "x_plot_news_impact__mutmut_61": x_plot_news_impact__mutmut_61,
    "x_plot_news_impact__mutmut_62": x_plot_news_impact__mutmut_62,
    "x_plot_news_impact__mutmut_63": x_plot_news_impact__mutmut_63,
    "x_plot_news_impact__mutmut_64": x_plot_news_impact__mutmut_64,
    "x_plot_news_impact__mutmut_65": x_plot_news_impact__mutmut_65,
    "x_plot_news_impact__mutmut_66": x_plot_news_impact__mutmut_66,
    "x_plot_news_impact__mutmut_67": x_plot_news_impact__mutmut_67,
    "x_plot_news_impact__mutmut_68": x_plot_news_impact__mutmut_68,
    "x_plot_news_impact__mutmut_69": x_plot_news_impact__mutmut_69,
    "x_plot_news_impact__mutmut_70": x_plot_news_impact__mutmut_70,
    "x_plot_news_impact__mutmut_71": x_plot_news_impact__mutmut_71,
    "x_plot_news_impact__mutmut_72": x_plot_news_impact__mutmut_72,
    "x_plot_news_impact__mutmut_73": x_plot_news_impact__mutmut_73,
    "x_plot_news_impact__mutmut_74": x_plot_news_impact__mutmut_74,
    "x_plot_news_impact__mutmut_75": x_plot_news_impact__mutmut_75,
    "x_plot_news_impact__mutmut_76": x_plot_news_impact__mutmut_76,
    "x_plot_news_impact__mutmut_77": x_plot_news_impact__mutmut_77,
    "x_plot_news_impact__mutmut_78": x_plot_news_impact__mutmut_78,
    "x_plot_news_impact__mutmut_79": x_plot_news_impact__mutmut_79,
    "x_plot_news_impact__mutmut_80": x_plot_news_impact__mutmut_80,
    "x_plot_news_impact__mutmut_81": x_plot_news_impact__mutmut_81,
    "x_plot_news_impact__mutmut_82": x_plot_news_impact__mutmut_82,
    "x_plot_news_impact__mutmut_83": x_plot_news_impact__mutmut_83,
    "x_plot_news_impact__mutmut_84": x_plot_news_impact__mutmut_84,
    "x_plot_news_impact__mutmut_85": x_plot_news_impact__mutmut_85,
    "x_plot_news_impact__mutmut_86": x_plot_news_impact__mutmut_86,
    "x_plot_news_impact__mutmut_87": x_plot_news_impact__mutmut_87,
    "x_plot_news_impact__mutmut_88": x_plot_news_impact__mutmut_88,
    "x_plot_news_impact__mutmut_89": x_plot_news_impact__mutmut_89,
    "x_plot_news_impact__mutmut_90": x_plot_news_impact__mutmut_90,
    "x_plot_news_impact__mutmut_91": x_plot_news_impact__mutmut_91,
    "x_plot_news_impact__mutmut_92": x_plot_news_impact__mutmut_92,
    "x_plot_news_impact__mutmut_93": x_plot_news_impact__mutmut_93,
    "x_plot_news_impact__mutmut_94": x_plot_news_impact__mutmut_94,
    "x_plot_news_impact__mutmut_95": x_plot_news_impact__mutmut_95,
    "x_plot_news_impact__mutmut_96": x_plot_news_impact__mutmut_96,
    "x_plot_news_impact__mutmut_97": x_plot_news_impact__mutmut_97,
    "x_plot_news_impact__mutmut_98": x_plot_news_impact__mutmut_98,
    "x_plot_news_impact__mutmut_99": x_plot_news_impact__mutmut_99,
    "x_plot_news_impact__mutmut_100": x_plot_news_impact__mutmut_100,
    "x_plot_news_impact__mutmut_101": x_plot_news_impact__mutmut_101,
    "x_plot_news_impact__mutmut_102": x_plot_news_impact__mutmut_102,
    "x_plot_news_impact__mutmut_103": x_plot_news_impact__mutmut_103,
    "x_plot_news_impact__mutmut_104": x_plot_news_impact__mutmut_104,
    "x_plot_news_impact__mutmut_105": x_plot_news_impact__mutmut_105,
    "x_plot_news_impact__mutmut_106": x_plot_news_impact__mutmut_106,
    "x_plot_news_impact__mutmut_107": x_plot_news_impact__mutmut_107,
    "x_plot_news_impact__mutmut_108": x_plot_news_impact__mutmut_108,
    "x_plot_news_impact__mutmut_109": x_plot_news_impact__mutmut_109,
    "x_plot_news_impact__mutmut_110": x_plot_news_impact__mutmut_110,
    "x_plot_news_impact__mutmut_111": x_plot_news_impact__mutmut_111,
    "x_plot_news_impact__mutmut_112": x_plot_news_impact__mutmut_112,
    "x_plot_news_impact__mutmut_113": x_plot_news_impact__mutmut_113,
    "x_plot_news_impact__mutmut_114": x_plot_news_impact__mutmut_114,
    "x_plot_news_impact__mutmut_115": x_plot_news_impact__mutmut_115,
    "x_plot_news_impact__mutmut_116": x_plot_news_impact__mutmut_116,
    "x_plot_news_impact__mutmut_117": x_plot_news_impact__mutmut_117,
    "x_plot_news_impact__mutmut_118": x_plot_news_impact__mutmut_118,
    "x_plot_news_impact__mutmut_119": x_plot_news_impact__mutmut_119,
    "x_plot_news_impact__mutmut_120": x_plot_news_impact__mutmut_120,
    "x_plot_news_impact__mutmut_121": x_plot_news_impact__mutmut_121,
    "x_plot_news_impact__mutmut_122": x_plot_news_impact__mutmut_122,
    "x_plot_news_impact__mutmut_123": x_plot_news_impact__mutmut_123,
    "x_plot_news_impact__mutmut_124": x_plot_news_impact__mutmut_124,
    "x_plot_news_impact__mutmut_125": x_plot_news_impact__mutmut_125,
    "x_plot_news_impact__mutmut_126": x_plot_news_impact__mutmut_126,
    "x_plot_news_impact__mutmut_127": x_plot_news_impact__mutmut_127,
    "x_plot_news_impact__mutmut_128": x_plot_news_impact__mutmut_128,
    "x_plot_news_impact__mutmut_129": x_plot_news_impact__mutmut_129,
    "x_plot_news_impact__mutmut_130": x_plot_news_impact__mutmut_130,
    "x_plot_news_impact__mutmut_131": x_plot_news_impact__mutmut_131,
    "x_plot_news_impact__mutmut_132": x_plot_news_impact__mutmut_132,
    "x_plot_news_impact__mutmut_133": x_plot_news_impact__mutmut_133,
    "x_plot_news_impact__mutmut_134": x_plot_news_impact__mutmut_134,
    "x_plot_news_impact__mutmut_135": x_plot_news_impact__mutmut_135,
    "x_plot_news_impact__mutmut_136": x_plot_news_impact__mutmut_136,
    "x_plot_news_impact__mutmut_137": x_plot_news_impact__mutmut_137,
    "x_plot_news_impact__mutmut_138": x_plot_news_impact__mutmut_138,
    "x_plot_news_impact__mutmut_139": x_plot_news_impact__mutmut_139,
    "x_plot_news_impact__mutmut_140": x_plot_news_impact__mutmut_140,
    "x_plot_news_impact__mutmut_141": x_plot_news_impact__mutmut_141,
    "x_plot_news_impact__mutmut_142": x_plot_news_impact__mutmut_142,
    "x_plot_news_impact__mutmut_143": x_plot_news_impact__mutmut_143,
    "x_plot_news_impact__mutmut_144": x_plot_news_impact__mutmut_144,
    "x_plot_news_impact__mutmut_145": x_plot_news_impact__mutmut_145,
    "x_plot_news_impact__mutmut_146": x_plot_news_impact__mutmut_146,
    "x_plot_news_impact__mutmut_147": x_plot_news_impact__mutmut_147,
    "x_plot_news_impact__mutmut_148": x_plot_news_impact__mutmut_148,
    "x_plot_news_impact__mutmut_149": x_plot_news_impact__mutmut_149,
    "x_plot_news_impact__mutmut_150": x_plot_news_impact__mutmut_150,
    "x_plot_news_impact__mutmut_151": x_plot_news_impact__mutmut_151,
    "x_plot_news_impact__mutmut_152": x_plot_news_impact__mutmut_152,
    "x_plot_news_impact__mutmut_153": x_plot_news_impact__mutmut_153,
    "x_plot_news_impact__mutmut_154": x_plot_news_impact__mutmut_154,
    "x_plot_news_impact__mutmut_155": x_plot_news_impact__mutmut_155,
    "x_plot_news_impact__mutmut_156": x_plot_news_impact__mutmut_156,
    "x_plot_news_impact__mutmut_157": x_plot_news_impact__mutmut_157,
    "x_plot_news_impact__mutmut_158": x_plot_news_impact__mutmut_158,
    "x_plot_news_impact__mutmut_159": x_plot_news_impact__mutmut_159,
    "x_plot_news_impact__mutmut_160": x_plot_news_impact__mutmut_160,
    "x_plot_news_impact__mutmut_161": x_plot_news_impact__mutmut_161,
    "x_plot_news_impact__mutmut_162": x_plot_news_impact__mutmut_162,
    "x_plot_news_impact__mutmut_163": x_plot_news_impact__mutmut_163,
    "x_plot_news_impact__mutmut_164": x_plot_news_impact__mutmut_164,
    "x_plot_news_impact__mutmut_165": x_plot_news_impact__mutmut_165,
    "x_plot_news_impact__mutmut_166": x_plot_news_impact__mutmut_166,
    "x_plot_news_impact__mutmut_167": x_plot_news_impact__mutmut_167,
    "x_plot_news_impact__mutmut_168": x_plot_news_impact__mutmut_168,
    "x_plot_news_impact__mutmut_169": x_plot_news_impact__mutmut_169,
    "x_plot_news_impact__mutmut_170": x_plot_news_impact__mutmut_170,
    "x_plot_news_impact__mutmut_171": x_plot_news_impact__mutmut_171,
    "x_plot_news_impact__mutmut_172": x_plot_news_impact__mutmut_172,
    "x_plot_news_impact__mutmut_173": x_plot_news_impact__mutmut_173,
    "x_plot_news_impact__mutmut_174": x_plot_news_impact__mutmut_174,
    "x_plot_news_impact__mutmut_175": x_plot_news_impact__mutmut_175,
    "x_plot_news_impact__mutmut_176": x_plot_news_impact__mutmut_176,
    "x_plot_news_impact__mutmut_177": x_plot_news_impact__mutmut_177,
    "x_plot_news_impact__mutmut_178": x_plot_news_impact__mutmut_178,
    "x_plot_news_impact__mutmut_179": x_plot_news_impact__mutmut_179,
    "x_plot_news_impact__mutmut_180": x_plot_news_impact__mutmut_180,
}
x_plot_news_impact__mutmut_orig.__name__ = "x_plot_news_impact"


def plot_news_impact_comparison(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    args = [results_list, labels, n_points, n_sigma, theme, figsize, title]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_plot_news_impact_comparison__mutmut_orig,
        x_plot_news_impact_comparison__mutmut_mutants,
        args,
        kwargs,
        None,
    )


def x_plot_news_impact_comparison__mutmut_orig(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_1(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 201,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_2(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 4.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_3(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "XXprofessionalXX",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_4(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "PROFESSIONAL",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_5(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = None

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_6(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(None)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_7(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is not None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_8(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = None

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_9(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(None, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_10(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, None, f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_11(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", None) for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_12(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr("model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_13(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_14(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [r.model_name for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_15(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "XXmodel_nameXX", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_16(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "MODEL_NAME", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_17(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(None)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_18(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = None
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_19(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = None

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_20(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get(None, "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_21(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", None),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_22(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_23(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get(
            "primary",
        ),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_24(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("XXprimaryXX", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_25(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("PRIMARY", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_26(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "XX#1f4e79XX"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_27(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1F4E79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_28(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get(None, "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_29(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", None),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_30(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_31(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get(
            "accent",
        ),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_32(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("XXaccentXX", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_33(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("ACCENT", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_34(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "XX#c00000XX"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_35(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#C00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_36(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get(None, "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_37(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", None),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_38(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_39(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get(
            "secondary",
        ),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_40(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("XXsecondaryXX", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_41(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("SECONDARY", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_42(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "XX#2e75b6XX"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_43(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2E75B6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_44(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get(None, "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_45(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", None),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_46(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_47(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get(
            "positive",
        ),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_48(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("XXpositiveXX", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_49(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("POSITIVE", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_50(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "XX#548235XX"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_51(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(None):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_52(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = None
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_53(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize and theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_54(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = None

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_55(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(None, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_56(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, None, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_57(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=None)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_58(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_59(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_60(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(
            1,
            1,
        )

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_61(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(2, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_62(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 2, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_63(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(None):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_64(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(None, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_65(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, None, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_66(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=None)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_67(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_68(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_69(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(
            zip(
                results_list,
                labels,
                strict=False,
            )
        ):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_70(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=True)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_71(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(None, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_72(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, None):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_73(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr("news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_74(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(
                results,
            ):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_75(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "XXnews_impactXX"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_76(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "NEWS_IMPACT"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_77(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = None
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_78(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=None, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_79(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=None)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_80(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_81(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(
                    n_points=n_points,
                )
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_82(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = None

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_83(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(None, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_84(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, None, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_85(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, None)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_86(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_87(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_88(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(
                    results,
                    n_points,
                )

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_89(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = None
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_90(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx / len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_91(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                None,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_92(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                None,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_93(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=None,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_94(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=None,
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_95(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=None,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_96(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_97(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_98(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_99(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_100(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_101(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get(None, 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_102(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", None),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_103(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get(1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_104(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get(
                    "primary",
                ),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_105(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("XXprimaryXX", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_106(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("PRIMARY", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_107(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 2.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_108(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=None,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_109(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=None,
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_110(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=None,
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_111(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=None,
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_112(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_113(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_114(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_115(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_116(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=1,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_117(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get(None, "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_118(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", None),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_119(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_120(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get(
                "grid",
            ),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_121(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("XXgridXX", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_122(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("GRID", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_123(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "XX#ccccccXX"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_124(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#CCCCCC"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_125(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(None, 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_126(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", None),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_127(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_128(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(
                "grid",
            ),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_129(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("XXgridXX", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_130(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("GRID", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_131(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 1.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_132(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle="XX:XX",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_133(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(None)
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_134(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"XX$\varepsilon_{t-1}$XX")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_135(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\vAREPSILON_{T-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_136(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(None)
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_137(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"XX$\sigma^2_t$XX")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_138(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sIGMA^2_T$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_139(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(None)
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_140(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title and "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_141(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "XXNews Impact Curve ComparisonXX")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_142(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "news impact curve comparison")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_143(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "NEWS IMPACT CURVE COMPARISON")
        ax.legend(loc="upper center", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_144(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc=None, framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_145(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=None)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_146(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_147(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(
            loc="upper center",
        )

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_148(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="XXupper centerXX", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_149(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="UPPER CENTER", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_news_impact_comparison__mutmut_150(
    results_list: list[Any],
    labels: list[str] | None = None,
    n_points: int = 200,
    n_sigma: float = 3.0,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot multiple News Impact Curves for comparison.

    Parameters
    ----------
    results_list : list
        List of fitted model results.
    labels : list of str, optional
        Labels for each model. If None, uses model_name attribute.
    n_points : int
        Number of points on the x-axis.
    n_sigma : float
        Range in standard deviations.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    if labels is None:
        labels = [getattr(r, "model_name", f"Model {i}") for i, r in enumerate(results_list)]

    rc_params = theme.to_matplotlib_rcparams()
    color_cycle = [
        theme.colors.get("primary", "#1f4e79"),
        theme.colors.get("accent", "#c00000"),
        theme.colors.get("secondary", "#2e75b6"),
        theme.colors.get("positive", "#548235"),
    ]

    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size
        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for idx, (results, label) in enumerate(zip(results_list, labels, strict=False)):
            if hasattr(results, "news_impact"):
                eps_range, sigma2 = results.news_impact(n_points=n_points, n_sigma=n_sigma)
            else:
                eps_range, sigma2 = _compute_news_impact(results, n_points, n_sigma)

            color = color_cycle[idx % len(color_cycle)]
            ax.plot(
                eps_range,
                sigma2,
                color=color,
                linewidth=theme.line_widths.get("primary", 1.5),
                label=label,
            )

        ax.axvline(
            x=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
            linestyle=":",
        )

        ax.set_xlabel(r"$\varepsilon_{t-1}$")
        ax.set_ylabel(r"$\sigma^2_t$")
        ax.set_title(title or "News Impact Curve Comparison")
        ax.legend(loc="upper center", framealpha=1.8)

        fig.tight_layout()
        return fig


x_plot_news_impact_comparison__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_plot_news_impact_comparison__mutmut_1": x_plot_news_impact_comparison__mutmut_1,
    "x_plot_news_impact_comparison__mutmut_2": x_plot_news_impact_comparison__mutmut_2,
    "x_plot_news_impact_comparison__mutmut_3": x_plot_news_impact_comparison__mutmut_3,
    "x_plot_news_impact_comparison__mutmut_4": x_plot_news_impact_comparison__mutmut_4,
    "x_plot_news_impact_comparison__mutmut_5": x_plot_news_impact_comparison__mutmut_5,
    "x_plot_news_impact_comparison__mutmut_6": x_plot_news_impact_comparison__mutmut_6,
    "x_plot_news_impact_comparison__mutmut_7": x_plot_news_impact_comparison__mutmut_7,
    "x_plot_news_impact_comparison__mutmut_8": x_plot_news_impact_comparison__mutmut_8,
    "x_plot_news_impact_comparison__mutmut_9": x_plot_news_impact_comparison__mutmut_9,
    "x_plot_news_impact_comparison__mutmut_10": x_plot_news_impact_comparison__mutmut_10,
    "x_plot_news_impact_comparison__mutmut_11": x_plot_news_impact_comparison__mutmut_11,
    "x_plot_news_impact_comparison__mutmut_12": x_plot_news_impact_comparison__mutmut_12,
    "x_plot_news_impact_comparison__mutmut_13": x_plot_news_impact_comparison__mutmut_13,
    "x_plot_news_impact_comparison__mutmut_14": x_plot_news_impact_comparison__mutmut_14,
    "x_plot_news_impact_comparison__mutmut_15": x_plot_news_impact_comparison__mutmut_15,
    "x_plot_news_impact_comparison__mutmut_16": x_plot_news_impact_comparison__mutmut_16,
    "x_plot_news_impact_comparison__mutmut_17": x_plot_news_impact_comparison__mutmut_17,
    "x_plot_news_impact_comparison__mutmut_18": x_plot_news_impact_comparison__mutmut_18,
    "x_plot_news_impact_comparison__mutmut_19": x_plot_news_impact_comparison__mutmut_19,
    "x_plot_news_impact_comparison__mutmut_20": x_plot_news_impact_comparison__mutmut_20,
    "x_plot_news_impact_comparison__mutmut_21": x_plot_news_impact_comparison__mutmut_21,
    "x_plot_news_impact_comparison__mutmut_22": x_plot_news_impact_comparison__mutmut_22,
    "x_plot_news_impact_comparison__mutmut_23": x_plot_news_impact_comparison__mutmut_23,
    "x_plot_news_impact_comparison__mutmut_24": x_plot_news_impact_comparison__mutmut_24,
    "x_plot_news_impact_comparison__mutmut_25": x_plot_news_impact_comparison__mutmut_25,
    "x_plot_news_impact_comparison__mutmut_26": x_plot_news_impact_comparison__mutmut_26,
    "x_plot_news_impact_comparison__mutmut_27": x_plot_news_impact_comparison__mutmut_27,
    "x_plot_news_impact_comparison__mutmut_28": x_plot_news_impact_comparison__mutmut_28,
    "x_plot_news_impact_comparison__mutmut_29": x_plot_news_impact_comparison__mutmut_29,
    "x_plot_news_impact_comparison__mutmut_30": x_plot_news_impact_comparison__mutmut_30,
    "x_plot_news_impact_comparison__mutmut_31": x_plot_news_impact_comparison__mutmut_31,
    "x_plot_news_impact_comparison__mutmut_32": x_plot_news_impact_comparison__mutmut_32,
    "x_plot_news_impact_comparison__mutmut_33": x_plot_news_impact_comparison__mutmut_33,
    "x_plot_news_impact_comparison__mutmut_34": x_plot_news_impact_comparison__mutmut_34,
    "x_plot_news_impact_comparison__mutmut_35": x_plot_news_impact_comparison__mutmut_35,
    "x_plot_news_impact_comparison__mutmut_36": x_plot_news_impact_comparison__mutmut_36,
    "x_plot_news_impact_comparison__mutmut_37": x_plot_news_impact_comparison__mutmut_37,
    "x_plot_news_impact_comparison__mutmut_38": x_plot_news_impact_comparison__mutmut_38,
    "x_plot_news_impact_comparison__mutmut_39": x_plot_news_impact_comparison__mutmut_39,
    "x_plot_news_impact_comparison__mutmut_40": x_plot_news_impact_comparison__mutmut_40,
    "x_plot_news_impact_comparison__mutmut_41": x_plot_news_impact_comparison__mutmut_41,
    "x_plot_news_impact_comparison__mutmut_42": x_plot_news_impact_comparison__mutmut_42,
    "x_plot_news_impact_comparison__mutmut_43": x_plot_news_impact_comparison__mutmut_43,
    "x_plot_news_impact_comparison__mutmut_44": x_plot_news_impact_comparison__mutmut_44,
    "x_plot_news_impact_comparison__mutmut_45": x_plot_news_impact_comparison__mutmut_45,
    "x_plot_news_impact_comparison__mutmut_46": x_plot_news_impact_comparison__mutmut_46,
    "x_plot_news_impact_comparison__mutmut_47": x_plot_news_impact_comparison__mutmut_47,
    "x_plot_news_impact_comparison__mutmut_48": x_plot_news_impact_comparison__mutmut_48,
    "x_plot_news_impact_comparison__mutmut_49": x_plot_news_impact_comparison__mutmut_49,
    "x_plot_news_impact_comparison__mutmut_50": x_plot_news_impact_comparison__mutmut_50,
    "x_plot_news_impact_comparison__mutmut_51": x_plot_news_impact_comparison__mutmut_51,
    "x_plot_news_impact_comparison__mutmut_52": x_plot_news_impact_comparison__mutmut_52,
    "x_plot_news_impact_comparison__mutmut_53": x_plot_news_impact_comparison__mutmut_53,
    "x_plot_news_impact_comparison__mutmut_54": x_plot_news_impact_comparison__mutmut_54,
    "x_plot_news_impact_comparison__mutmut_55": x_plot_news_impact_comparison__mutmut_55,
    "x_plot_news_impact_comparison__mutmut_56": x_plot_news_impact_comparison__mutmut_56,
    "x_plot_news_impact_comparison__mutmut_57": x_plot_news_impact_comparison__mutmut_57,
    "x_plot_news_impact_comparison__mutmut_58": x_plot_news_impact_comparison__mutmut_58,
    "x_plot_news_impact_comparison__mutmut_59": x_plot_news_impact_comparison__mutmut_59,
    "x_plot_news_impact_comparison__mutmut_60": x_plot_news_impact_comparison__mutmut_60,
    "x_plot_news_impact_comparison__mutmut_61": x_plot_news_impact_comparison__mutmut_61,
    "x_plot_news_impact_comparison__mutmut_62": x_plot_news_impact_comparison__mutmut_62,
    "x_plot_news_impact_comparison__mutmut_63": x_plot_news_impact_comparison__mutmut_63,
    "x_plot_news_impact_comparison__mutmut_64": x_plot_news_impact_comparison__mutmut_64,
    "x_plot_news_impact_comparison__mutmut_65": x_plot_news_impact_comparison__mutmut_65,
    "x_plot_news_impact_comparison__mutmut_66": x_plot_news_impact_comparison__mutmut_66,
    "x_plot_news_impact_comparison__mutmut_67": x_plot_news_impact_comparison__mutmut_67,
    "x_plot_news_impact_comparison__mutmut_68": x_plot_news_impact_comparison__mutmut_68,
    "x_plot_news_impact_comparison__mutmut_69": x_plot_news_impact_comparison__mutmut_69,
    "x_plot_news_impact_comparison__mutmut_70": x_plot_news_impact_comparison__mutmut_70,
    "x_plot_news_impact_comparison__mutmut_71": x_plot_news_impact_comparison__mutmut_71,
    "x_plot_news_impact_comparison__mutmut_72": x_plot_news_impact_comparison__mutmut_72,
    "x_plot_news_impact_comparison__mutmut_73": x_plot_news_impact_comparison__mutmut_73,
    "x_plot_news_impact_comparison__mutmut_74": x_plot_news_impact_comparison__mutmut_74,
    "x_plot_news_impact_comparison__mutmut_75": x_plot_news_impact_comparison__mutmut_75,
    "x_plot_news_impact_comparison__mutmut_76": x_plot_news_impact_comparison__mutmut_76,
    "x_plot_news_impact_comparison__mutmut_77": x_plot_news_impact_comparison__mutmut_77,
    "x_plot_news_impact_comparison__mutmut_78": x_plot_news_impact_comparison__mutmut_78,
    "x_plot_news_impact_comparison__mutmut_79": x_plot_news_impact_comparison__mutmut_79,
    "x_plot_news_impact_comparison__mutmut_80": x_plot_news_impact_comparison__mutmut_80,
    "x_plot_news_impact_comparison__mutmut_81": x_plot_news_impact_comparison__mutmut_81,
    "x_plot_news_impact_comparison__mutmut_82": x_plot_news_impact_comparison__mutmut_82,
    "x_plot_news_impact_comparison__mutmut_83": x_plot_news_impact_comparison__mutmut_83,
    "x_plot_news_impact_comparison__mutmut_84": x_plot_news_impact_comparison__mutmut_84,
    "x_plot_news_impact_comparison__mutmut_85": x_plot_news_impact_comparison__mutmut_85,
    "x_plot_news_impact_comparison__mutmut_86": x_plot_news_impact_comparison__mutmut_86,
    "x_plot_news_impact_comparison__mutmut_87": x_plot_news_impact_comparison__mutmut_87,
    "x_plot_news_impact_comparison__mutmut_88": x_plot_news_impact_comparison__mutmut_88,
    "x_plot_news_impact_comparison__mutmut_89": x_plot_news_impact_comparison__mutmut_89,
    "x_plot_news_impact_comparison__mutmut_90": x_plot_news_impact_comparison__mutmut_90,
    "x_plot_news_impact_comparison__mutmut_91": x_plot_news_impact_comparison__mutmut_91,
    "x_plot_news_impact_comparison__mutmut_92": x_plot_news_impact_comparison__mutmut_92,
    "x_plot_news_impact_comparison__mutmut_93": x_plot_news_impact_comparison__mutmut_93,
    "x_plot_news_impact_comparison__mutmut_94": x_plot_news_impact_comparison__mutmut_94,
    "x_plot_news_impact_comparison__mutmut_95": x_plot_news_impact_comparison__mutmut_95,
    "x_plot_news_impact_comparison__mutmut_96": x_plot_news_impact_comparison__mutmut_96,
    "x_plot_news_impact_comparison__mutmut_97": x_plot_news_impact_comparison__mutmut_97,
    "x_plot_news_impact_comparison__mutmut_98": x_plot_news_impact_comparison__mutmut_98,
    "x_plot_news_impact_comparison__mutmut_99": x_plot_news_impact_comparison__mutmut_99,
    "x_plot_news_impact_comparison__mutmut_100": x_plot_news_impact_comparison__mutmut_100,
    "x_plot_news_impact_comparison__mutmut_101": x_plot_news_impact_comparison__mutmut_101,
    "x_plot_news_impact_comparison__mutmut_102": x_plot_news_impact_comparison__mutmut_102,
    "x_plot_news_impact_comparison__mutmut_103": x_plot_news_impact_comparison__mutmut_103,
    "x_plot_news_impact_comparison__mutmut_104": x_plot_news_impact_comparison__mutmut_104,
    "x_plot_news_impact_comparison__mutmut_105": x_plot_news_impact_comparison__mutmut_105,
    "x_plot_news_impact_comparison__mutmut_106": x_plot_news_impact_comparison__mutmut_106,
    "x_plot_news_impact_comparison__mutmut_107": x_plot_news_impact_comparison__mutmut_107,
    "x_plot_news_impact_comparison__mutmut_108": x_plot_news_impact_comparison__mutmut_108,
    "x_plot_news_impact_comparison__mutmut_109": x_plot_news_impact_comparison__mutmut_109,
    "x_plot_news_impact_comparison__mutmut_110": x_plot_news_impact_comparison__mutmut_110,
    "x_plot_news_impact_comparison__mutmut_111": x_plot_news_impact_comparison__mutmut_111,
    "x_plot_news_impact_comparison__mutmut_112": x_plot_news_impact_comparison__mutmut_112,
    "x_plot_news_impact_comparison__mutmut_113": x_plot_news_impact_comparison__mutmut_113,
    "x_plot_news_impact_comparison__mutmut_114": x_plot_news_impact_comparison__mutmut_114,
    "x_plot_news_impact_comparison__mutmut_115": x_plot_news_impact_comparison__mutmut_115,
    "x_plot_news_impact_comparison__mutmut_116": x_plot_news_impact_comparison__mutmut_116,
    "x_plot_news_impact_comparison__mutmut_117": x_plot_news_impact_comparison__mutmut_117,
    "x_plot_news_impact_comparison__mutmut_118": x_plot_news_impact_comparison__mutmut_118,
    "x_plot_news_impact_comparison__mutmut_119": x_plot_news_impact_comparison__mutmut_119,
    "x_plot_news_impact_comparison__mutmut_120": x_plot_news_impact_comparison__mutmut_120,
    "x_plot_news_impact_comparison__mutmut_121": x_plot_news_impact_comparison__mutmut_121,
    "x_plot_news_impact_comparison__mutmut_122": x_plot_news_impact_comparison__mutmut_122,
    "x_plot_news_impact_comparison__mutmut_123": x_plot_news_impact_comparison__mutmut_123,
    "x_plot_news_impact_comparison__mutmut_124": x_plot_news_impact_comparison__mutmut_124,
    "x_plot_news_impact_comparison__mutmut_125": x_plot_news_impact_comparison__mutmut_125,
    "x_plot_news_impact_comparison__mutmut_126": x_plot_news_impact_comparison__mutmut_126,
    "x_plot_news_impact_comparison__mutmut_127": x_plot_news_impact_comparison__mutmut_127,
    "x_plot_news_impact_comparison__mutmut_128": x_plot_news_impact_comparison__mutmut_128,
    "x_plot_news_impact_comparison__mutmut_129": x_plot_news_impact_comparison__mutmut_129,
    "x_plot_news_impact_comparison__mutmut_130": x_plot_news_impact_comparison__mutmut_130,
    "x_plot_news_impact_comparison__mutmut_131": x_plot_news_impact_comparison__mutmut_131,
    "x_plot_news_impact_comparison__mutmut_132": x_plot_news_impact_comparison__mutmut_132,
    "x_plot_news_impact_comparison__mutmut_133": x_plot_news_impact_comparison__mutmut_133,
    "x_plot_news_impact_comparison__mutmut_134": x_plot_news_impact_comparison__mutmut_134,
    "x_plot_news_impact_comparison__mutmut_135": x_plot_news_impact_comparison__mutmut_135,
    "x_plot_news_impact_comparison__mutmut_136": x_plot_news_impact_comparison__mutmut_136,
    "x_plot_news_impact_comparison__mutmut_137": x_plot_news_impact_comparison__mutmut_137,
    "x_plot_news_impact_comparison__mutmut_138": x_plot_news_impact_comparison__mutmut_138,
    "x_plot_news_impact_comparison__mutmut_139": x_plot_news_impact_comparison__mutmut_139,
    "x_plot_news_impact_comparison__mutmut_140": x_plot_news_impact_comparison__mutmut_140,
    "x_plot_news_impact_comparison__mutmut_141": x_plot_news_impact_comparison__mutmut_141,
    "x_plot_news_impact_comparison__mutmut_142": x_plot_news_impact_comparison__mutmut_142,
    "x_plot_news_impact_comparison__mutmut_143": x_plot_news_impact_comparison__mutmut_143,
    "x_plot_news_impact_comparison__mutmut_144": x_plot_news_impact_comparison__mutmut_144,
    "x_plot_news_impact_comparison__mutmut_145": x_plot_news_impact_comparison__mutmut_145,
    "x_plot_news_impact_comparison__mutmut_146": x_plot_news_impact_comparison__mutmut_146,
    "x_plot_news_impact_comparison__mutmut_147": x_plot_news_impact_comparison__mutmut_147,
    "x_plot_news_impact_comparison__mutmut_148": x_plot_news_impact_comparison__mutmut_148,
    "x_plot_news_impact_comparison__mutmut_149": x_plot_news_impact_comparison__mutmut_149,
    "x_plot_news_impact_comparison__mutmut_150": x_plot_news_impact_comparison__mutmut_150,
}
x_plot_news_impact_comparison__mutmut_orig.__name__ = "x_plot_news_impact_comparison"


def _compute_news_impact(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    args = [results, n_points, n_sigma]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x__compute_news_impact__mutmut_orig,
        x__compute_news_impact__mutmut_mutants,
        args,
        kwargs,
        None,
    )


def x__compute_news_impact__mutmut_orig(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_1(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(None, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_2(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, None):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_3(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr("conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_4(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(
        results,
    ):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_5(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "XXconditional_volatilityXX"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_6(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "CONDITIONAL_VOLATILITY"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_7(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = None
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_8(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(None)
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_9(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(None))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_10(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = None

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_11(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 1.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_12(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = None

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_13(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(None, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_14(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, None, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_15(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, None)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_16(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_17(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_18(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(
        -n_sigma * sigma_bar,
        n_sigma * sigma_bar,
    )

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_19(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma / sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_20(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(+n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_21(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma / sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_22(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_23(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(None, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_24(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=None) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_25(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_26(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = (
        np.asarray(
            results.params,
        )
        if hasattr(results, "params")
        else None
    )

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_27(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(None, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_28(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, None) else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_29(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr("params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_30(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = (
        np.asarray(results.params, dtype=np.float64)
        if hasattr(
            results,
        )
        else None
    )

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_31(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = (
        np.asarray(results.params, dtype=np.float64) if hasattr(results, "XXparamsXX") else None
    )

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_32(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "PARAMS") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_33(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None or len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_34(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_35(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) > 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_36(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 4:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_37(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = None
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_38(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[1]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_39(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = None
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_40(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[2]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_41(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = None
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_42(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[3] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_43(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) >= 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_44(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 3 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_45(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 1.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_46(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = None

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_47(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega * (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_48(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha + beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_49(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 + alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_50(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (2.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_51(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha - beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_52(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) <= 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_53(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 2 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_54(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar * 2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_55(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**3

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_56(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = None
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_57(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 - beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_58(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega - alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_59(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha / eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_60(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range * 2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_61(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**3 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_62(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta / sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_63(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = None

    return eps_range, sigma2


def x__compute_news_impact__mutmut_64(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 - 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_65(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar * 2 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_66(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**3 + 0.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_67(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 / eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_68(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 1.1 * eps_range**2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_69(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range * 2

    return eps_range, sigma2


def x__compute_news_impact__mutmut_70(
    results: Any, n_points: int, n_sigma: float
) -> tuple[NDArray[np.float64], NDArray[np.float64]]:
    """Compute News Impact Curve from model parameters.

    Fallback computation when results doesn't have a news_impact method.
    Uses the variance recursion with a single shock.
    """
    # Get unconditional variance
    if hasattr(results, "conditional_volatility"):
        sigma_bar = float(np.mean(results.conditional_volatility))
    else:
        sigma_bar = 0.01

    eps_range = np.linspace(-n_sigma * sigma_bar, n_sigma * sigma_bar, n_points)

    # Get model parameters
    params = np.asarray(results.params, dtype=np.float64) if hasattr(results, "params") else None

    if params is not None and len(params) >= 3:
        omega = params[0]
        alpha = params[1]
        beta = params[2] if len(params) > 2 else 0.0
        sigma2_bar = omega / (1.0 - alpha - beta) if (alpha + beta) < 1 else sigma_bar**2

        # GARCH NIC: sigma^2_t = omega + alpha * eps^2_{t-1} + beta * sigma^2_bar
        sigma2 = omega + alpha * eps_range**2 + beta * sigma2_bar
    else:
        # Fallback: simple parabola
        sigma2 = sigma_bar**2 + 0.1 * eps_range**3

    return eps_range, sigma2


x__compute_news_impact__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x__compute_news_impact__mutmut_1": x__compute_news_impact__mutmut_1,
    "x__compute_news_impact__mutmut_2": x__compute_news_impact__mutmut_2,
    "x__compute_news_impact__mutmut_3": x__compute_news_impact__mutmut_3,
    "x__compute_news_impact__mutmut_4": x__compute_news_impact__mutmut_4,
    "x__compute_news_impact__mutmut_5": x__compute_news_impact__mutmut_5,
    "x__compute_news_impact__mutmut_6": x__compute_news_impact__mutmut_6,
    "x__compute_news_impact__mutmut_7": x__compute_news_impact__mutmut_7,
    "x__compute_news_impact__mutmut_8": x__compute_news_impact__mutmut_8,
    "x__compute_news_impact__mutmut_9": x__compute_news_impact__mutmut_9,
    "x__compute_news_impact__mutmut_10": x__compute_news_impact__mutmut_10,
    "x__compute_news_impact__mutmut_11": x__compute_news_impact__mutmut_11,
    "x__compute_news_impact__mutmut_12": x__compute_news_impact__mutmut_12,
    "x__compute_news_impact__mutmut_13": x__compute_news_impact__mutmut_13,
    "x__compute_news_impact__mutmut_14": x__compute_news_impact__mutmut_14,
    "x__compute_news_impact__mutmut_15": x__compute_news_impact__mutmut_15,
    "x__compute_news_impact__mutmut_16": x__compute_news_impact__mutmut_16,
    "x__compute_news_impact__mutmut_17": x__compute_news_impact__mutmut_17,
    "x__compute_news_impact__mutmut_18": x__compute_news_impact__mutmut_18,
    "x__compute_news_impact__mutmut_19": x__compute_news_impact__mutmut_19,
    "x__compute_news_impact__mutmut_20": x__compute_news_impact__mutmut_20,
    "x__compute_news_impact__mutmut_21": x__compute_news_impact__mutmut_21,
    "x__compute_news_impact__mutmut_22": x__compute_news_impact__mutmut_22,
    "x__compute_news_impact__mutmut_23": x__compute_news_impact__mutmut_23,
    "x__compute_news_impact__mutmut_24": x__compute_news_impact__mutmut_24,
    "x__compute_news_impact__mutmut_25": x__compute_news_impact__mutmut_25,
    "x__compute_news_impact__mutmut_26": x__compute_news_impact__mutmut_26,
    "x__compute_news_impact__mutmut_27": x__compute_news_impact__mutmut_27,
    "x__compute_news_impact__mutmut_28": x__compute_news_impact__mutmut_28,
    "x__compute_news_impact__mutmut_29": x__compute_news_impact__mutmut_29,
    "x__compute_news_impact__mutmut_30": x__compute_news_impact__mutmut_30,
    "x__compute_news_impact__mutmut_31": x__compute_news_impact__mutmut_31,
    "x__compute_news_impact__mutmut_32": x__compute_news_impact__mutmut_32,
    "x__compute_news_impact__mutmut_33": x__compute_news_impact__mutmut_33,
    "x__compute_news_impact__mutmut_34": x__compute_news_impact__mutmut_34,
    "x__compute_news_impact__mutmut_35": x__compute_news_impact__mutmut_35,
    "x__compute_news_impact__mutmut_36": x__compute_news_impact__mutmut_36,
    "x__compute_news_impact__mutmut_37": x__compute_news_impact__mutmut_37,
    "x__compute_news_impact__mutmut_38": x__compute_news_impact__mutmut_38,
    "x__compute_news_impact__mutmut_39": x__compute_news_impact__mutmut_39,
    "x__compute_news_impact__mutmut_40": x__compute_news_impact__mutmut_40,
    "x__compute_news_impact__mutmut_41": x__compute_news_impact__mutmut_41,
    "x__compute_news_impact__mutmut_42": x__compute_news_impact__mutmut_42,
    "x__compute_news_impact__mutmut_43": x__compute_news_impact__mutmut_43,
    "x__compute_news_impact__mutmut_44": x__compute_news_impact__mutmut_44,
    "x__compute_news_impact__mutmut_45": x__compute_news_impact__mutmut_45,
    "x__compute_news_impact__mutmut_46": x__compute_news_impact__mutmut_46,
    "x__compute_news_impact__mutmut_47": x__compute_news_impact__mutmut_47,
    "x__compute_news_impact__mutmut_48": x__compute_news_impact__mutmut_48,
    "x__compute_news_impact__mutmut_49": x__compute_news_impact__mutmut_49,
    "x__compute_news_impact__mutmut_50": x__compute_news_impact__mutmut_50,
    "x__compute_news_impact__mutmut_51": x__compute_news_impact__mutmut_51,
    "x__compute_news_impact__mutmut_52": x__compute_news_impact__mutmut_52,
    "x__compute_news_impact__mutmut_53": x__compute_news_impact__mutmut_53,
    "x__compute_news_impact__mutmut_54": x__compute_news_impact__mutmut_54,
    "x__compute_news_impact__mutmut_55": x__compute_news_impact__mutmut_55,
    "x__compute_news_impact__mutmut_56": x__compute_news_impact__mutmut_56,
    "x__compute_news_impact__mutmut_57": x__compute_news_impact__mutmut_57,
    "x__compute_news_impact__mutmut_58": x__compute_news_impact__mutmut_58,
    "x__compute_news_impact__mutmut_59": x__compute_news_impact__mutmut_59,
    "x__compute_news_impact__mutmut_60": x__compute_news_impact__mutmut_60,
    "x__compute_news_impact__mutmut_61": x__compute_news_impact__mutmut_61,
    "x__compute_news_impact__mutmut_62": x__compute_news_impact__mutmut_62,
    "x__compute_news_impact__mutmut_63": x__compute_news_impact__mutmut_63,
    "x__compute_news_impact__mutmut_64": x__compute_news_impact__mutmut_64,
    "x__compute_news_impact__mutmut_65": x__compute_news_impact__mutmut_65,
    "x__compute_news_impact__mutmut_66": x__compute_news_impact__mutmut_66,
    "x__compute_news_impact__mutmut_67": x__compute_news_impact__mutmut_67,
    "x__compute_news_impact__mutmut_68": x__compute_news_impact__mutmut_68,
    "x__compute_news_impact__mutmut_69": x__compute_news_impact__mutmut_69,
    "x__compute_news_impact__mutmut_70": x__compute_news_impact__mutmut_70,
}
x__compute_news_impact__mutmut_orig.__name__ = "x__compute_news_impact"
