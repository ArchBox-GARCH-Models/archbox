"""Volatility visualization for archbox.

Provides plots for conditional volatility, returns with volatility bands,
standardized residuals, and variance persistence analysis.
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Annotated, Any, ClassVar

import matplotlib.pyplot as plt
import numpy as np
from matplotlib.figure import Figure
from numpy.typing import NDArray

from archbox.visualization.themes import Theme, get_theme

MutantDict = Annotated[dict[str, Callable], "Mutant"]  # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg=None):  # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os  # type: ignore

    mutant_under_test = os.environ["MUTANT_UNDER_TEST"]  # type: ignore
    if mutant_under_test == "fail":  # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException  # type: ignore

        raise MutmutProgrammaticFailException("Failed programmatically")  # type: ignore
    elif mutant_under_test == "stats":  # type: ignore
        from mutmut.__main__ import record_trampoline_hit  # type: ignore

        record_trampoline_hit(orig.__module__ + "." + orig.__name__)  # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    prefix = orig.__module__ + "." + orig.__name__ + "__mutmut_"  # type: ignore
    if not mutant_under_test.startswith(prefix):  # type: ignore
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    mutant_name = mutant_under_test.rpartition(".")[-1]  # type: ignore
    if self_arg is not None:  # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)  # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)  # type: ignore
    return result  # type: ignore


def plot_volatility(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    args = [results, annualize, ci, theme, figsize, title]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_plot_volatility__mutmut_orig, x_plot_volatility__mutmut_mutants, args, kwargs, None
    )


def x_plot_volatility__mutmut_orig(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_1(
    results: Any,
    annualize: bool = True,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_2(
    results: Any,
    annualize: bool = False,
    ci: float = 1.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_3(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "XXprofessionalXX",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_4(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "PROFESSIONAL",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_5(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = None

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_6(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(None)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_7(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = None
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_8(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(None):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_9(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = None

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_10(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize and (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_11(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[1], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_12(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] / 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_13(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[2] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_14(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 2.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_15(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = None
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_16(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(None, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_17(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=None)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_18(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_19(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(
            results.resid,
        )
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_20(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = None

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_21(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(None, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_22(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=None)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_23(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_24(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(
            results.conditional_volatility,
        )

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_25(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") or results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_26(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(None, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_27(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, None) and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_28(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr("std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_29(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if (
            hasattr(
                results,
            )
            and results.std_resid is not None
        ):
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_30(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "XXstd_residXX") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_31(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "STD_RESID") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_32(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_33(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = None
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_34(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(None, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_35(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=None)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_36(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_37(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(
                results.std_resid,
            )
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_38(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = None

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_39(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns * np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_40(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(None, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_41(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, None)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_42(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_43(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(
                sigma,
            )

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_44(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1.000000000001)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_45(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = None
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_46(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 4
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_47(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = None
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_48(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(None) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_49(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(253) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_50(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 2.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_51(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = None

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_52(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma / scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_53(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier

        z_ci = None

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_54(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(None)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_55(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 - ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_56(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(1.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_57(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci * 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_58(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 3)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_59(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = None
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_60(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = None

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_61(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(None)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_62(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = None

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_63(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(None, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_64(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, None, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_65(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=None, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_66(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=None)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_67(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_68(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_69(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_70(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(
            n_panels,
            1,
            figsize=fig_size,
        )

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_71(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 2, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_72(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=False)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_73(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = None
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_74(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[1]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_75(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            None,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_76(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            None,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_77(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=None,
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_78(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=None,
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_79(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=None,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_80(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_81(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_82(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_83(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_84(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_85(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get(None, "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_86(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", None),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_87(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_88(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get(
                "returns",
            ),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_89(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("XXreturnsXX", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_90(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("RETURNS", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_91(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "XX#7f7f7fXX"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_92(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7F7F7F"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_93(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get(None, 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_94(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", None),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_95(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get(0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_96(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get(
                "secondary",
            ),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_97(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("XXsecondaryXX", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_98(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("SECONDARY", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_99(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 1.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_100(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=1.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_101(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=None,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_102(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=None,
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_103(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=None,
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_104(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_105(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_106(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_107(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=1,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_108(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get(None, "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_109(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", None),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_110(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_111(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get(
                "grid",
            ),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_112(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("XXgridXX", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_113(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("GRID", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_114(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "XX#ccccccXX"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_115(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#CCCCCC"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_116(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(None, 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_117(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", None),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_118(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_119(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(
                "grid",
            ),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_120(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("XXgridXX", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_121(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("GRID", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_122(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 1.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_123(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel(None)
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_124(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("XXReturnsXX")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_125(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_126(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("RETURNS")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_127(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(None)

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_128(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title and "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_129(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "XXConditional Volatility AnalysisXX")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_130(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "conditional volatility analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_131(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "CONDITIONAL VOLATILITY ANALYSIS")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_132(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = None
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_133(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[2]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_134(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            None,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_135(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            None,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_136(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=None,
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_137(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=None,
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_138(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=None,
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_139(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_140(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_141(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_142(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_143(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_144(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get(None, "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_145(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", None),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_146(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_147(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get(
                "volatility",
            ),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_148(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("XXvolatilityXX", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_149(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("VOLATILITY", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_150(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "XX#2e75b6XX"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_151(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2E75B6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_152(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get(None, 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_153(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", None),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_154(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get(1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_155(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get(
                "primary",
            ),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_156(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("XXprimaryXX", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_157(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("PRIMARY", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_158(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 2.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_159(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"XX$\sigma_t$XX",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_160(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sIGMA_T$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_161(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            None,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_162(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            None,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_163(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            None,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_164(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=None,
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_165(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=None,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_166(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label=None,
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_167(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_168(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_169(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_170(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_171(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_172(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_173(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) / scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_174(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            +np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_175(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(None) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_176(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) / scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_177(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(None) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_178(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get(None, "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_179(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", None),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_180(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_181(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get(
                "returns",
            ),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_182(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("XXreturnsXX", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_183(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("RETURNS", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_184(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "XX#7f7f7fXX"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_185(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7F7F7F"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_186(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=1.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_187(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="XXReturnsXX",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_188(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_189(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="RETURNS",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_190(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = None
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_191(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled / z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_192(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = None
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_193(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled / z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_194(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = +sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_195(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            None,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_196(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            None,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_197(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            None,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_198(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=None,
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_199(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=None,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_200(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=None,
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_201(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_202(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_203(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_204(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_205(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_206(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_207(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get(None, "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_208(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", None),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_209(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_210(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get(
                "confidence_band",
            ),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_211(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("XXconfidence_bandXX", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_212(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("CONFIDENCE_BAND", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_213(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "XX#2e75b6XX"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_214(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2E75B6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_215(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=1.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_216(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = None
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_217(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "XXAnnualized VolatilityXX" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_218(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "annualized volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_219(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "ANNUALIZED VOLATILITY" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_220(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "XXConditional VolatilityXX"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_221(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "conditional volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_222(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "CONDITIONAL VOLATILITY"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_223(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(None)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_224(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc=None, framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_225(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=None)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_226(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_227(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(
            loc="upper right",
        )

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_228(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="XXupper rightXX", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_229(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="UPPER RIGHT", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_230(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=1.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_231(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = None
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_232(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[3]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_233(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            None,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_234(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            None,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_235(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=None,
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_236(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=None,
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_237(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=None,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_238(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_239(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_240(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_241(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_242(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_243(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get(None, "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_244(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", None),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_245(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_246(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get(
                "residuals",
            ),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_247(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("XXresidualsXX", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_248(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("RESIDUALS", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_249(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "XX#548235XX"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_250(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get(None, 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_251(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", None),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_252(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get(0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_253(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get(
                "secondary",
            ),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_254(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("XXsecondaryXX", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_255(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("SECONDARY", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_256(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 1.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_257(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=1.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_258(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=None,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_259(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=None,
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_260(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle=None,
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_261(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=None,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_262(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=None,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_263(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_264(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_265(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_266(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_267(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_268(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=3,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_269(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get(None, "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_270(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", None),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_271(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_272(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get(
                "accent",
            ),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_273(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("XXaccentXX", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_274(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("ACCENT", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_275(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "XX#c00000XX"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_276(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#C00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_277(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="XX--XX",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_278(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=1.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_279(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=1.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_280(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=None,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_281(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=None,
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_282(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle=None,
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_283(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=None,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_284(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=None,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_285(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_286(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_287(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_288(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_289(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_290(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=+2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_291(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-3,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_292(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get(None, "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_293(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", None),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_294(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_295(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get(
                "accent",
            ),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_296(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("XXaccentXX", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_297(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("ACCENT", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_298(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "XX#c00000XX"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_299(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#C00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_300(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="XX--XX",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_301(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=1.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_302(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=1.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_303(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=None,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_304(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=None,
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_305(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=None,
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_306(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_307(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_308(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_309(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=1,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_310(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get(None, "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_311(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", None),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_312(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_313(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get(
                "grid",
            ),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_314(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("XXgridXX", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_315(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("GRID", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_316(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "XX#ccccccXX"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_317(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#CCCCCC"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_318(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(None, 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_319(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", None),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_320(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_321(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get(
                "grid",
            ),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_322(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("XXgridXX", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_323(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("GRID", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_324(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 1.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_325(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel(None)
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_326(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("XXStandardized ResidualsXX")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_327(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("standardized residuals")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_328(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("STANDARDIZED RESIDUALS")
        ax3.set_xlabel("Observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_329(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel(None)

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_330(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("XXObservationXX")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_331(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("observation")

        fig.tight_layout()
        return fig


def x_plot_volatility__mutmut_332(
    results: Any,
    annualize: bool = False,
    ci: float = 0.95,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot conditional volatility panel.

    Creates a vertical panel with 3 subplots:
    1. Returns: time series of returns
    2. Conditional Volatility: sigma_t with confidence bands
    3. Standardized Residuals: z_t = eps_t / sigma_t with +-2 lines

    Parameters
    ----------
    results : ArchResults
        Fitted model results containing conditional_volatility,
        resid, and std_resid attributes.
    annualize : bool
        If True, multiply sigma by sqrt(252).
    ci : float
        Confidence interval level (e.g. 0.95).
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    # Apply theme
    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or (theme.figure_size[0], theme.figure_size[1] * 1.2)

        # Extract data from results
        returns: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        sigma: NDArray[np.float64] = np.asarray(results.conditional_volatility, dtype=np.float64)

        # Check for standardized residuals
        if hasattr(results, "std_resid") and results.std_resid is not None:
            std_resid: NDArray[np.float64] = np.asarray(results.std_resid, dtype=np.float64)
        else:
            std_resid = returns / np.maximum(sigma, 1e-12)

        n_panels = 3
        scale = np.sqrt(252) if annualize else 1.0
        sigma_scaled = sigma * scale

        # Confidence interval multiplier
        from scipy import stats

        z_ci = stats.norm.ppf(0.5 + ci / 2)

        t_len = len(returns)
        t_axis = np.arange(t_len)

        fig, axes = plt.subplots(n_panels, 1, figsize=fig_size, sharex=True)

        # Panel 1: Returns
        ax1 = axes[0]
        ax1.plot(
            t_axis,
            returns,
            color=theme.colors.get("returns", "#7f7f7f"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax1.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax1.set_ylabel("Returns")
        ax1.set_title(title or "Conditional Volatility Analysis")

        # Panel 2: Conditional Volatility
        ax2 = axes[1]
        ax2.plot(
            t_axis,
            sigma_scaled,
            color=theme.colors.get("volatility", "#2e75b6"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=r"$\sigma_t$",
        )

        # Returns overlay in gray
        ax2.fill_between(
            t_axis,
            -np.abs(returns) * scale,
            np.abs(returns) * scale,
            color=theme.colors.get("returns", "#7f7f7f"),
            alpha=0.15,
            label="Returns",
        )

        # Confidence bands
        upper = sigma_scaled * z_ci
        lower = -sigma_scaled * z_ci
        ax2.fill_between(
            t_axis,
            lower,
            upper,
            color=theme.colors.get("confidence_band", "#2e75b6"),
            alpha=0.1,
            label=f"{ci:.0%} CI",
        )

        ylabel = "Annualized Volatility" if annualize else "Conditional Volatility"
        ax2.set_ylabel(ylabel)
        ax2.legend(loc="upper right", framealpha=0.8)

        # Panel 3: Standardized Residuals
        ax3 = axes[2]
        ax3.plot(
            t_axis,
            std_resid,
            color=theme.colors.get("residuals", "#548235"),
            linewidth=theme.line_widths.get("secondary", 0.8),
            alpha=0.7,
        )
        ax3.axhline(
            y=2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=-2,
            color=theme.colors.get("accent", "#c00000"),
            linestyle="--",
            linewidth=0.8,
            alpha=0.6,
        )
        ax3.axhline(
            y=0,
            color=theme.colors.get("grid", "#cccccc"),
            linewidth=theme.line_widths.get("grid", 0.5),
        )
        ax3.set_ylabel("Standardized Residuals")
        ax3.set_xlabel("OBSERVATION")

        fig.tight_layout()
        return fig


x_plot_volatility__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_plot_volatility__mutmut_1": x_plot_volatility__mutmut_1,
    "x_plot_volatility__mutmut_2": x_plot_volatility__mutmut_2,
    "x_plot_volatility__mutmut_3": x_plot_volatility__mutmut_3,
    "x_plot_volatility__mutmut_4": x_plot_volatility__mutmut_4,
    "x_plot_volatility__mutmut_5": x_plot_volatility__mutmut_5,
    "x_plot_volatility__mutmut_6": x_plot_volatility__mutmut_6,
    "x_plot_volatility__mutmut_7": x_plot_volatility__mutmut_7,
    "x_plot_volatility__mutmut_8": x_plot_volatility__mutmut_8,
    "x_plot_volatility__mutmut_9": x_plot_volatility__mutmut_9,
    "x_plot_volatility__mutmut_10": x_plot_volatility__mutmut_10,
    "x_plot_volatility__mutmut_11": x_plot_volatility__mutmut_11,
    "x_plot_volatility__mutmut_12": x_plot_volatility__mutmut_12,
    "x_plot_volatility__mutmut_13": x_plot_volatility__mutmut_13,
    "x_plot_volatility__mutmut_14": x_plot_volatility__mutmut_14,
    "x_plot_volatility__mutmut_15": x_plot_volatility__mutmut_15,
    "x_plot_volatility__mutmut_16": x_plot_volatility__mutmut_16,
    "x_plot_volatility__mutmut_17": x_plot_volatility__mutmut_17,
    "x_plot_volatility__mutmut_18": x_plot_volatility__mutmut_18,
    "x_plot_volatility__mutmut_19": x_plot_volatility__mutmut_19,
    "x_plot_volatility__mutmut_20": x_plot_volatility__mutmut_20,
    "x_plot_volatility__mutmut_21": x_plot_volatility__mutmut_21,
    "x_plot_volatility__mutmut_22": x_plot_volatility__mutmut_22,
    "x_plot_volatility__mutmut_23": x_plot_volatility__mutmut_23,
    "x_plot_volatility__mutmut_24": x_plot_volatility__mutmut_24,
    "x_plot_volatility__mutmut_25": x_plot_volatility__mutmut_25,
    "x_plot_volatility__mutmut_26": x_plot_volatility__mutmut_26,
    "x_plot_volatility__mutmut_27": x_plot_volatility__mutmut_27,
    "x_plot_volatility__mutmut_28": x_plot_volatility__mutmut_28,
    "x_plot_volatility__mutmut_29": x_plot_volatility__mutmut_29,
    "x_plot_volatility__mutmut_30": x_plot_volatility__mutmut_30,
    "x_plot_volatility__mutmut_31": x_plot_volatility__mutmut_31,
    "x_plot_volatility__mutmut_32": x_plot_volatility__mutmut_32,
    "x_plot_volatility__mutmut_33": x_plot_volatility__mutmut_33,
    "x_plot_volatility__mutmut_34": x_plot_volatility__mutmut_34,
    "x_plot_volatility__mutmut_35": x_plot_volatility__mutmut_35,
    "x_plot_volatility__mutmut_36": x_plot_volatility__mutmut_36,
    "x_plot_volatility__mutmut_37": x_plot_volatility__mutmut_37,
    "x_plot_volatility__mutmut_38": x_plot_volatility__mutmut_38,
    "x_plot_volatility__mutmut_39": x_plot_volatility__mutmut_39,
    "x_plot_volatility__mutmut_40": x_plot_volatility__mutmut_40,
    "x_plot_volatility__mutmut_41": x_plot_volatility__mutmut_41,
    "x_plot_volatility__mutmut_42": x_plot_volatility__mutmut_42,
    "x_plot_volatility__mutmut_43": x_plot_volatility__mutmut_43,
    "x_plot_volatility__mutmut_44": x_plot_volatility__mutmut_44,
    "x_plot_volatility__mutmut_45": x_plot_volatility__mutmut_45,
    "x_plot_volatility__mutmut_46": x_plot_volatility__mutmut_46,
    "x_plot_volatility__mutmut_47": x_plot_volatility__mutmut_47,
    "x_plot_volatility__mutmut_48": x_plot_volatility__mutmut_48,
    "x_plot_volatility__mutmut_49": x_plot_volatility__mutmut_49,
    "x_plot_volatility__mutmut_50": x_plot_volatility__mutmut_50,
    "x_plot_volatility__mutmut_51": x_plot_volatility__mutmut_51,
    "x_plot_volatility__mutmut_52": x_plot_volatility__mutmut_52,
    "x_plot_volatility__mutmut_53": x_plot_volatility__mutmut_53,
    "x_plot_volatility__mutmut_54": x_plot_volatility__mutmut_54,
    "x_plot_volatility__mutmut_55": x_plot_volatility__mutmut_55,
    "x_plot_volatility__mutmut_56": x_plot_volatility__mutmut_56,
    "x_plot_volatility__mutmut_57": x_plot_volatility__mutmut_57,
    "x_plot_volatility__mutmut_58": x_plot_volatility__mutmut_58,
    "x_plot_volatility__mutmut_59": x_plot_volatility__mutmut_59,
    "x_plot_volatility__mutmut_60": x_plot_volatility__mutmut_60,
    "x_plot_volatility__mutmut_61": x_plot_volatility__mutmut_61,
    "x_plot_volatility__mutmut_62": x_plot_volatility__mutmut_62,
    "x_plot_volatility__mutmut_63": x_plot_volatility__mutmut_63,
    "x_plot_volatility__mutmut_64": x_plot_volatility__mutmut_64,
    "x_plot_volatility__mutmut_65": x_plot_volatility__mutmut_65,
    "x_plot_volatility__mutmut_66": x_plot_volatility__mutmut_66,
    "x_plot_volatility__mutmut_67": x_plot_volatility__mutmut_67,
    "x_plot_volatility__mutmut_68": x_plot_volatility__mutmut_68,
    "x_plot_volatility__mutmut_69": x_plot_volatility__mutmut_69,
    "x_plot_volatility__mutmut_70": x_plot_volatility__mutmut_70,
    "x_plot_volatility__mutmut_71": x_plot_volatility__mutmut_71,
    "x_plot_volatility__mutmut_72": x_plot_volatility__mutmut_72,
    "x_plot_volatility__mutmut_73": x_plot_volatility__mutmut_73,
    "x_plot_volatility__mutmut_74": x_plot_volatility__mutmut_74,
    "x_plot_volatility__mutmut_75": x_plot_volatility__mutmut_75,
    "x_plot_volatility__mutmut_76": x_plot_volatility__mutmut_76,
    "x_plot_volatility__mutmut_77": x_plot_volatility__mutmut_77,
    "x_plot_volatility__mutmut_78": x_plot_volatility__mutmut_78,
    "x_plot_volatility__mutmut_79": x_plot_volatility__mutmut_79,
    "x_plot_volatility__mutmut_80": x_plot_volatility__mutmut_80,
    "x_plot_volatility__mutmut_81": x_plot_volatility__mutmut_81,
    "x_plot_volatility__mutmut_82": x_plot_volatility__mutmut_82,
    "x_plot_volatility__mutmut_83": x_plot_volatility__mutmut_83,
    "x_plot_volatility__mutmut_84": x_plot_volatility__mutmut_84,
    "x_plot_volatility__mutmut_85": x_plot_volatility__mutmut_85,
    "x_plot_volatility__mutmut_86": x_plot_volatility__mutmut_86,
    "x_plot_volatility__mutmut_87": x_plot_volatility__mutmut_87,
    "x_plot_volatility__mutmut_88": x_plot_volatility__mutmut_88,
    "x_plot_volatility__mutmut_89": x_plot_volatility__mutmut_89,
    "x_plot_volatility__mutmut_90": x_plot_volatility__mutmut_90,
    "x_plot_volatility__mutmut_91": x_plot_volatility__mutmut_91,
    "x_plot_volatility__mutmut_92": x_plot_volatility__mutmut_92,
    "x_plot_volatility__mutmut_93": x_plot_volatility__mutmut_93,
    "x_plot_volatility__mutmut_94": x_plot_volatility__mutmut_94,
    "x_plot_volatility__mutmut_95": x_plot_volatility__mutmut_95,
    "x_plot_volatility__mutmut_96": x_plot_volatility__mutmut_96,
    "x_plot_volatility__mutmut_97": x_plot_volatility__mutmut_97,
    "x_plot_volatility__mutmut_98": x_plot_volatility__mutmut_98,
    "x_plot_volatility__mutmut_99": x_plot_volatility__mutmut_99,
    "x_plot_volatility__mutmut_100": x_plot_volatility__mutmut_100,
    "x_plot_volatility__mutmut_101": x_plot_volatility__mutmut_101,
    "x_plot_volatility__mutmut_102": x_plot_volatility__mutmut_102,
    "x_plot_volatility__mutmut_103": x_plot_volatility__mutmut_103,
    "x_plot_volatility__mutmut_104": x_plot_volatility__mutmut_104,
    "x_plot_volatility__mutmut_105": x_plot_volatility__mutmut_105,
    "x_plot_volatility__mutmut_106": x_plot_volatility__mutmut_106,
    "x_plot_volatility__mutmut_107": x_plot_volatility__mutmut_107,
    "x_plot_volatility__mutmut_108": x_plot_volatility__mutmut_108,
    "x_plot_volatility__mutmut_109": x_plot_volatility__mutmut_109,
    "x_plot_volatility__mutmut_110": x_plot_volatility__mutmut_110,
    "x_plot_volatility__mutmut_111": x_plot_volatility__mutmut_111,
    "x_plot_volatility__mutmut_112": x_plot_volatility__mutmut_112,
    "x_plot_volatility__mutmut_113": x_plot_volatility__mutmut_113,
    "x_plot_volatility__mutmut_114": x_plot_volatility__mutmut_114,
    "x_plot_volatility__mutmut_115": x_plot_volatility__mutmut_115,
    "x_plot_volatility__mutmut_116": x_plot_volatility__mutmut_116,
    "x_plot_volatility__mutmut_117": x_plot_volatility__mutmut_117,
    "x_plot_volatility__mutmut_118": x_plot_volatility__mutmut_118,
    "x_plot_volatility__mutmut_119": x_plot_volatility__mutmut_119,
    "x_plot_volatility__mutmut_120": x_plot_volatility__mutmut_120,
    "x_plot_volatility__mutmut_121": x_plot_volatility__mutmut_121,
    "x_plot_volatility__mutmut_122": x_plot_volatility__mutmut_122,
    "x_plot_volatility__mutmut_123": x_plot_volatility__mutmut_123,
    "x_plot_volatility__mutmut_124": x_plot_volatility__mutmut_124,
    "x_plot_volatility__mutmut_125": x_plot_volatility__mutmut_125,
    "x_plot_volatility__mutmut_126": x_plot_volatility__mutmut_126,
    "x_plot_volatility__mutmut_127": x_plot_volatility__mutmut_127,
    "x_plot_volatility__mutmut_128": x_plot_volatility__mutmut_128,
    "x_plot_volatility__mutmut_129": x_plot_volatility__mutmut_129,
    "x_plot_volatility__mutmut_130": x_plot_volatility__mutmut_130,
    "x_plot_volatility__mutmut_131": x_plot_volatility__mutmut_131,
    "x_plot_volatility__mutmut_132": x_plot_volatility__mutmut_132,
    "x_plot_volatility__mutmut_133": x_plot_volatility__mutmut_133,
    "x_plot_volatility__mutmut_134": x_plot_volatility__mutmut_134,
    "x_plot_volatility__mutmut_135": x_plot_volatility__mutmut_135,
    "x_plot_volatility__mutmut_136": x_plot_volatility__mutmut_136,
    "x_plot_volatility__mutmut_137": x_plot_volatility__mutmut_137,
    "x_plot_volatility__mutmut_138": x_plot_volatility__mutmut_138,
    "x_plot_volatility__mutmut_139": x_plot_volatility__mutmut_139,
    "x_plot_volatility__mutmut_140": x_plot_volatility__mutmut_140,
    "x_plot_volatility__mutmut_141": x_plot_volatility__mutmut_141,
    "x_plot_volatility__mutmut_142": x_plot_volatility__mutmut_142,
    "x_plot_volatility__mutmut_143": x_plot_volatility__mutmut_143,
    "x_plot_volatility__mutmut_144": x_plot_volatility__mutmut_144,
    "x_plot_volatility__mutmut_145": x_plot_volatility__mutmut_145,
    "x_plot_volatility__mutmut_146": x_plot_volatility__mutmut_146,
    "x_plot_volatility__mutmut_147": x_plot_volatility__mutmut_147,
    "x_plot_volatility__mutmut_148": x_plot_volatility__mutmut_148,
    "x_plot_volatility__mutmut_149": x_plot_volatility__mutmut_149,
    "x_plot_volatility__mutmut_150": x_plot_volatility__mutmut_150,
    "x_plot_volatility__mutmut_151": x_plot_volatility__mutmut_151,
    "x_plot_volatility__mutmut_152": x_plot_volatility__mutmut_152,
    "x_plot_volatility__mutmut_153": x_plot_volatility__mutmut_153,
    "x_plot_volatility__mutmut_154": x_plot_volatility__mutmut_154,
    "x_plot_volatility__mutmut_155": x_plot_volatility__mutmut_155,
    "x_plot_volatility__mutmut_156": x_plot_volatility__mutmut_156,
    "x_plot_volatility__mutmut_157": x_plot_volatility__mutmut_157,
    "x_plot_volatility__mutmut_158": x_plot_volatility__mutmut_158,
    "x_plot_volatility__mutmut_159": x_plot_volatility__mutmut_159,
    "x_plot_volatility__mutmut_160": x_plot_volatility__mutmut_160,
    "x_plot_volatility__mutmut_161": x_plot_volatility__mutmut_161,
    "x_plot_volatility__mutmut_162": x_plot_volatility__mutmut_162,
    "x_plot_volatility__mutmut_163": x_plot_volatility__mutmut_163,
    "x_plot_volatility__mutmut_164": x_plot_volatility__mutmut_164,
    "x_plot_volatility__mutmut_165": x_plot_volatility__mutmut_165,
    "x_plot_volatility__mutmut_166": x_plot_volatility__mutmut_166,
    "x_plot_volatility__mutmut_167": x_plot_volatility__mutmut_167,
    "x_plot_volatility__mutmut_168": x_plot_volatility__mutmut_168,
    "x_plot_volatility__mutmut_169": x_plot_volatility__mutmut_169,
    "x_plot_volatility__mutmut_170": x_plot_volatility__mutmut_170,
    "x_plot_volatility__mutmut_171": x_plot_volatility__mutmut_171,
    "x_plot_volatility__mutmut_172": x_plot_volatility__mutmut_172,
    "x_plot_volatility__mutmut_173": x_plot_volatility__mutmut_173,
    "x_plot_volatility__mutmut_174": x_plot_volatility__mutmut_174,
    "x_plot_volatility__mutmut_175": x_plot_volatility__mutmut_175,
    "x_plot_volatility__mutmut_176": x_plot_volatility__mutmut_176,
    "x_plot_volatility__mutmut_177": x_plot_volatility__mutmut_177,
    "x_plot_volatility__mutmut_178": x_plot_volatility__mutmut_178,
    "x_plot_volatility__mutmut_179": x_plot_volatility__mutmut_179,
    "x_plot_volatility__mutmut_180": x_plot_volatility__mutmut_180,
    "x_plot_volatility__mutmut_181": x_plot_volatility__mutmut_181,
    "x_plot_volatility__mutmut_182": x_plot_volatility__mutmut_182,
    "x_plot_volatility__mutmut_183": x_plot_volatility__mutmut_183,
    "x_plot_volatility__mutmut_184": x_plot_volatility__mutmut_184,
    "x_plot_volatility__mutmut_185": x_plot_volatility__mutmut_185,
    "x_plot_volatility__mutmut_186": x_plot_volatility__mutmut_186,
    "x_plot_volatility__mutmut_187": x_plot_volatility__mutmut_187,
    "x_plot_volatility__mutmut_188": x_plot_volatility__mutmut_188,
    "x_plot_volatility__mutmut_189": x_plot_volatility__mutmut_189,
    "x_plot_volatility__mutmut_190": x_plot_volatility__mutmut_190,
    "x_plot_volatility__mutmut_191": x_plot_volatility__mutmut_191,
    "x_plot_volatility__mutmut_192": x_plot_volatility__mutmut_192,
    "x_plot_volatility__mutmut_193": x_plot_volatility__mutmut_193,
    "x_plot_volatility__mutmut_194": x_plot_volatility__mutmut_194,
    "x_plot_volatility__mutmut_195": x_plot_volatility__mutmut_195,
    "x_plot_volatility__mutmut_196": x_plot_volatility__mutmut_196,
    "x_plot_volatility__mutmut_197": x_plot_volatility__mutmut_197,
    "x_plot_volatility__mutmut_198": x_plot_volatility__mutmut_198,
    "x_plot_volatility__mutmut_199": x_plot_volatility__mutmut_199,
    "x_plot_volatility__mutmut_200": x_plot_volatility__mutmut_200,
    "x_plot_volatility__mutmut_201": x_plot_volatility__mutmut_201,
    "x_plot_volatility__mutmut_202": x_plot_volatility__mutmut_202,
    "x_plot_volatility__mutmut_203": x_plot_volatility__mutmut_203,
    "x_plot_volatility__mutmut_204": x_plot_volatility__mutmut_204,
    "x_plot_volatility__mutmut_205": x_plot_volatility__mutmut_205,
    "x_plot_volatility__mutmut_206": x_plot_volatility__mutmut_206,
    "x_plot_volatility__mutmut_207": x_plot_volatility__mutmut_207,
    "x_plot_volatility__mutmut_208": x_plot_volatility__mutmut_208,
    "x_plot_volatility__mutmut_209": x_plot_volatility__mutmut_209,
    "x_plot_volatility__mutmut_210": x_plot_volatility__mutmut_210,
    "x_plot_volatility__mutmut_211": x_plot_volatility__mutmut_211,
    "x_plot_volatility__mutmut_212": x_plot_volatility__mutmut_212,
    "x_plot_volatility__mutmut_213": x_plot_volatility__mutmut_213,
    "x_plot_volatility__mutmut_214": x_plot_volatility__mutmut_214,
    "x_plot_volatility__mutmut_215": x_plot_volatility__mutmut_215,
    "x_plot_volatility__mutmut_216": x_plot_volatility__mutmut_216,
    "x_plot_volatility__mutmut_217": x_plot_volatility__mutmut_217,
    "x_plot_volatility__mutmut_218": x_plot_volatility__mutmut_218,
    "x_plot_volatility__mutmut_219": x_plot_volatility__mutmut_219,
    "x_plot_volatility__mutmut_220": x_plot_volatility__mutmut_220,
    "x_plot_volatility__mutmut_221": x_plot_volatility__mutmut_221,
    "x_plot_volatility__mutmut_222": x_plot_volatility__mutmut_222,
    "x_plot_volatility__mutmut_223": x_plot_volatility__mutmut_223,
    "x_plot_volatility__mutmut_224": x_plot_volatility__mutmut_224,
    "x_plot_volatility__mutmut_225": x_plot_volatility__mutmut_225,
    "x_plot_volatility__mutmut_226": x_plot_volatility__mutmut_226,
    "x_plot_volatility__mutmut_227": x_plot_volatility__mutmut_227,
    "x_plot_volatility__mutmut_228": x_plot_volatility__mutmut_228,
    "x_plot_volatility__mutmut_229": x_plot_volatility__mutmut_229,
    "x_plot_volatility__mutmut_230": x_plot_volatility__mutmut_230,
    "x_plot_volatility__mutmut_231": x_plot_volatility__mutmut_231,
    "x_plot_volatility__mutmut_232": x_plot_volatility__mutmut_232,
    "x_plot_volatility__mutmut_233": x_plot_volatility__mutmut_233,
    "x_plot_volatility__mutmut_234": x_plot_volatility__mutmut_234,
    "x_plot_volatility__mutmut_235": x_plot_volatility__mutmut_235,
    "x_plot_volatility__mutmut_236": x_plot_volatility__mutmut_236,
    "x_plot_volatility__mutmut_237": x_plot_volatility__mutmut_237,
    "x_plot_volatility__mutmut_238": x_plot_volatility__mutmut_238,
    "x_plot_volatility__mutmut_239": x_plot_volatility__mutmut_239,
    "x_plot_volatility__mutmut_240": x_plot_volatility__mutmut_240,
    "x_plot_volatility__mutmut_241": x_plot_volatility__mutmut_241,
    "x_plot_volatility__mutmut_242": x_plot_volatility__mutmut_242,
    "x_plot_volatility__mutmut_243": x_plot_volatility__mutmut_243,
    "x_plot_volatility__mutmut_244": x_plot_volatility__mutmut_244,
    "x_plot_volatility__mutmut_245": x_plot_volatility__mutmut_245,
    "x_plot_volatility__mutmut_246": x_plot_volatility__mutmut_246,
    "x_plot_volatility__mutmut_247": x_plot_volatility__mutmut_247,
    "x_plot_volatility__mutmut_248": x_plot_volatility__mutmut_248,
    "x_plot_volatility__mutmut_249": x_plot_volatility__mutmut_249,
    "x_plot_volatility__mutmut_250": x_plot_volatility__mutmut_250,
    "x_plot_volatility__mutmut_251": x_plot_volatility__mutmut_251,
    "x_plot_volatility__mutmut_252": x_plot_volatility__mutmut_252,
    "x_plot_volatility__mutmut_253": x_plot_volatility__mutmut_253,
    "x_plot_volatility__mutmut_254": x_plot_volatility__mutmut_254,
    "x_plot_volatility__mutmut_255": x_plot_volatility__mutmut_255,
    "x_plot_volatility__mutmut_256": x_plot_volatility__mutmut_256,
    "x_plot_volatility__mutmut_257": x_plot_volatility__mutmut_257,
    "x_plot_volatility__mutmut_258": x_plot_volatility__mutmut_258,
    "x_plot_volatility__mutmut_259": x_plot_volatility__mutmut_259,
    "x_plot_volatility__mutmut_260": x_plot_volatility__mutmut_260,
    "x_plot_volatility__mutmut_261": x_plot_volatility__mutmut_261,
    "x_plot_volatility__mutmut_262": x_plot_volatility__mutmut_262,
    "x_plot_volatility__mutmut_263": x_plot_volatility__mutmut_263,
    "x_plot_volatility__mutmut_264": x_plot_volatility__mutmut_264,
    "x_plot_volatility__mutmut_265": x_plot_volatility__mutmut_265,
    "x_plot_volatility__mutmut_266": x_plot_volatility__mutmut_266,
    "x_plot_volatility__mutmut_267": x_plot_volatility__mutmut_267,
    "x_plot_volatility__mutmut_268": x_plot_volatility__mutmut_268,
    "x_plot_volatility__mutmut_269": x_plot_volatility__mutmut_269,
    "x_plot_volatility__mutmut_270": x_plot_volatility__mutmut_270,
    "x_plot_volatility__mutmut_271": x_plot_volatility__mutmut_271,
    "x_plot_volatility__mutmut_272": x_plot_volatility__mutmut_272,
    "x_plot_volatility__mutmut_273": x_plot_volatility__mutmut_273,
    "x_plot_volatility__mutmut_274": x_plot_volatility__mutmut_274,
    "x_plot_volatility__mutmut_275": x_plot_volatility__mutmut_275,
    "x_plot_volatility__mutmut_276": x_plot_volatility__mutmut_276,
    "x_plot_volatility__mutmut_277": x_plot_volatility__mutmut_277,
    "x_plot_volatility__mutmut_278": x_plot_volatility__mutmut_278,
    "x_plot_volatility__mutmut_279": x_plot_volatility__mutmut_279,
    "x_plot_volatility__mutmut_280": x_plot_volatility__mutmut_280,
    "x_plot_volatility__mutmut_281": x_plot_volatility__mutmut_281,
    "x_plot_volatility__mutmut_282": x_plot_volatility__mutmut_282,
    "x_plot_volatility__mutmut_283": x_plot_volatility__mutmut_283,
    "x_plot_volatility__mutmut_284": x_plot_volatility__mutmut_284,
    "x_plot_volatility__mutmut_285": x_plot_volatility__mutmut_285,
    "x_plot_volatility__mutmut_286": x_plot_volatility__mutmut_286,
    "x_plot_volatility__mutmut_287": x_plot_volatility__mutmut_287,
    "x_plot_volatility__mutmut_288": x_plot_volatility__mutmut_288,
    "x_plot_volatility__mutmut_289": x_plot_volatility__mutmut_289,
    "x_plot_volatility__mutmut_290": x_plot_volatility__mutmut_290,
    "x_plot_volatility__mutmut_291": x_plot_volatility__mutmut_291,
    "x_plot_volatility__mutmut_292": x_plot_volatility__mutmut_292,
    "x_plot_volatility__mutmut_293": x_plot_volatility__mutmut_293,
    "x_plot_volatility__mutmut_294": x_plot_volatility__mutmut_294,
    "x_plot_volatility__mutmut_295": x_plot_volatility__mutmut_295,
    "x_plot_volatility__mutmut_296": x_plot_volatility__mutmut_296,
    "x_plot_volatility__mutmut_297": x_plot_volatility__mutmut_297,
    "x_plot_volatility__mutmut_298": x_plot_volatility__mutmut_298,
    "x_plot_volatility__mutmut_299": x_plot_volatility__mutmut_299,
    "x_plot_volatility__mutmut_300": x_plot_volatility__mutmut_300,
    "x_plot_volatility__mutmut_301": x_plot_volatility__mutmut_301,
    "x_plot_volatility__mutmut_302": x_plot_volatility__mutmut_302,
    "x_plot_volatility__mutmut_303": x_plot_volatility__mutmut_303,
    "x_plot_volatility__mutmut_304": x_plot_volatility__mutmut_304,
    "x_plot_volatility__mutmut_305": x_plot_volatility__mutmut_305,
    "x_plot_volatility__mutmut_306": x_plot_volatility__mutmut_306,
    "x_plot_volatility__mutmut_307": x_plot_volatility__mutmut_307,
    "x_plot_volatility__mutmut_308": x_plot_volatility__mutmut_308,
    "x_plot_volatility__mutmut_309": x_plot_volatility__mutmut_309,
    "x_plot_volatility__mutmut_310": x_plot_volatility__mutmut_310,
    "x_plot_volatility__mutmut_311": x_plot_volatility__mutmut_311,
    "x_plot_volatility__mutmut_312": x_plot_volatility__mutmut_312,
    "x_plot_volatility__mutmut_313": x_plot_volatility__mutmut_313,
    "x_plot_volatility__mutmut_314": x_plot_volatility__mutmut_314,
    "x_plot_volatility__mutmut_315": x_plot_volatility__mutmut_315,
    "x_plot_volatility__mutmut_316": x_plot_volatility__mutmut_316,
    "x_plot_volatility__mutmut_317": x_plot_volatility__mutmut_317,
    "x_plot_volatility__mutmut_318": x_plot_volatility__mutmut_318,
    "x_plot_volatility__mutmut_319": x_plot_volatility__mutmut_319,
    "x_plot_volatility__mutmut_320": x_plot_volatility__mutmut_320,
    "x_plot_volatility__mutmut_321": x_plot_volatility__mutmut_321,
    "x_plot_volatility__mutmut_322": x_plot_volatility__mutmut_322,
    "x_plot_volatility__mutmut_323": x_plot_volatility__mutmut_323,
    "x_plot_volatility__mutmut_324": x_plot_volatility__mutmut_324,
    "x_plot_volatility__mutmut_325": x_plot_volatility__mutmut_325,
    "x_plot_volatility__mutmut_326": x_plot_volatility__mutmut_326,
    "x_plot_volatility__mutmut_327": x_plot_volatility__mutmut_327,
    "x_plot_volatility__mutmut_328": x_plot_volatility__mutmut_328,
    "x_plot_volatility__mutmut_329": x_plot_volatility__mutmut_329,
    "x_plot_volatility__mutmut_330": x_plot_volatility__mutmut_330,
    "x_plot_volatility__mutmut_331": x_plot_volatility__mutmut_331,
    "x_plot_volatility__mutmut_332": x_plot_volatility__mutmut_332,
}
x_plot_volatility__mutmut_orig.__name__ = "x_plot_volatility"


def plot_variance_persistence(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    args = [results, max_lags, theme, figsize]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_plot_variance_persistence__mutmut_orig,
        x_plot_variance_persistence__mutmut_mutants,
        args,
        kwargs,
        None,
    )


def x_plot_variance_persistence__mutmut_orig(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_1(
    results: Any,
    max_lags: int = 51,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_2(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "XXprofessionalXX",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_3(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "PROFESSIONAL",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_4(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = None

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_5(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(None)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_6(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = None
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_7(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(None):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_8(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = None

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_9(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize and theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_10(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = None
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_11(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(None, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_12(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=None)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_13(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_14(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(
            results.resid,
        )
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_15(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = None
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_16(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids * 2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_17(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**3
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_18(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = None

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_19(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 + np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_20(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(None)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_21(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = None
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_22(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = None
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_23(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(None)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_24(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags - 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_25(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 2)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_26(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = None
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_27(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(None)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_28(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(None):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_29(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags - 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_30(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 2):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_31(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag != 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_32(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 1:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_33(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = None
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_34(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 2.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_35(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = None
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_36(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(None)
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_37(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] / resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_38(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:+lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_39(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = None

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_40(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov * var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_41(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 >= 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_42(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 1 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_43(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 1.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_44(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = None
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_45(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(None, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_46(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, None, None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_47(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr("persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_48(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_49(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = results.persistence
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_50(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "XXpersistenceXX", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_51(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "PERSISTENCE", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_52(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None or 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_53(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_54(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 1 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_55(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 <= persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_56(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence <= 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_57(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 2:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_58(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = None
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_59(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence * np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_60(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(None)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_61(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags - 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_62(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 2)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_63(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = None
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_64(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) * np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_65(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(None) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_66(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(1.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_67(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(None)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_68(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = ""
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_69(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = ""

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_70(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = None

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_71(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 * np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_72(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 2.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_73(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(None)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_74(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = None
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_75(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(None, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_76(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, None, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_77(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=None)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_78(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_79(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_80(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(
            1,
            1,
        )
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_81(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(2, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_82(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 2, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_83(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = None

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_84(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(None)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_85(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags - 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_86(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 2)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_87(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            None,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_88(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            None,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_89(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=None,
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_90(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=None,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_91(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=None,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_92(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=None,
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_93(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_94(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_95(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_96(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_97(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_98(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_99(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get(None, "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_100(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", None),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_101(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_102(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get(
                "primary",
            ),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_103(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("XXprimaryXX", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_104(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("PRIMARY", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_105(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "XX#1f4e79XX"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_106(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1F4E79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_107(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=1.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_108(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=1.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_109(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"XXACF($r^2$)XX",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_110(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"acf($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_111(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($R^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_112(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_113(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                None,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_114(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                None,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_115(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=None,
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_116(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=None,
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_117(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle=None,
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_118(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label=None,
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_119(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_120(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_121(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_122(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_123(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_124(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_125(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get(None, "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_126(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", None),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_127(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_128(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get(
                    "accent",
                ),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_129(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("XXaccentXX", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_130(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("ACCENT", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_131(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "XX#c00000XX"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_132(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#C00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_133(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get(None, 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_134(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", None),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_135(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get(1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_136(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get(
                    "primary",
                ),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_137(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("XXprimaryXX", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_138(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("PRIMARY", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_139(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 2.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_140(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="XX--XX",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_141(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="XXGARCH Fitted ACFXX",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_142(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="garch fitted acf",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_143(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH FITTED ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_144(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(y=None, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8)
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_145(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(y=sig_bound, color=None, linestyle=":", linewidth=0.8)
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_146(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=None, linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_147(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=None
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_148(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8)
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_149(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(y=sig_bound, linestyle=":", linewidth=0.8)
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_150(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linewidth=0.8)
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_151(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound,
            color=theme.colors.get("grid", "#cccccc"),
            linestyle=":",
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_152(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get(None, "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_153(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(y=sig_bound, color=theme.colors.get("grid", None), linestyle=":", linewidth=0.8)
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_154(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(y=sig_bound, color=theme.colors.get("#cccccc"), linestyle=":", linewidth=0.8)
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_155(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound,
            color=theme.colors.get(
                "grid",
            ),
            linestyle=":",
            linewidth=0.8,
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_156(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("XXgridXX", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_157(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("GRID", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_158(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "XX#ccccccXX"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_159(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#CCCCCC"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_160(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle="XX:XX", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_161(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=1.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_162(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(y=None, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8)

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_163(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(y=-sig_bound, color=None, linestyle=":", linewidth=0.8)

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_164(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=None, linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_165(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=None
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_166(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8)

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_167(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(y=-sig_bound, linestyle=":", linewidth=0.8)

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_168(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linewidth=0.8)

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_169(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound,
            color=theme.colors.get("grid", "#cccccc"),
            linestyle=":",
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_170(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=+sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_171(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get(None, "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_172(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(y=-sig_bound, color=theme.colors.get("grid", None), linestyle=":", linewidth=0.8)

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_173(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(y=-sig_bound, color=theme.colors.get("#cccccc"), linestyle=":", linewidth=0.8)

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_174(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound,
            color=theme.colors.get(
                "grid",
            ),
            linestyle=":",
            linewidth=0.8,
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_175(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound,
            color=theme.colors.get("XXgridXX", "#cccccc"),
            linestyle=":",
            linewidth=0.8,
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_176(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("GRID", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_177(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound,
            color=theme.colors.get("grid", "XX#ccccccXX"),
            linestyle=":",
            linewidth=0.8,
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_178(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#CCCCCC"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_179(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound,
            color=theme.colors.get("grid", "#cccccc"),
            linestyle="XX:XX",
            linewidth=0.8,
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_180(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=1.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_181(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel(None)
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_182(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("XXLagXX")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_183(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_184(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("LAG")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_185(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel(None)
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_186(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("XXAutocorrelationXX")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_187(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_188(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("AUTOCORRELATION")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_189(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title(None)
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_190(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("XXVariance Persistence: ACF of Squared ReturnsXX")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_191(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("variance persistence: acf of squared returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_192(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("VARIANCE PERSISTENCE: ACF OF SQUARED RETURNS")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_193(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc=None, framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_194(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=None)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_195(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_196(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(
            loc="upper right",
        )

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_197(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="XXupper rightXX", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_198(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="UPPER RIGHT", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_199(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=1.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_200(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_201(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                None,
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_202(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=None,
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_203(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords=None,
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_204(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha=None,
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_205(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=None,
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_206(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox=None,
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_207(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_208(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_209(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_210(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_211(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_212(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_213(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(1.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_214(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 1.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_215(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="XXaxes fractionXX",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_216(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="AXES FRACTION",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_217(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="XXrightXX",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_218(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="RIGHT",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_219(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get(None, 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_220(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", None),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_221(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get(9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_222(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get(
                    "annotation",
                ),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_223(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("XXannotationXX", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_224(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("ANNOTATION", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_225(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 10),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_226(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"XXboxstyleXX": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_227(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"BOXSTYLE": "round,pad=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_228(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "XXround,pad=0.3XX", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_229(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "ROUND,PAD=0.3", "facecolor": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_230(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "XXfacecolorXX": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_231(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "FACECOLOR": "wheat", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_232(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "XXwheatXX", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_233(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "WHEAT", "alpha": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_234(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "XXalphaXX": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_235(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "ALPHA": 0.5},
            )

        fig.tight_layout()
        return fig


def x_plot_variance_persistence__mutmut_236(
    results: Any,
    max_lags: int = 50,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
) -> Figure:
    """Plot variance persistence via autocorrelation analysis.

    Shows autocorrelation of squared returns with fitted GARCH ACF overlay
    and displays the half-life of volatility shocks.

    Parameters
    ----------
    results : ArchResults
        Fitted model results.
    max_lags : int
        Maximum number of lags for ACF.
    theme : str or Theme
        Visual theme name or Theme instance.
    figsize : tuple, optional
        Figure size override.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        resids: NDArray[np.float64] = np.asarray(results.resid, dtype=np.float64)
        resids2 = resids**2
        resids2_demean = resids2 - np.mean(resids2)

        # Compute empirical ACF
        t_len = len(resids2)
        acf_vals = np.zeros(max_lags + 1)
        var_r2 = np.var(resids2)
        for lag in range(max_lags + 1):
            if lag == 0:
                acf_vals[lag] = 1.0
            else:
                cov = np.mean(resids2_demean[lag:] * resids2_demean[:-lag])
                acf_vals[lag] = cov / var_r2 if var_r2 > 0 else 0.0

        # Compute theoretical GARCH ACF if persistence available
        persistence = getattr(results, "persistence", None)
        if persistence is not None and 0 < persistence < 1:
            theoretical_acf = persistence ** np.arange(max_lags + 1)
            half_life = np.log(0.5) / np.log(persistence)
        else:
            theoretical_acf = None
            half_life = None

        # Significance bounds
        sig_bound = 1.96 / np.sqrt(t_len)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)
        lags = np.arange(max_lags + 1)

        # Empirical ACF as bars
        ax.bar(
            lags,
            acf_vals,
            color=theme.colors.get("primary", "#1f4e79"),
            alpha=0.6,
            width=0.8,
            label=r"ACF($r^2$)",
        )

        # Theoretical ACF overlay
        if theoretical_acf is not None:
            ax.plot(
                lags,
                theoretical_acf,
                color=theme.colors.get("accent", "#c00000"),
                linewidth=theme.line_widths.get("primary", 1.5),
                linestyle="--",
                label="GARCH Fitted ACF",
            )

        # Significance bounds
        ax.axhline(
            y=sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )
        ax.axhline(
            y=-sig_bound, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.8
        )

        ax.set_xlabel("Lag")
        ax.set_ylabel("Autocorrelation")
        ax.set_title("Variance Persistence: ACF of Squared Returns")
        ax.legend(loc="upper right", framealpha=0.8)

        if half_life is not None:
            ax.annotate(
                f"Half-life: {half_life:.1f} obs",
                xy=(0.95, 0.85),
                xycoords="axes fraction",
                ha="right",
                fontsize=theme.font_sizes.get("annotation", 9),
                bbox={"boxstyle": "round,pad=0.3", "facecolor": "wheat", "alpha": 1.5},
            )

        fig.tight_layout()
        return fig


x_plot_variance_persistence__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_plot_variance_persistence__mutmut_1": x_plot_variance_persistence__mutmut_1,
    "x_plot_variance_persistence__mutmut_2": x_plot_variance_persistence__mutmut_2,
    "x_plot_variance_persistence__mutmut_3": x_plot_variance_persistence__mutmut_3,
    "x_plot_variance_persistence__mutmut_4": x_plot_variance_persistence__mutmut_4,
    "x_plot_variance_persistence__mutmut_5": x_plot_variance_persistence__mutmut_5,
    "x_plot_variance_persistence__mutmut_6": x_plot_variance_persistence__mutmut_6,
    "x_plot_variance_persistence__mutmut_7": x_plot_variance_persistence__mutmut_7,
    "x_plot_variance_persistence__mutmut_8": x_plot_variance_persistence__mutmut_8,
    "x_plot_variance_persistence__mutmut_9": x_plot_variance_persistence__mutmut_9,
    "x_plot_variance_persistence__mutmut_10": x_plot_variance_persistence__mutmut_10,
    "x_plot_variance_persistence__mutmut_11": x_plot_variance_persistence__mutmut_11,
    "x_plot_variance_persistence__mutmut_12": x_plot_variance_persistence__mutmut_12,
    "x_plot_variance_persistence__mutmut_13": x_plot_variance_persistence__mutmut_13,
    "x_plot_variance_persistence__mutmut_14": x_plot_variance_persistence__mutmut_14,
    "x_plot_variance_persistence__mutmut_15": x_plot_variance_persistence__mutmut_15,
    "x_plot_variance_persistence__mutmut_16": x_plot_variance_persistence__mutmut_16,
    "x_plot_variance_persistence__mutmut_17": x_plot_variance_persistence__mutmut_17,
    "x_plot_variance_persistence__mutmut_18": x_plot_variance_persistence__mutmut_18,
    "x_plot_variance_persistence__mutmut_19": x_plot_variance_persistence__mutmut_19,
    "x_plot_variance_persistence__mutmut_20": x_plot_variance_persistence__mutmut_20,
    "x_plot_variance_persistence__mutmut_21": x_plot_variance_persistence__mutmut_21,
    "x_plot_variance_persistence__mutmut_22": x_plot_variance_persistence__mutmut_22,
    "x_plot_variance_persistence__mutmut_23": x_plot_variance_persistence__mutmut_23,
    "x_plot_variance_persistence__mutmut_24": x_plot_variance_persistence__mutmut_24,
    "x_plot_variance_persistence__mutmut_25": x_plot_variance_persistence__mutmut_25,
    "x_plot_variance_persistence__mutmut_26": x_plot_variance_persistence__mutmut_26,
    "x_plot_variance_persistence__mutmut_27": x_plot_variance_persistence__mutmut_27,
    "x_plot_variance_persistence__mutmut_28": x_plot_variance_persistence__mutmut_28,
    "x_plot_variance_persistence__mutmut_29": x_plot_variance_persistence__mutmut_29,
    "x_plot_variance_persistence__mutmut_30": x_plot_variance_persistence__mutmut_30,
    "x_plot_variance_persistence__mutmut_31": x_plot_variance_persistence__mutmut_31,
    "x_plot_variance_persistence__mutmut_32": x_plot_variance_persistence__mutmut_32,
    "x_plot_variance_persistence__mutmut_33": x_plot_variance_persistence__mutmut_33,
    "x_plot_variance_persistence__mutmut_34": x_plot_variance_persistence__mutmut_34,
    "x_plot_variance_persistence__mutmut_35": x_plot_variance_persistence__mutmut_35,
    "x_plot_variance_persistence__mutmut_36": x_plot_variance_persistence__mutmut_36,
    "x_plot_variance_persistence__mutmut_37": x_plot_variance_persistence__mutmut_37,
    "x_plot_variance_persistence__mutmut_38": x_plot_variance_persistence__mutmut_38,
    "x_plot_variance_persistence__mutmut_39": x_plot_variance_persistence__mutmut_39,
    "x_plot_variance_persistence__mutmut_40": x_plot_variance_persistence__mutmut_40,
    "x_plot_variance_persistence__mutmut_41": x_plot_variance_persistence__mutmut_41,
    "x_plot_variance_persistence__mutmut_42": x_plot_variance_persistence__mutmut_42,
    "x_plot_variance_persistence__mutmut_43": x_plot_variance_persistence__mutmut_43,
    "x_plot_variance_persistence__mutmut_44": x_plot_variance_persistence__mutmut_44,
    "x_plot_variance_persistence__mutmut_45": x_plot_variance_persistence__mutmut_45,
    "x_plot_variance_persistence__mutmut_46": x_plot_variance_persistence__mutmut_46,
    "x_plot_variance_persistence__mutmut_47": x_plot_variance_persistence__mutmut_47,
    "x_plot_variance_persistence__mutmut_48": x_plot_variance_persistence__mutmut_48,
    "x_plot_variance_persistence__mutmut_49": x_plot_variance_persistence__mutmut_49,
    "x_plot_variance_persistence__mutmut_50": x_plot_variance_persistence__mutmut_50,
    "x_plot_variance_persistence__mutmut_51": x_plot_variance_persistence__mutmut_51,
    "x_plot_variance_persistence__mutmut_52": x_plot_variance_persistence__mutmut_52,
    "x_plot_variance_persistence__mutmut_53": x_plot_variance_persistence__mutmut_53,
    "x_plot_variance_persistence__mutmut_54": x_plot_variance_persistence__mutmut_54,
    "x_plot_variance_persistence__mutmut_55": x_plot_variance_persistence__mutmut_55,
    "x_plot_variance_persistence__mutmut_56": x_plot_variance_persistence__mutmut_56,
    "x_plot_variance_persistence__mutmut_57": x_plot_variance_persistence__mutmut_57,
    "x_plot_variance_persistence__mutmut_58": x_plot_variance_persistence__mutmut_58,
    "x_plot_variance_persistence__mutmut_59": x_plot_variance_persistence__mutmut_59,
    "x_plot_variance_persistence__mutmut_60": x_plot_variance_persistence__mutmut_60,
    "x_plot_variance_persistence__mutmut_61": x_plot_variance_persistence__mutmut_61,
    "x_plot_variance_persistence__mutmut_62": x_plot_variance_persistence__mutmut_62,
    "x_plot_variance_persistence__mutmut_63": x_plot_variance_persistence__mutmut_63,
    "x_plot_variance_persistence__mutmut_64": x_plot_variance_persistence__mutmut_64,
    "x_plot_variance_persistence__mutmut_65": x_plot_variance_persistence__mutmut_65,
    "x_plot_variance_persistence__mutmut_66": x_plot_variance_persistence__mutmut_66,
    "x_plot_variance_persistence__mutmut_67": x_plot_variance_persistence__mutmut_67,
    "x_plot_variance_persistence__mutmut_68": x_plot_variance_persistence__mutmut_68,
    "x_plot_variance_persistence__mutmut_69": x_plot_variance_persistence__mutmut_69,
    "x_plot_variance_persistence__mutmut_70": x_plot_variance_persistence__mutmut_70,
    "x_plot_variance_persistence__mutmut_71": x_plot_variance_persistence__mutmut_71,
    "x_plot_variance_persistence__mutmut_72": x_plot_variance_persistence__mutmut_72,
    "x_plot_variance_persistence__mutmut_73": x_plot_variance_persistence__mutmut_73,
    "x_plot_variance_persistence__mutmut_74": x_plot_variance_persistence__mutmut_74,
    "x_plot_variance_persistence__mutmut_75": x_plot_variance_persistence__mutmut_75,
    "x_plot_variance_persistence__mutmut_76": x_plot_variance_persistence__mutmut_76,
    "x_plot_variance_persistence__mutmut_77": x_plot_variance_persistence__mutmut_77,
    "x_plot_variance_persistence__mutmut_78": x_plot_variance_persistence__mutmut_78,
    "x_plot_variance_persistence__mutmut_79": x_plot_variance_persistence__mutmut_79,
    "x_plot_variance_persistence__mutmut_80": x_plot_variance_persistence__mutmut_80,
    "x_plot_variance_persistence__mutmut_81": x_plot_variance_persistence__mutmut_81,
    "x_plot_variance_persistence__mutmut_82": x_plot_variance_persistence__mutmut_82,
    "x_plot_variance_persistence__mutmut_83": x_plot_variance_persistence__mutmut_83,
    "x_plot_variance_persistence__mutmut_84": x_plot_variance_persistence__mutmut_84,
    "x_plot_variance_persistence__mutmut_85": x_plot_variance_persistence__mutmut_85,
    "x_plot_variance_persistence__mutmut_86": x_plot_variance_persistence__mutmut_86,
    "x_plot_variance_persistence__mutmut_87": x_plot_variance_persistence__mutmut_87,
    "x_plot_variance_persistence__mutmut_88": x_plot_variance_persistence__mutmut_88,
    "x_plot_variance_persistence__mutmut_89": x_plot_variance_persistence__mutmut_89,
    "x_plot_variance_persistence__mutmut_90": x_plot_variance_persistence__mutmut_90,
    "x_plot_variance_persistence__mutmut_91": x_plot_variance_persistence__mutmut_91,
    "x_plot_variance_persistence__mutmut_92": x_plot_variance_persistence__mutmut_92,
    "x_plot_variance_persistence__mutmut_93": x_plot_variance_persistence__mutmut_93,
    "x_plot_variance_persistence__mutmut_94": x_plot_variance_persistence__mutmut_94,
    "x_plot_variance_persistence__mutmut_95": x_plot_variance_persistence__mutmut_95,
    "x_plot_variance_persistence__mutmut_96": x_plot_variance_persistence__mutmut_96,
    "x_plot_variance_persistence__mutmut_97": x_plot_variance_persistence__mutmut_97,
    "x_plot_variance_persistence__mutmut_98": x_plot_variance_persistence__mutmut_98,
    "x_plot_variance_persistence__mutmut_99": x_plot_variance_persistence__mutmut_99,
    "x_plot_variance_persistence__mutmut_100": x_plot_variance_persistence__mutmut_100,
    "x_plot_variance_persistence__mutmut_101": x_plot_variance_persistence__mutmut_101,
    "x_plot_variance_persistence__mutmut_102": x_plot_variance_persistence__mutmut_102,
    "x_plot_variance_persistence__mutmut_103": x_plot_variance_persistence__mutmut_103,
    "x_plot_variance_persistence__mutmut_104": x_plot_variance_persistence__mutmut_104,
    "x_plot_variance_persistence__mutmut_105": x_plot_variance_persistence__mutmut_105,
    "x_plot_variance_persistence__mutmut_106": x_plot_variance_persistence__mutmut_106,
    "x_plot_variance_persistence__mutmut_107": x_plot_variance_persistence__mutmut_107,
    "x_plot_variance_persistence__mutmut_108": x_plot_variance_persistence__mutmut_108,
    "x_plot_variance_persistence__mutmut_109": x_plot_variance_persistence__mutmut_109,
    "x_plot_variance_persistence__mutmut_110": x_plot_variance_persistence__mutmut_110,
    "x_plot_variance_persistence__mutmut_111": x_plot_variance_persistence__mutmut_111,
    "x_plot_variance_persistence__mutmut_112": x_plot_variance_persistence__mutmut_112,
    "x_plot_variance_persistence__mutmut_113": x_plot_variance_persistence__mutmut_113,
    "x_plot_variance_persistence__mutmut_114": x_plot_variance_persistence__mutmut_114,
    "x_plot_variance_persistence__mutmut_115": x_plot_variance_persistence__mutmut_115,
    "x_plot_variance_persistence__mutmut_116": x_plot_variance_persistence__mutmut_116,
    "x_plot_variance_persistence__mutmut_117": x_plot_variance_persistence__mutmut_117,
    "x_plot_variance_persistence__mutmut_118": x_plot_variance_persistence__mutmut_118,
    "x_plot_variance_persistence__mutmut_119": x_plot_variance_persistence__mutmut_119,
    "x_plot_variance_persistence__mutmut_120": x_plot_variance_persistence__mutmut_120,
    "x_plot_variance_persistence__mutmut_121": x_plot_variance_persistence__mutmut_121,
    "x_plot_variance_persistence__mutmut_122": x_plot_variance_persistence__mutmut_122,
    "x_plot_variance_persistence__mutmut_123": x_plot_variance_persistence__mutmut_123,
    "x_plot_variance_persistence__mutmut_124": x_plot_variance_persistence__mutmut_124,
    "x_plot_variance_persistence__mutmut_125": x_plot_variance_persistence__mutmut_125,
    "x_plot_variance_persistence__mutmut_126": x_plot_variance_persistence__mutmut_126,
    "x_plot_variance_persistence__mutmut_127": x_plot_variance_persistence__mutmut_127,
    "x_plot_variance_persistence__mutmut_128": x_plot_variance_persistence__mutmut_128,
    "x_plot_variance_persistence__mutmut_129": x_plot_variance_persistence__mutmut_129,
    "x_plot_variance_persistence__mutmut_130": x_plot_variance_persistence__mutmut_130,
    "x_plot_variance_persistence__mutmut_131": x_plot_variance_persistence__mutmut_131,
    "x_plot_variance_persistence__mutmut_132": x_plot_variance_persistence__mutmut_132,
    "x_plot_variance_persistence__mutmut_133": x_plot_variance_persistence__mutmut_133,
    "x_plot_variance_persistence__mutmut_134": x_plot_variance_persistence__mutmut_134,
    "x_plot_variance_persistence__mutmut_135": x_plot_variance_persistence__mutmut_135,
    "x_plot_variance_persistence__mutmut_136": x_plot_variance_persistence__mutmut_136,
    "x_plot_variance_persistence__mutmut_137": x_plot_variance_persistence__mutmut_137,
    "x_plot_variance_persistence__mutmut_138": x_plot_variance_persistence__mutmut_138,
    "x_plot_variance_persistence__mutmut_139": x_plot_variance_persistence__mutmut_139,
    "x_plot_variance_persistence__mutmut_140": x_plot_variance_persistence__mutmut_140,
    "x_plot_variance_persistence__mutmut_141": x_plot_variance_persistence__mutmut_141,
    "x_plot_variance_persistence__mutmut_142": x_plot_variance_persistence__mutmut_142,
    "x_plot_variance_persistence__mutmut_143": x_plot_variance_persistence__mutmut_143,
    "x_plot_variance_persistence__mutmut_144": x_plot_variance_persistence__mutmut_144,
    "x_plot_variance_persistence__mutmut_145": x_plot_variance_persistence__mutmut_145,
    "x_plot_variance_persistence__mutmut_146": x_plot_variance_persistence__mutmut_146,
    "x_plot_variance_persistence__mutmut_147": x_plot_variance_persistence__mutmut_147,
    "x_plot_variance_persistence__mutmut_148": x_plot_variance_persistence__mutmut_148,
    "x_plot_variance_persistence__mutmut_149": x_plot_variance_persistence__mutmut_149,
    "x_plot_variance_persistence__mutmut_150": x_plot_variance_persistence__mutmut_150,
    "x_plot_variance_persistence__mutmut_151": x_plot_variance_persistence__mutmut_151,
    "x_plot_variance_persistence__mutmut_152": x_plot_variance_persistence__mutmut_152,
    "x_plot_variance_persistence__mutmut_153": x_plot_variance_persistence__mutmut_153,
    "x_plot_variance_persistence__mutmut_154": x_plot_variance_persistence__mutmut_154,
    "x_plot_variance_persistence__mutmut_155": x_plot_variance_persistence__mutmut_155,
    "x_plot_variance_persistence__mutmut_156": x_plot_variance_persistence__mutmut_156,
    "x_plot_variance_persistence__mutmut_157": x_plot_variance_persistence__mutmut_157,
    "x_plot_variance_persistence__mutmut_158": x_plot_variance_persistence__mutmut_158,
    "x_plot_variance_persistence__mutmut_159": x_plot_variance_persistence__mutmut_159,
    "x_plot_variance_persistence__mutmut_160": x_plot_variance_persistence__mutmut_160,
    "x_plot_variance_persistence__mutmut_161": x_plot_variance_persistence__mutmut_161,
    "x_plot_variance_persistence__mutmut_162": x_plot_variance_persistence__mutmut_162,
    "x_plot_variance_persistence__mutmut_163": x_plot_variance_persistence__mutmut_163,
    "x_plot_variance_persistence__mutmut_164": x_plot_variance_persistence__mutmut_164,
    "x_plot_variance_persistence__mutmut_165": x_plot_variance_persistence__mutmut_165,
    "x_plot_variance_persistence__mutmut_166": x_plot_variance_persistence__mutmut_166,
    "x_plot_variance_persistence__mutmut_167": x_plot_variance_persistence__mutmut_167,
    "x_plot_variance_persistence__mutmut_168": x_plot_variance_persistence__mutmut_168,
    "x_plot_variance_persistence__mutmut_169": x_plot_variance_persistence__mutmut_169,
    "x_plot_variance_persistence__mutmut_170": x_plot_variance_persistence__mutmut_170,
    "x_plot_variance_persistence__mutmut_171": x_plot_variance_persistence__mutmut_171,
    "x_plot_variance_persistence__mutmut_172": x_plot_variance_persistence__mutmut_172,
    "x_plot_variance_persistence__mutmut_173": x_plot_variance_persistence__mutmut_173,
    "x_plot_variance_persistence__mutmut_174": x_plot_variance_persistence__mutmut_174,
    "x_plot_variance_persistence__mutmut_175": x_plot_variance_persistence__mutmut_175,
    "x_plot_variance_persistence__mutmut_176": x_plot_variance_persistence__mutmut_176,
    "x_plot_variance_persistence__mutmut_177": x_plot_variance_persistence__mutmut_177,
    "x_plot_variance_persistence__mutmut_178": x_plot_variance_persistence__mutmut_178,
    "x_plot_variance_persistence__mutmut_179": x_plot_variance_persistence__mutmut_179,
    "x_plot_variance_persistence__mutmut_180": x_plot_variance_persistence__mutmut_180,
    "x_plot_variance_persistence__mutmut_181": x_plot_variance_persistence__mutmut_181,
    "x_plot_variance_persistence__mutmut_182": x_plot_variance_persistence__mutmut_182,
    "x_plot_variance_persistence__mutmut_183": x_plot_variance_persistence__mutmut_183,
    "x_plot_variance_persistence__mutmut_184": x_plot_variance_persistence__mutmut_184,
    "x_plot_variance_persistence__mutmut_185": x_plot_variance_persistence__mutmut_185,
    "x_plot_variance_persistence__mutmut_186": x_plot_variance_persistence__mutmut_186,
    "x_plot_variance_persistence__mutmut_187": x_plot_variance_persistence__mutmut_187,
    "x_plot_variance_persistence__mutmut_188": x_plot_variance_persistence__mutmut_188,
    "x_plot_variance_persistence__mutmut_189": x_plot_variance_persistence__mutmut_189,
    "x_plot_variance_persistence__mutmut_190": x_plot_variance_persistence__mutmut_190,
    "x_plot_variance_persistence__mutmut_191": x_plot_variance_persistence__mutmut_191,
    "x_plot_variance_persistence__mutmut_192": x_plot_variance_persistence__mutmut_192,
    "x_plot_variance_persistence__mutmut_193": x_plot_variance_persistence__mutmut_193,
    "x_plot_variance_persistence__mutmut_194": x_plot_variance_persistence__mutmut_194,
    "x_plot_variance_persistence__mutmut_195": x_plot_variance_persistence__mutmut_195,
    "x_plot_variance_persistence__mutmut_196": x_plot_variance_persistence__mutmut_196,
    "x_plot_variance_persistence__mutmut_197": x_plot_variance_persistence__mutmut_197,
    "x_plot_variance_persistence__mutmut_198": x_plot_variance_persistence__mutmut_198,
    "x_plot_variance_persistence__mutmut_199": x_plot_variance_persistence__mutmut_199,
    "x_plot_variance_persistence__mutmut_200": x_plot_variance_persistence__mutmut_200,
    "x_plot_variance_persistence__mutmut_201": x_plot_variance_persistence__mutmut_201,
    "x_plot_variance_persistence__mutmut_202": x_plot_variance_persistence__mutmut_202,
    "x_plot_variance_persistence__mutmut_203": x_plot_variance_persistence__mutmut_203,
    "x_plot_variance_persistence__mutmut_204": x_plot_variance_persistence__mutmut_204,
    "x_plot_variance_persistence__mutmut_205": x_plot_variance_persistence__mutmut_205,
    "x_plot_variance_persistence__mutmut_206": x_plot_variance_persistence__mutmut_206,
    "x_plot_variance_persistence__mutmut_207": x_plot_variance_persistence__mutmut_207,
    "x_plot_variance_persistence__mutmut_208": x_plot_variance_persistence__mutmut_208,
    "x_plot_variance_persistence__mutmut_209": x_plot_variance_persistence__mutmut_209,
    "x_plot_variance_persistence__mutmut_210": x_plot_variance_persistence__mutmut_210,
    "x_plot_variance_persistence__mutmut_211": x_plot_variance_persistence__mutmut_211,
    "x_plot_variance_persistence__mutmut_212": x_plot_variance_persistence__mutmut_212,
    "x_plot_variance_persistence__mutmut_213": x_plot_variance_persistence__mutmut_213,
    "x_plot_variance_persistence__mutmut_214": x_plot_variance_persistence__mutmut_214,
    "x_plot_variance_persistence__mutmut_215": x_plot_variance_persistence__mutmut_215,
    "x_plot_variance_persistence__mutmut_216": x_plot_variance_persistence__mutmut_216,
    "x_plot_variance_persistence__mutmut_217": x_plot_variance_persistence__mutmut_217,
    "x_plot_variance_persistence__mutmut_218": x_plot_variance_persistence__mutmut_218,
    "x_plot_variance_persistence__mutmut_219": x_plot_variance_persistence__mutmut_219,
    "x_plot_variance_persistence__mutmut_220": x_plot_variance_persistence__mutmut_220,
    "x_plot_variance_persistence__mutmut_221": x_plot_variance_persistence__mutmut_221,
    "x_plot_variance_persistence__mutmut_222": x_plot_variance_persistence__mutmut_222,
    "x_plot_variance_persistence__mutmut_223": x_plot_variance_persistence__mutmut_223,
    "x_plot_variance_persistence__mutmut_224": x_plot_variance_persistence__mutmut_224,
    "x_plot_variance_persistence__mutmut_225": x_plot_variance_persistence__mutmut_225,
    "x_plot_variance_persistence__mutmut_226": x_plot_variance_persistence__mutmut_226,
    "x_plot_variance_persistence__mutmut_227": x_plot_variance_persistence__mutmut_227,
    "x_plot_variance_persistence__mutmut_228": x_plot_variance_persistence__mutmut_228,
    "x_plot_variance_persistence__mutmut_229": x_plot_variance_persistence__mutmut_229,
    "x_plot_variance_persistence__mutmut_230": x_plot_variance_persistence__mutmut_230,
    "x_plot_variance_persistence__mutmut_231": x_plot_variance_persistence__mutmut_231,
    "x_plot_variance_persistence__mutmut_232": x_plot_variance_persistence__mutmut_232,
    "x_plot_variance_persistence__mutmut_233": x_plot_variance_persistence__mutmut_233,
    "x_plot_variance_persistence__mutmut_234": x_plot_variance_persistence__mutmut_234,
    "x_plot_variance_persistence__mutmut_235": x_plot_variance_persistence__mutmut_235,
    "x_plot_variance_persistence__mutmut_236": x_plot_variance_persistence__mutmut_236,
}
x_plot_variance_persistence__mutmut_orig.__name__ = "x_plot_variance_persistence"
