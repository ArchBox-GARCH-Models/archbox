"""Transition function visualization for STAR models.

Provides plots for Smooth Transition Autoregressive (STAR) models:
- Transition function G(s) for LSTAR/ESTAR
- Phase diagrams y_t vs y_{t-1} colored by regime
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Annotated, Any, ClassVar

import matplotlib.pyplot as plt
import numpy as np
from matplotlib.figure import Figure
from numpy.typing import NDArray

from archbox.visualization.themes import Theme, get_theme

MutantDict = Annotated[dict[str, Callable], "Mutant"]  # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg=None):  # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os  # type: ignore

    mutant_under_test = os.environ["MUTANT_UNDER_TEST"]  # type: ignore
    if mutant_under_test == "fail":  # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException  # type: ignore

        raise MutmutProgrammaticFailException("Failed programmatically")  # type: ignore
    elif mutant_under_test == "stats":  # type: ignore
        from mutmut.__main__ import record_trampoline_hit  # type: ignore

        record_trampoline_hit(orig.__module__ + "." + orig.__name__)  # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    prefix = orig.__module__ + "." + orig.__name__ + "__mutmut_"  # type: ignore
    if not mutant_under_test.startswith(prefix):  # type: ignore
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    mutant_name = mutant_under_test.rpartition(".")[-1]  # type: ignore
    if self_arg is not None:  # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)  # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)  # type: ignore
    return result  # type: ignore


def plot_transition_function(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    args = [results, gamma_values, n_points, theme, figsize, title]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_plot_transition_function__mutmut_orig,
        x_plot_transition_function__mutmut_mutants,
        args,
        kwargs,
        None,
    )


def x_plot_transition_function__mutmut_orig(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_1(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 301,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_2(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "XXprofessionalXX",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_3(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "PROFESSIONAL",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_4(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = None

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_5(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(None)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_6(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = None
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_7(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(None):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_8(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = None

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_9(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize and theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_10(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = None
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_11(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(None, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_12(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=None)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_13(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_14(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(
            results.transition_variable,
        )
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_15(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = None
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_16(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(None)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_17(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = None
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_18(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(None)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_19(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = None

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_20(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(None, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_21(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, None, "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_22(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", None)

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_23(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = ("transition_type").LSTAR

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_24(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = results.LSTAR

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_25(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = results.transition_type

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_26(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "XXtransition_typeXX", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_27(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "TRANSITION_TYPE", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_28(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "XXLSTARXX")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_29(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "lstar")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_30(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = None
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_31(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(None, c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_32(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), None)
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_33(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_34(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(
            s_t.min(),
        )
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_35(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c + 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_36(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 / np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_37(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 4 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_38(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(None))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_39(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = None
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_40(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(None, c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_41(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), None)
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_42(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_43(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(
            s_t.max(),
        )
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_44(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c - 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_45(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 / np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_46(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 4 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_47(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(None))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_48(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = None

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_49(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(None, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_50(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, None, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_51(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, None)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_52(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_53(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_54(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(
            s_min,
            s_max,
        )

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_55(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = None

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_56(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(None, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_57(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, None, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_58(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=None)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_59(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_60(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_61(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(
            1,
            1,
        )

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_62(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(2, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_63(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 2, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_64(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = None
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_65(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(None, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_66(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, None, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_67(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, None, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_68(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, None)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_69(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_70(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_71(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_72(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(
            s_range,
            gamma,
            c,
        )
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_73(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            None,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_74(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            None,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_75(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=None,
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_76(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=None,
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_77(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=None,
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_78(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_79(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_80(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_81(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_82(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_83(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get(None, "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_84(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", None),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_85(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_86(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get(
                "primary",
            ),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_87(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("XXprimaryXX", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_88(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("PRIMARY", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_89(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "XX#1f4e79XX"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_90(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1F4E79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_91(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get(None, 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_92(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", None),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_93(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get(1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_94(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get(
                "primary",
            ),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_95(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("XXprimaryXX", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_96(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("PRIMARY", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_97(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 2.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_98(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = None
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_99(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get(None, "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_100(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", None),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_101(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_102(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get(
                    "accent",
                ),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_103(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("XXaccentXX", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_104(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("ACCENT", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_105(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "XX#c00000XX"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_106(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#C00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_107(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get(None, "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_108(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", None),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_109(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_110(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get(
                    "secondary",
                ),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_111(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("XXsecondaryXX", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_112(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("SECONDARY", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_113(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "XX#2e75b6XX"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_114(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2E75B6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_115(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get(None, "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_116(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", None),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_117(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_118(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get(
                    "positive",
                ),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_119(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("XXpositiveXX", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_120(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("POSITIVE", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_121(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "XX#548235XX"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_122(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(None):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_123(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = None
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_124(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(None, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_125(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, None, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_126(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, None, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_127(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, None)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_128(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_129(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_130(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_131(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(
                    s_range,
                    g,
                    c,
                )
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_132(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = None
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_133(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx / len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_134(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    None,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_135(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    None,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_136(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=None,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_137(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=None,
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_138(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle=None,
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_139(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=None,
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_140(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_141(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_142(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_143(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_144(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_145(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_146(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get(None, 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_147(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", None),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_148(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get(1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_149(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get(
                        "secondary",
                    ),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_150(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("XXsecondaryXX", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_151(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("SECONDARY", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_152(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 2.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_153(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="XX--XX",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_154(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = None
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_155(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(None, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_156(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, None, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_157(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, None, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_158(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, None)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_159(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_160(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_161(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_162(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(
            s_t,
            gamma,
            c,
        )
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_163(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            None, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_164(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, None, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_165(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(s_t, g_observed, color=None, s=5, alpha=0.3, zorder=0)

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_166(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t,
            g_observed,
            color=theme.colors.get("returns", "#7f7f7f"),
            s=None,
            alpha=0.3,
            zorder=0,
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_167(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=None, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_168(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t,
            g_observed,
            color=theme.colors.get("returns", "#7f7f7f"),
            s=5,
            alpha=0.3,
            zorder=None,
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_169(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_170(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(s_t, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0)

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_171(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(s_t, g_observed, s=5, alpha=0.3, zorder=0)

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_172(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_173(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, zorder=0)

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_174(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t,
            g_observed,
            color=theme.colors.get("returns", "#7f7f7f"),
            s=5,
            alpha=0.3,
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_175(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get(None, "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_176(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", None), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_177(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(s_t, g_observed, color=theme.colors.get("#7f7f7f"), s=5, alpha=0.3, zorder=0)

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_178(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t,
            g_observed,
            color=theme.colors.get(
                "returns",
            ),
            s=5,
            alpha=0.3,
            zorder=0,
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_179(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t,
            g_observed,
            color=theme.colors.get("XXreturnsXX", "#7f7f7f"),
            s=5,
            alpha=0.3,
            zorder=0,
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_180(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("RETURNS", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_181(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t,
            g_observed,
            color=theme.colors.get("returns", "XX#7f7f7fXX"),
            s=5,
            alpha=0.3,
            zorder=0,
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_182(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7F7F7F"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_183(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=6, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_184(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=1.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_185(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=1
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_186(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=None, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_187(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=None, linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_188(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=None, linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_189(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=None)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_190(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_191(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_192(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_193(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(
            y=0.5,
            color=theme.colors.get("grid", "#cccccc"),
            linestyle=":",
        )
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_194(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=1.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_195(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get(None, "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_196(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", None), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_197(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_198(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(
            y=0.5,
            color=theme.colors.get(
                "grid",
            ),
            linestyle=":",
            linewidth=0.7,
        )
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_199(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(
            y=0.5, color=theme.colors.get("XXgridXX", "#cccccc"), linestyle=":", linewidth=0.7
        )
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_200(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("GRID", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_201(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(
            y=0.5, color=theme.colors.get("grid", "XX#ccccccXX"), linestyle=":", linewidth=0.7
        )
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_202(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#CCCCCC"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_203(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(
            y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle="XX:XX", linewidth=0.7
        )
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_204(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=1.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_205(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=None, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_206(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=None, linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_207(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=None, linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_208(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=None)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_209(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_210(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_211(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_212(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(
            x=c,
            color=theme.colors.get("grid", "#cccccc"),
            linestyle=":",
        )

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_213(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get(None, "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_214(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", None), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_215(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_216(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(
            x=c,
            color=theme.colors.get(
                "grid",
            ),
            linestyle=":",
            linewidth=0.7,
        )

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_217(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("XXgridXX", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_218(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("GRID", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_219(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "XX#ccccccXX"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_220(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#CCCCCC"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_221(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle="XX:XX", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_222(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=1.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_223(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(None)
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_224(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("XXTransition Variable (s)XX")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_225(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("transition variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_226(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("TRANSITION VARIABLE (S)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_227(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel(None)
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_228(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("XXG(s)XX")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_229(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("g(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_230(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(S)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_231(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(None, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_232(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, None)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_233(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_234(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(
            -0.05,
        )
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_235(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(+0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_236(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-1.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_237(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 2.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_238(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(None)
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_239(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title and f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_240(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc=None, framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_241(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=None)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_242(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_243(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(
            loc="lower right",
        )

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_244(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="XXlower rightXX", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_245(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="LOWER RIGHT", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_transition_function__mutmut_246(
    results: Any,
    gamma_values: list[float] | None = None,
    n_points: int = 300,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot transition function G(s) for LSTAR/ESTAR models.

    Parameters
    ----------
    results : STARResults
        Fitted STAR results. Must have:
        - transition_variable: (T,) array of s_t values
        - gamma: transition speed parameter
        - c: location parameter (threshold)
        - transition_type: 'LSTAR' or 'ESTAR'
    gamma_values : list of float, optional
        Additional gamma values to overlay for comparison.
    n_points : int
        Number of points for the smooth curve.
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        s_t = np.asarray(results.transition_variable, dtype=np.float64)
        gamma = float(results.gamma)
        c = float(results.c)
        trans_type = getattr(results, "transition_type", "LSTAR")

        # Create smooth s range
        s_min = min(s_t.min(), c - 3 * np.std(s_t))
        s_max = max(s_t.max(), c + 3 * np.std(s_t))
        s_range = np.linspace(s_min, s_max, n_points)

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        # Plot main transition function
        g_main = _compute_transition(s_range, gamma, c, trans_type)
        ax.plot(
            s_range,
            g_main,
            color=theme.colors.get("primary", "#1f4e79"),
            linewidth=theme.line_widths.get("primary", 1.5),
            label=f"G(s), \u03b3={gamma:.2f}",
        )

        # Plot for different gamma values
        if gamma_values:
            color_cycle = [
                theme.colors.get("accent", "#c00000"),
                theme.colors.get("secondary", "#2e75b6"),
                theme.colors.get("positive", "#548235"),
            ]
            for idx, g in enumerate(gamma_values):
                g_alt = _compute_transition(s_range, g, c, trans_type)
                color = color_cycle[idx % len(color_cycle)]
                ax.plot(
                    s_range,
                    g_alt,
                    color=color,
                    linewidth=theme.line_widths.get("secondary", 1.0),
                    linestyle="--",
                    label=f"\u03b3={g:.2f}",
                )

        # Plot observed transition variable values
        g_observed = _compute_transition(s_t, gamma, c, trans_type)
        ax.scatter(
            s_t, g_observed, color=theme.colors.get("returns", "#7f7f7f"), s=5, alpha=0.3, zorder=0
        )

        # Reference lines
        ax.axhline(y=0.5, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)
        ax.axvline(x=c, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel("Transition Variable (s)")
        ax.set_ylabel("G(s)")
        ax.set_ylim(-0.05, 1.05)
        ax.set_title(title or f"{trans_type} Transition Function")
        ax.legend(loc="lower right", framealpha=1.8)

        fig.tight_layout()
        return fig


x_plot_transition_function__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_plot_transition_function__mutmut_1": x_plot_transition_function__mutmut_1,
    "x_plot_transition_function__mutmut_2": x_plot_transition_function__mutmut_2,
    "x_plot_transition_function__mutmut_3": x_plot_transition_function__mutmut_3,
    "x_plot_transition_function__mutmut_4": x_plot_transition_function__mutmut_4,
    "x_plot_transition_function__mutmut_5": x_plot_transition_function__mutmut_5,
    "x_plot_transition_function__mutmut_6": x_plot_transition_function__mutmut_6,
    "x_plot_transition_function__mutmut_7": x_plot_transition_function__mutmut_7,
    "x_plot_transition_function__mutmut_8": x_plot_transition_function__mutmut_8,
    "x_plot_transition_function__mutmut_9": x_plot_transition_function__mutmut_9,
    "x_plot_transition_function__mutmut_10": x_plot_transition_function__mutmut_10,
    "x_plot_transition_function__mutmut_11": x_plot_transition_function__mutmut_11,
    "x_plot_transition_function__mutmut_12": x_plot_transition_function__mutmut_12,
    "x_plot_transition_function__mutmut_13": x_plot_transition_function__mutmut_13,
    "x_plot_transition_function__mutmut_14": x_plot_transition_function__mutmut_14,
    "x_plot_transition_function__mutmut_15": x_plot_transition_function__mutmut_15,
    "x_plot_transition_function__mutmut_16": x_plot_transition_function__mutmut_16,
    "x_plot_transition_function__mutmut_17": x_plot_transition_function__mutmut_17,
    "x_plot_transition_function__mutmut_18": x_plot_transition_function__mutmut_18,
    "x_plot_transition_function__mutmut_19": x_plot_transition_function__mutmut_19,
    "x_plot_transition_function__mutmut_20": x_plot_transition_function__mutmut_20,
    "x_plot_transition_function__mutmut_21": x_plot_transition_function__mutmut_21,
    "x_plot_transition_function__mutmut_22": x_plot_transition_function__mutmut_22,
    "x_plot_transition_function__mutmut_23": x_plot_transition_function__mutmut_23,
    "x_plot_transition_function__mutmut_24": x_plot_transition_function__mutmut_24,
    "x_plot_transition_function__mutmut_25": x_plot_transition_function__mutmut_25,
    "x_plot_transition_function__mutmut_26": x_plot_transition_function__mutmut_26,
    "x_plot_transition_function__mutmut_27": x_plot_transition_function__mutmut_27,
    "x_plot_transition_function__mutmut_28": x_plot_transition_function__mutmut_28,
    "x_plot_transition_function__mutmut_29": x_plot_transition_function__mutmut_29,
    "x_plot_transition_function__mutmut_30": x_plot_transition_function__mutmut_30,
    "x_plot_transition_function__mutmut_31": x_plot_transition_function__mutmut_31,
    "x_plot_transition_function__mutmut_32": x_plot_transition_function__mutmut_32,
    "x_plot_transition_function__mutmut_33": x_plot_transition_function__mutmut_33,
    "x_plot_transition_function__mutmut_34": x_plot_transition_function__mutmut_34,
    "x_plot_transition_function__mutmut_35": x_plot_transition_function__mutmut_35,
    "x_plot_transition_function__mutmut_36": x_plot_transition_function__mutmut_36,
    "x_plot_transition_function__mutmut_37": x_plot_transition_function__mutmut_37,
    "x_plot_transition_function__mutmut_38": x_plot_transition_function__mutmut_38,
    "x_plot_transition_function__mutmut_39": x_plot_transition_function__mutmut_39,
    "x_plot_transition_function__mutmut_40": x_plot_transition_function__mutmut_40,
    "x_plot_transition_function__mutmut_41": x_plot_transition_function__mutmut_41,
    "x_plot_transition_function__mutmut_42": x_plot_transition_function__mutmut_42,
    "x_plot_transition_function__mutmut_43": x_plot_transition_function__mutmut_43,
    "x_plot_transition_function__mutmut_44": x_plot_transition_function__mutmut_44,
    "x_plot_transition_function__mutmut_45": x_plot_transition_function__mutmut_45,
    "x_plot_transition_function__mutmut_46": x_plot_transition_function__mutmut_46,
    "x_plot_transition_function__mutmut_47": x_plot_transition_function__mutmut_47,
    "x_plot_transition_function__mutmut_48": x_plot_transition_function__mutmut_48,
    "x_plot_transition_function__mutmut_49": x_plot_transition_function__mutmut_49,
    "x_plot_transition_function__mutmut_50": x_plot_transition_function__mutmut_50,
    "x_plot_transition_function__mutmut_51": x_plot_transition_function__mutmut_51,
    "x_plot_transition_function__mutmut_52": x_plot_transition_function__mutmut_52,
    "x_plot_transition_function__mutmut_53": x_plot_transition_function__mutmut_53,
    "x_plot_transition_function__mutmut_54": x_plot_transition_function__mutmut_54,
    "x_plot_transition_function__mutmut_55": x_plot_transition_function__mutmut_55,
    "x_plot_transition_function__mutmut_56": x_plot_transition_function__mutmut_56,
    "x_plot_transition_function__mutmut_57": x_plot_transition_function__mutmut_57,
    "x_plot_transition_function__mutmut_58": x_plot_transition_function__mutmut_58,
    "x_plot_transition_function__mutmut_59": x_plot_transition_function__mutmut_59,
    "x_plot_transition_function__mutmut_60": x_plot_transition_function__mutmut_60,
    "x_plot_transition_function__mutmut_61": x_plot_transition_function__mutmut_61,
    "x_plot_transition_function__mutmut_62": x_plot_transition_function__mutmut_62,
    "x_plot_transition_function__mutmut_63": x_plot_transition_function__mutmut_63,
    "x_plot_transition_function__mutmut_64": x_plot_transition_function__mutmut_64,
    "x_plot_transition_function__mutmut_65": x_plot_transition_function__mutmut_65,
    "x_plot_transition_function__mutmut_66": x_plot_transition_function__mutmut_66,
    "x_plot_transition_function__mutmut_67": x_plot_transition_function__mutmut_67,
    "x_plot_transition_function__mutmut_68": x_plot_transition_function__mutmut_68,
    "x_plot_transition_function__mutmut_69": x_plot_transition_function__mutmut_69,
    "x_plot_transition_function__mutmut_70": x_plot_transition_function__mutmut_70,
    "x_plot_transition_function__mutmut_71": x_plot_transition_function__mutmut_71,
    "x_plot_transition_function__mutmut_72": x_plot_transition_function__mutmut_72,
    "x_plot_transition_function__mutmut_73": x_plot_transition_function__mutmut_73,
    "x_plot_transition_function__mutmut_74": x_plot_transition_function__mutmut_74,
    "x_plot_transition_function__mutmut_75": x_plot_transition_function__mutmut_75,
    "x_plot_transition_function__mutmut_76": x_plot_transition_function__mutmut_76,
    "x_plot_transition_function__mutmut_77": x_plot_transition_function__mutmut_77,
    "x_plot_transition_function__mutmut_78": x_plot_transition_function__mutmut_78,
    "x_plot_transition_function__mutmut_79": x_plot_transition_function__mutmut_79,
    "x_plot_transition_function__mutmut_80": x_plot_transition_function__mutmut_80,
    "x_plot_transition_function__mutmut_81": x_plot_transition_function__mutmut_81,
    "x_plot_transition_function__mutmut_82": x_plot_transition_function__mutmut_82,
    "x_plot_transition_function__mutmut_83": x_plot_transition_function__mutmut_83,
    "x_plot_transition_function__mutmut_84": x_plot_transition_function__mutmut_84,
    "x_plot_transition_function__mutmut_85": x_plot_transition_function__mutmut_85,
    "x_plot_transition_function__mutmut_86": x_plot_transition_function__mutmut_86,
    "x_plot_transition_function__mutmut_87": x_plot_transition_function__mutmut_87,
    "x_plot_transition_function__mutmut_88": x_plot_transition_function__mutmut_88,
    "x_plot_transition_function__mutmut_89": x_plot_transition_function__mutmut_89,
    "x_plot_transition_function__mutmut_90": x_plot_transition_function__mutmut_90,
    "x_plot_transition_function__mutmut_91": x_plot_transition_function__mutmut_91,
    "x_plot_transition_function__mutmut_92": x_plot_transition_function__mutmut_92,
    "x_plot_transition_function__mutmut_93": x_plot_transition_function__mutmut_93,
    "x_plot_transition_function__mutmut_94": x_plot_transition_function__mutmut_94,
    "x_plot_transition_function__mutmut_95": x_plot_transition_function__mutmut_95,
    "x_plot_transition_function__mutmut_96": x_plot_transition_function__mutmut_96,
    "x_plot_transition_function__mutmut_97": x_plot_transition_function__mutmut_97,
    "x_plot_transition_function__mutmut_98": x_plot_transition_function__mutmut_98,
    "x_plot_transition_function__mutmut_99": x_plot_transition_function__mutmut_99,
    "x_plot_transition_function__mutmut_100": x_plot_transition_function__mutmut_100,
    "x_plot_transition_function__mutmut_101": x_plot_transition_function__mutmut_101,
    "x_plot_transition_function__mutmut_102": x_plot_transition_function__mutmut_102,
    "x_plot_transition_function__mutmut_103": x_plot_transition_function__mutmut_103,
    "x_plot_transition_function__mutmut_104": x_plot_transition_function__mutmut_104,
    "x_plot_transition_function__mutmut_105": x_plot_transition_function__mutmut_105,
    "x_plot_transition_function__mutmut_106": x_plot_transition_function__mutmut_106,
    "x_plot_transition_function__mutmut_107": x_plot_transition_function__mutmut_107,
    "x_plot_transition_function__mutmut_108": x_plot_transition_function__mutmut_108,
    "x_plot_transition_function__mutmut_109": x_plot_transition_function__mutmut_109,
    "x_plot_transition_function__mutmut_110": x_plot_transition_function__mutmut_110,
    "x_plot_transition_function__mutmut_111": x_plot_transition_function__mutmut_111,
    "x_plot_transition_function__mutmut_112": x_plot_transition_function__mutmut_112,
    "x_plot_transition_function__mutmut_113": x_plot_transition_function__mutmut_113,
    "x_plot_transition_function__mutmut_114": x_plot_transition_function__mutmut_114,
    "x_plot_transition_function__mutmut_115": x_plot_transition_function__mutmut_115,
    "x_plot_transition_function__mutmut_116": x_plot_transition_function__mutmut_116,
    "x_plot_transition_function__mutmut_117": x_plot_transition_function__mutmut_117,
    "x_plot_transition_function__mutmut_118": x_plot_transition_function__mutmut_118,
    "x_plot_transition_function__mutmut_119": x_plot_transition_function__mutmut_119,
    "x_plot_transition_function__mutmut_120": x_plot_transition_function__mutmut_120,
    "x_plot_transition_function__mutmut_121": x_plot_transition_function__mutmut_121,
    "x_plot_transition_function__mutmut_122": x_plot_transition_function__mutmut_122,
    "x_plot_transition_function__mutmut_123": x_plot_transition_function__mutmut_123,
    "x_plot_transition_function__mutmut_124": x_plot_transition_function__mutmut_124,
    "x_plot_transition_function__mutmut_125": x_plot_transition_function__mutmut_125,
    "x_plot_transition_function__mutmut_126": x_plot_transition_function__mutmut_126,
    "x_plot_transition_function__mutmut_127": x_plot_transition_function__mutmut_127,
    "x_plot_transition_function__mutmut_128": x_plot_transition_function__mutmut_128,
    "x_plot_transition_function__mutmut_129": x_plot_transition_function__mutmut_129,
    "x_plot_transition_function__mutmut_130": x_plot_transition_function__mutmut_130,
    "x_plot_transition_function__mutmut_131": x_plot_transition_function__mutmut_131,
    "x_plot_transition_function__mutmut_132": x_plot_transition_function__mutmut_132,
    "x_plot_transition_function__mutmut_133": x_plot_transition_function__mutmut_133,
    "x_plot_transition_function__mutmut_134": x_plot_transition_function__mutmut_134,
    "x_plot_transition_function__mutmut_135": x_plot_transition_function__mutmut_135,
    "x_plot_transition_function__mutmut_136": x_plot_transition_function__mutmut_136,
    "x_plot_transition_function__mutmut_137": x_plot_transition_function__mutmut_137,
    "x_plot_transition_function__mutmut_138": x_plot_transition_function__mutmut_138,
    "x_plot_transition_function__mutmut_139": x_plot_transition_function__mutmut_139,
    "x_plot_transition_function__mutmut_140": x_plot_transition_function__mutmut_140,
    "x_plot_transition_function__mutmut_141": x_plot_transition_function__mutmut_141,
    "x_plot_transition_function__mutmut_142": x_plot_transition_function__mutmut_142,
    "x_plot_transition_function__mutmut_143": x_plot_transition_function__mutmut_143,
    "x_plot_transition_function__mutmut_144": x_plot_transition_function__mutmut_144,
    "x_plot_transition_function__mutmut_145": x_plot_transition_function__mutmut_145,
    "x_plot_transition_function__mutmut_146": x_plot_transition_function__mutmut_146,
    "x_plot_transition_function__mutmut_147": x_plot_transition_function__mutmut_147,
    "x_plot_transition_function__mutmut_148": x_plot_transition_function__mutmut_148,
    "x_plot_transition_function__mutmut_149": x_plot_transition_function__mutmut_149,
    "x_plot_transition_function__mutmut_150": x_plot_transition_function__mutmut_150,
    "x_plot_transition_function__mutmut_151": x_plot_transition_function__mutmut_151,
    "x_plot_transition_function__mutmut_152": x_plot_transition_function__mutmut_152,
    "x_plot_transition_function__mutmut_153": x_plot_transition_function__mutmut_153,
    "x_plot_transition_function__mutmut_154": x_plot_transition_function__mutmut_154,
    "x_plot_transition_function__mutmut_155": x_plot_transition_function__mutmut_155,
    "x_plot_transition_function__mutmut_156": x_plot_transition_function__mutmut_156,
    "x_plot_transition_function__mutmut_157": x_plot_transition_function__mutmut_157,
    "x_plot_transition_function__mutmut_158": x_plot_transition_function__mutmut_158,
    "x_plot_transition_function__mutmut_159": x_plot_transition_function__mutmut_159,
    "x_plot_transition_function__mutmut_160": x_plot_transition_function__mutmut_160,
    "x_plot_transition_function__mutmut_161": x_plot_transition_function__mutmut_161,
    "x_plot_transition_function__mutmut_162": x_plot_transition_function__mutmut_162,
    "x_plot_transition_function__mutmut_163": x_plot_transition_function__mutmut_163,
    "x_plot_transition_function__mutmut_164": x_plot_transition_function__mutmut_164,
    "x_plot_transition_function__mutmut_165": x_plot_transition_function__mutmut_165,
    "x_plot_transition_function__mutmut_166": x_plot_transition_function__mutmut_166,
    "x_plot_transition_function__mutmut_167": x_plot_transition_function__mutmut_167,
    "x_plot_transition_function__mutmut_168": x_plot_transition_function__mutmut_168,
    "x_plot_transition_function__mutmut_169": x_plot_transition_function__mutmut_169,
    "x_plot_transition_function__mutmut_170": x_plot_transition_function__mutmut_170,
    "x_plot_transition_function__mutmut_171": x_plot_transition_function__mutmut_171,
    "x_plot_transition_function__mutmut_172": x_plot_transition_function__mutmut_172,
    "x_plot_transition_function__mutmut_173": x_plot_transition_function__mutmut_173,
    "x_plot_transition_function__mutmut_174": x_plot_transition_function__mutmut_174,
    "x_plot_transition_function__mutmut_175": x_plot_transition_function__mutmut_175,
    "x_plot_transition_function__mutmut_176": x_plot_transition_function__mutmut_176,
    "x_plot_transition_function__mutmut_177": x_plot_transition_function__mutmut_177,
    "x_plot_transition_function__mutmut_178": x_plot_transition_function__mutmut_178,
    "x_plot_transition_function__mutmut_179": x_plot_transition_function__mutmut_179,
    "x_plot_transition_function__mutmut_180": x_plot_transition_function__mutmut_180,
    "x_plot_transition_function__mutmut_181": x_plot_transition_function__mutmut_181,
    "x_plot_transition_function__mutmut_182": x_plot_transition_function__mutmut_182,
    "x_plot_transition_function__mutmut_183": x_plot_transition_function__mutmut_183,
    "x_plot_transition_function__mutmut_184": x_plot_transition_function__mutmut_184,
    "x_plot_transition_function__mutmut_185": x_plot_transition_function__mutmut_185,
    "x_plot_transition_function__mutmut_186": x_plot_transition_function__mutmut_186,
    "x_plot_transition_function__mutmut_187": x_plot_transition_function__mutmut_187,
    "x_plot_transition_function__mutmut_188": x_plot_transition_function__mutmut_188,
    "x_plot_transition_function__mutmut_189": x_plot_transition_function__mutmut_189,
    "x_plot_transition_function__mutmut_190": x_plot_transition_function__mutmut_190,
    "x_plot_transition_function__mutmut_191": x_plot_transition_function__mutmut_191,
    "x_plot_transition_function__mutmut_192": x_plot_transition_function__mutmut_192,
    "x_plot_transition_function__mutmut_193": x_plot_transition_function__mutmut_193,
    "x_plot_transition_function__mutmut_194": x_plot_transition_function__mutmut_194,
    "x_plot_transition_function__mutmut_195": x_plot_transition_function__mutmut_195,
    "x_plot_transition_function__mutmut_196": x_plot_transition_function__mutmut_196,
    "x_plot_transition_function__mutmut_197": x_plot_transition_function__mutmut_197,
    "x_plot_transition_function__mutmut_198": x_plot_transition_function__mutmut_198,
    "x_plot_transition_function__mutmut_199": x_plot_transition_function__mutmut_199,
    "x_plot_transition_function__mutmut_200": x_plot_transition_function__mutmut_200,
    "x_plot_transition_function__mutmut_201": x_plot_transition_function__mutmut_201,
    "x_plot_transition_function__mutmut_202": x_plot_transition_function__mutmut_202,
    "x_plot_transition_function__mutmut_203": x_plot_transition_function__mutmut_203,
    "x_plot_transition_function__mutmut_204": x_plot_transition_function__mutmut_204,
    "x_plot_transition_function__mutmut_205": x_plot_transition_function__mutmut_205,
    "x_plot_transition_function__mutmut_206": x_plot_transition_function__mutmut_206,
    "x_plot_transition_function__mutmut_207": x_plot_transition_function__mutmut_207,
    "x_plot_transition_function__mutmut_208": x_plot_transition_function__mutmut_208,
    "x_plot_transition_function__mutmut_209": x_plot_transition_function__mutmut_209,
    "x_plot_transition_function__mutmut_210": x_plot_transition_function__mutmut_210,
    "x_plot_transition_function__mutmut_211": x_plot_transition_function__mutmut_211,
    "x_plot_transition_function__mutmut_212": x_plot_transition_function__mutmut_212,
    "x_plot_transition_function__mutmut_213": x_plot_transition_function__mutmut_213,
    "x_plot_transition_function__mutmut_214": x_plot_transition_function__mutmut_214,
    "x_plot_transition_function__mutmut_215": x_plot_transition_function__mutmut_215,
    "x_plot_transition_function__mutmut_216": x_plot_transition_function__mutmut_216,
    "x_plot_transition_function__mutmut_217": x_plot_transition_function__mutmut_217,
    "x_plot_transition_function__mutmut_218": x_plot_transition_function__mutmut_218,
    "x_plot_transition_function__mutmut_219": x_plot_transition_function__mutmut_219,
    "x_plot_transition_function__mutmut_220": x_plot_transition_function__mutmut_220,
    "x_plot_transition_function__mutmut_221": x_plot_transition_function__mutmut_221,
    "x_plot_transition_function__mutmut_222": x_plot_transition_function__mutmut_222,
    "x_plot_transition_function__mutmut_223": x_plot_transition_function__mutmut_223,
    "x_plot_transition_function__mutmut_224": x_plot_transition_function__mutmut_224,
    "x_plot_transition_function__mutmut_225": x_plot_transition_function__mutmut_225,
    "x_plot_transition_function__mutmut_226": x_plot_transition_function__mutmut_226,
    "x_plot_transition_function__mutmut_227": x_plot_transition_function__mutmut_227,
    "x_plot_transition_function__mutmut_228": x_plot_transition_function__mutmut_228,
    "x_plot_transition_function__mutmut_229": x_plot_transition_function__mutmut_229,
    "x_plot_transition_function__mutmut_230": x_plot_transition_function__mutmut_230,
    "x_plot_transition_function__mutmut_231": x_plot_transition_function__mutmut_231,
    "x_plot_transition_function__mutmut_232": x_plot_transition_function__mutmut_232,
    "x_plot_transition_function__mutmut_233": x_plot_transition_function__mutmut_233,
    "x_plot_transition_function__mutmut_234": x_plot_transition_function__mutmut_234,
    "x_plot_transition_function__mutmut_235": x_plot_transition_function__mutmut_235,
    "x_plot_transition_function__mutmut_236": x_plot_transition_function__mutmut_236,
    "x_plot_transition_function__mutmut_237": x_plot_transition_function__mutmut_237,
    "x_plot_transition_function__mutmut_238": x_plot_transition_function__mutmut_238,
    "x_plot_transition_function__mutmut_239": x_plot_transition_function__mutmut_239,
    "x_plot_transition_function__mutmut_240": x_plot_transition_function__mutmut_240,
    "x_plot_transition_function__mutmut_241": x_plot_transition_function__mutmut_241,
    "x_plot_transition_function__mutmut_242": x_plot_transition_function__mutmut_242,
    "x_plot_transition_function__mutmut_243": x_plot_transition_function__mutmut_243,
    "x_plot_transition_function__mutmut_244": x_plot_transition_function__mutmut_244,
    "x_plot_transition_function__mutmut_245": x_plot_transition_function__mutmut_245,
    "x_plot_transition_function__mutmut_246": x_plot_transition_function__mutmut_246,
}
x_plot_transition_function__mutmut_orig.__name__ = "x_plot_transition_function"


def plot_phase_diagram(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    args = [results, theme, figsize, title]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_plot_phase_diagram__mutmut_orig, x_plot_phase_diagram__mutmut_mutants, args, kwargs, None
    )


def x_plot_phase_diagram__mutmut_orig(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_1(
    results: Any,
    theme: str | Theme = "XXprofessionalXX",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_2(
    results: Any,
    theme: str | Theme = "PROFESSIONAL",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_3(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = None

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_4(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(None)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_5(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = None
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_6(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(None):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_7(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = None

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_8(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize and theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_9(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = None
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_10(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(None, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_11(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=None)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_12(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_13(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(
            results.series,
        )
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_14(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = None
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_15(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[2:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_16(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = None

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_17(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:+1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_18(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-2]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_19(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(None, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_20(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, None):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_21(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr("smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_22(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(
            results,
        ):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_23(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "XXsmoothed_probsXX"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_24(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "SMOOTHED_PROBS"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_25(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = None
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_26(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(None, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_27(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=None)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_28(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_29(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(
                results.smoothed_probs,
            )
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_30(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(None, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_31(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, None):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_32(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr("regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_33(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(
            results,
        ):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_34(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "XXregime_probsXX"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_35(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "REGIME_PROBS"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_36(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = None
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_37(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(None, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_38(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=None)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_39(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_40(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(
                results.regime_probs,
            )
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_41(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = None

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_42(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones(None)

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_43(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 2))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_44(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = None
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_45(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[2:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_46(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = None
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_47(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(None, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_48(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=None)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_49(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_50(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(
            probs,
        )
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_51(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=2)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_52(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = None

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_53(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[2]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_54(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = None

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_55(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "XX#2e75b6XX",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_56(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2E75B6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_57(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "XX#c00000XX",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_58(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#C00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_59(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "XX#548235XX",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_60(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "XX#ffc000XX",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_61(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#FFC000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_62(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = None

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_63(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(None, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_64(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, None, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_65(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=None)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_66(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_67(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_68(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(
            1,
            1,
        )

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_69(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(2, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_70(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 2, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_71(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(None):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_72(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = None
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_73(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class != k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_74(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                None,
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_75(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                None,
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_76(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=None,
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_77(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=None,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_78(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=None,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_79(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=None,
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_80(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors=None,
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_81(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_82(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_83(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_84(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_85(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_86(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_87(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_88(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k / len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_89(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=16,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_90(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=1.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_91(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k - 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_92(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 2}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_93(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="XXnoneXX",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_94(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="NONE",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_95(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(None) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_96(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) >= 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_97(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 3:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_98(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = None
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_99(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(None, y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_100(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], None, 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_101(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], None)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_102(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_103(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_104(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(
                    y_tm1[mask],
                    y_t[mask],
                )
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_105(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 2)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_106(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = None
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_107(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(None, y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_108(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), None, 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_109(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), None)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_110(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_111(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_112(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(
                    y_tm1[mask].min(),
                    y_tm1[mask].max(),
                )
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_113(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 51)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_114(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = None
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_115(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(None, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_116(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, None)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_117(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_118(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(
                    coeffs,
                )
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_119(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    None,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_120(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    None,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_121(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=None,
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_122(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=None,
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_123(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle=None,
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_124(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=None,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_125(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_126(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_127(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_128(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_129(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_130(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_131(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k / len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_132(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get(None, 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_133(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", None),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_134(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get(1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_135(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get(
                        "primary",
                    ),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_136(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("XXprimaryXX", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_137(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("PRIMARY", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_138(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 2.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_139(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="XX--XX",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_140(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=1.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_141(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = None
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_142(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(None, y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_143(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), None), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_144(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_145(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [
            min(
                y_tm1.min(),
            ),
            max(y_tm1.max(), y_t.max()),
        ]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_146(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(None, y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_147(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), None)]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_148(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_149(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [
            min(y_tm1.min(), y_t.min()),
            max(
                y_tm1.max(),
            ),
        ]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_150(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(None, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_151(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, None, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_152(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=None, linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_153(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(
            lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=None, linewidth=0.7
        )

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_154(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(
            lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=None
        )

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_155(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_156(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_157(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_158(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_159(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(
            lims,
            lims,
            color=theme.colors.get("grid", "#cccccc"),
            linestyle=":",
        )

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_160(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get(None, "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_161(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", None), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_162(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_163(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(
            lims,
            lims,
            color=theme.colors.get(
                "grid",
            ),
            linestyle=":",
            linewidth=0.7,
        )

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_164(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(
            lims, lims, color=theme.colors.get("XXgridXX", "#cccccc"), linestyle=":", linewidth=0.7
        )

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_165(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("GRID", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_166(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(
            lims, lims, color=theme.colors.get("grid", "XX#ccccccXX"), linestyle=":", linewidth=0.7
        )

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_167(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#CCCCCC"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_168(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(
            lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle="XX:XX", linewidth=0.7
        )

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_169(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=1.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_170(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(None)
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_171(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"XX$y_{t-1}$XX")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_172(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$Y_{T-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_173(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(None)
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_174(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"XX$y_t$XX")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_175(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$Y_T$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_176(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(None)
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_177(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title and "Phase Diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_178(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "XXPhase DiagramXX")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_179(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "phase diagram")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_180(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "PHASE DIAGRAM")
        ax.legend(loc="upper left", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_181(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc=None, framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_182(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=None)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_183(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_184(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(
            loc="upper left",
        )

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_185(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="XXupper leftXX", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_186(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="UPPER LEFT", framealpha=0.8)

        fig.tight_layout()
        return fig


def x_plot_phase_diagram__mutmut_187(
    results: Any,
    theme: str | Theme = "professional",
    figsize: tuple[float, float] | None = None,
    title: str | None = None,
) -> Figure:
    """Plot phase diagram y_t vs y_{t-1} colored by regime.

    Parameters
    ----------
    results : STARResults or RegimeResults
        Fitted results with:
        - series: (T,) original series
        - regime_probs or smoothed_probs: (T, K) probabilities
    theme : str or Theme
        Visual theme.
    figsize : tuple, optional
        Figure size override.
    title : str, optional
        Custom title.

    Returns
    -------
    matplotlib.figure.Figure
        The generated figure.
    """
    if isinstance(theme, str):
        theme = get_theme(theme)

    rc_params = theme.to_matplotlib_rcparams()
    with plt.rc_context(rc_params):
        fig_size = figsize or theme.figure_size

        series = np.asarray(results.series, dtype=np.float64)
        y_t = series[1:]
        y_tm1 = series[:-1]

        # Get regime assignment
        if hasattr(results, "smoothed_probs"):
            probs = np.asarray(results.smoothed_probs, dtype=np.float64)
        elif hasattr(results, "regime_probs"):
            probs = np.asarray(results.regime_probs, dtype=np.float64)
        else:
            # Fallback: single regime
            probs = np.ones((len(series), 1))

        # Use probabilities from t=1 onward (matching y_t)
        probs = probs[1:]
        regime_class = np.argmax(probs, axis=1)
        n_regimes = probs.shape[1]

        regime_colors = [
            "#2e75b6",  # Blue
            "#c00000",  # Red
            "#548235",  # Green
            "#ffc000",  # Yellow
        ]

        fig, ax = plt.subplots(1, 1, figsize=fig_size)

        for k in range(n_regimes):
            mask = regime_class == k
            ax.scatter(
                y_tm1[mask],
                y_t[mask],
                color=regime_colors[k % len(regime_colors)],
                s=15,
                alpha=0.5,
                label=f"Regime {k + 1}",
                edgecolors="none",
            )

            # Regression line per regime
            if np.sum(mask) > 2:
                coeffs = np.polyfit(y_tm1[mask], y_t[mask], 1)
                x_fit = np.linspace(y_tm1[mask].min(), y_tm1[mask].max(), 50)
                y_fit = np.polyval(coeffs, x_fit)
                ax.plot(
                    x_fit,
                    y_fit,
                    color=regime_colors[k % len(regime_colors)],
                    linewidth=theme.line_widths.get("primary", 1.5),
                    linestyle="--",
                    alpha=0.8,
                )

        # 45-degree line
        lims = [min(y_tm1.min(), y_t.min()), max(y_tm1.max(), y_t.max())]
        ax.plot(lims, lims, color=theme.colors.get("grid", "#cccccc"), linestyle=":", linewidth=0.7)

        ax.set_xlabel(r"$y_{t-1}$")
        ax.set_ylabel(r"$y_t$")
        ax.set_title(title or "Phase Diagram")
        ax.legend(loc="upper left", framealpha=1.8)

        fig.tight_layout()
        return fig


x_plot_phase_diagram__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_plot_phase_diagram__mutmut_1": x_plot_phase_diagram__mutmut_1,
    "x_plot_phase_diagram__mutmut_2": x_plot_phase_diagram__mutmut_2,
    "x_plot_phase_diagram__mutmut_3": x_plot_phase_diagram__mutmut_3,
    "x_plot_phase_diagram__mutmut_4": x_plot_phase_diagram__mutmut_4,
    "x_plot_phase_diagram__mutmut_5": x_plot_phase_diagram__mutmut_5,
    "x_plot_phase_diagram__mutmut_6": x_plot_phase_diagram__mutmut_6,
    "x_plot_phase_diagram__mutmut_7": x_plot_phase_diagram__mutmut_7,
    "x_plot_phase_diagram__mutmut_8": x_plot_phase_diagram__mutmut_8,
    "x_plot_phase_diagram__mutmut_9": x_plot_phase_diagram__mutmut_9,
    "x_plot_phase_diagram__mutmut_10": x_plot_phase_diagram__mutmut_10,
    "x_plot_phase_diagram__mutmut_11": x_plot_phase_diagram__mutmut_11,
    "x_plot_phase_diagram__mutmut_12": x_plot_phase_diagram__mutmut_12,
    "x_plot_phase_diagram__mutmut_13": x_plot_phase_diagram__mutmut_13,
    "x_plot_phase_diagram__mutmut_14": x_plot_phase_diagram__mutmut_14,
    "x_plot_phase_diagram__mutmut_15": x_plot_phase_diagram__mutmut_15,
    "x_plot_phase_diagram__mutmut_16": x_plot_phase_diagram__mutmut_16,
    "x_plot_phase_diagram__mutmut_17": x_plot_phase_diagram__mutmut_17,
    "x_plot_phase_diagram__mutmut_18": x_plot_phase_diagram__mutmut_18,
    "x_plot_phase_diagram__mutmut_19": x_plot_phase_diagram__mutmut_19,
    "x_plot_phase_diagram__mutmut_20": x_plot_phase_diagram__mutmut_20,
    "x_plot_phase_diagram__mutmut_21": x_plot_phase_diagram__mutmut_21,
    "x_plot_phase_diagram__mutmut_22": x_plot_phase_diagram__mutmut_22,
    "x_plot_phase_diagram__mutmut_23": x_plot_phase_diagram__mutmut_23,
    "x_plot_phase_diagram__mutmut_24": x_plot_phase_diagram__mutmut_24,
    "x_plot_phase_diagram__mutmut_25": x_plot_phase_diagram__mutmut_25,
    "x_plot_phase_diagram__mutmut_26": x_plot_phase_diagram__mutmut_26,
    "x_plot_phase_diagram__mutmut_27": x_plot_phase_diagram__mutmut_27,
    "x_plot_phase_diagram__mutmut_28": x_plot_phase_diagram__mutmut_28,
    "x_plot_phase_diagram__mutmut_29": x_plot_phase_diagram__mutmut_29,
    "x_plot_phase_diagram__mutmut_30": x_plot_phase_diagram__mutmut_30,
    "x_plot_phase_diagram__mutmut_31": x_plot_phase_diagram__mutmut_31,
    "x_plot_phase_diagram__mutmut_32": x_plot_phase_diagram__mutmut_32,
    "x_plot_phase_diagram__mutmut_33": x_plot_phase_diagram__mutmut_33,
    "x_plot_phase_diagram__mutmut_34": x_plot_phase_diagram__mutmut_34,
    "x_plot_phase_diagram__mutmut_35": x_plot_phase_diagram__mutmut_35,
    "x_plot_phase_diagram__mutmut_36": x_plot_phase_diagram__mutmut_36,
    "x_plot_phase_diagram__mutmut_37": x_plot_phase_diagram__mutmut_37,
    "x_plot_phase_diagram__mutmut_38": x_plot_phase_diagram__mutmut_38,
    "x_plot_phase_diagram__mutmut_39": x_plot_phase_diagram__mutmut_39,
    "x_plot_phase_diagram__mutmut_40": x_plot_phase_diagram__mutmut_40,
    "x_plot_phase_diagram__mutmut_41": x_plot_phase_diagram__mutmut_41,
    "x_plot_phase_diagram__mutmut_42": x_plot_phase_diagram__mutmut_42,
    "x_plot_phase_diagram__mutmut_43": x_plot_phase_diagram__mutmut_43,
    "x_plot_phase_diagram__mutmut_44": x_plot_phase_diagram__mutmut_44,
    "x_plot_phase_diagram__mutmut_45": x_plot_phase_diagram__mutmut_45,
    "x_plot_phase_diagram__mutmut_46": x_plot_phase_diagram__mutmut_46,
    "x_plot_phase_diagram__mutmut_47": x_plot_phase_diagram__mutmut_47,
    "x_plot_phase_diagram__mutmut_48": x_plot_phase_diagram__mutmut_48,
    "x_plot_phase_diagram__mutmut_49": x_plot_phase_diagram__mutmut_49,
    "x_plot_phase_diagram__mutmut_50": x_plot_phase_diagram__mutmut_50,
    "x_plot_phase_diagram__mutmut_51": x_plot_phase_diagram__mutmut_51,
    "x_plot_phase_diagram__mutmut_52": x_plot_phase_diagram__mutmut_52,
    "x_plot_phase_diagram__mutmut_53": x_plot_phase_diagram__mutmut_53,
    "x_plot_phase_diagram__mutmut_54": x_plot_phase_diagram__mutmut_54,
    "x_plot_phase_diagram__mutmut_55": x_plot_phase_diagram__mutmut_55,
    "x_plot_phase_diagram__mutmut_56": x_plot_phase_diagram__mutmut_56,
    "x_plot_phase_diagram__mutmut_57": x_plot_phase_diagram__mutmut_57,
    "x_plot_phase_diagram__mutmut_58": x_plot_phase_diagram__mutmut_58,
    "x_plot_phase_diagram__mutmut_59": x_plot_phase_diagram__mutmut_59,
    "x_plot_phase_diagram__mutmut_60": x_plot_phase_diagram__mutmut_60,
    "x_plot_phase_diagram__mutmut_61": x_plot_phase_diagram__mutmut_61,
    "x_plot_phase_diagram__mutmut_62": x_plot_phase_diagram__mutmut_62,
    "x_plot_phase_diagram__mutmut_63": x_plot_phase_diagram__mutmut_63,
    "x_plot_phase_diagram__mutmut_64": x_plot_phase_diagram__mutmut_64,
    "x_plot_phase_diagram__mutmut_65": x_plot_phase_diagram__mutmut_65,
    "x_plot_phase_diagram__mutmut_66": x_plot_phase_diagram__mutmut_66,
    "x_plot_phase_diagram__mutmut_67": x_plot_phase_diagram__mutmut_67,
    "x_plot_phase_diagram__mutmut_68": x_plot_phase_diagram__mutmut_68,
    "x_plot_phase_diagram__mutmut_69": x_plot_phase_diagram__mutmut_69,
    "x_plot_phase_diagram__mutmut_70": x_plot_phase_diagram__mutmut_70,
    "x_plot_phase_diagram__mutmut_71": x_plot_phase_diagram__mutmut_71,
    "x_plot_phase_diagram__mutmut_72": x_plot_phase_diagram__mutmut_72,
    "x_plot_phase_diagram__mutmut_73": x_plot_phase_diagram__mutmut_73,
    "x_plot_phase_diagram__mutmut_74": x_plot_phase_diagram__mutmut_74,
    "x_plot_phase_diagram__mutmut_75": x_plot_phase_diagram__mutmut_75,
    "x_plot_phase_diagram__mutmut_76": x_plot_phase_diagram__mutmut_76,
    "x_plot_phase_diagram__mutmut_77": x_plot_phase_diagram__mutmut_77,
    "x_plot_phase_diagram__mutmut_78": x_plot_phase_diagram__mutmut_78,
    "x_plot_phase_diagram__mutmut_79": x_plot_phase_diagram__mutmut_79,
    "x_plot_phase_diagram__mutmut_80": x_plot_phase_diagram__mutmut_80,
    "x_plot_phase_diagram__mutmut_81": x_plot_phase_diagram__mutmut_81,
    "x_plot_phase_diagram__mutmut_82": x_plot_phase_diagram__mutmut_82,
    "x_plot_phase_diagram__mutmut_83": x_plot_phase_diagram__mutmut_83,
    "x_plot_phase_diagram__mutmut_84": x_plot_phase_diagram__mutmut_84,
    "x_plot_phase_diagram__mutmut_85": x_plot_phase_diagram__mutmut_85,
    "x_plot_phase_diagram__mutmut_86": x_plot_phase_diagram__mutmut_86,
    "x_plot_phase_diagram__mutmut_87": x_plot_phase_diagram__mutmut_87,
    "x_plot_phase_diagram__mutmut_88": x_plot_phase_diagram__mutmut_88,
    "x_plot_phase_diagram__mutmut_89": x_plot_phase_diagram__mutmut_89,
    "x_plot_phase_diagram__mutmut_90": x_plot_phase_diagram__mutmut_90,
    "x_plot_phase_diagram__mutmut_91": x_plot_phase_diagram__mutmut_91,
    "x_plot_phase_diagram__mutmut_92": x_plot_phase_diagram__mutmut_92,
    "x_plot_phase_diagram__mutmut_93": x_plot_phase_diagram__mutmut_93,
    "x_plot_phase_diagram__mutmut_94": x_plot_phase_diagram__mutmut_94,
    "x_plot_phase_diagram__mutmut_95": x_plot_phase_diagram__mutmut_95,
    "x_plot_phase_diagram__mutmut_96": x_plot_phase_diagram__mutmut_96,
    "x_plot_phase_diagram__mutmut_97": x_plot_phase_diagram__mutmut_97,
    "x_plot_phase_diagram__mutmut_98": x_plot_phase_diagram__mutmut_98,
    "x_plot_phase_diagram__mutmut_99": x_plot_phase_diagram__mutmut_99,
    "x_plot_phase_diagram__mutmut_100": x_plot_phase_diagram__mutmut_100,
    "x_plot_phase_diagram__mutmut_101": x_plot_phase_diagram__mutmut_101,
    "x_plot_phase_diagram__mutmut_102": x_plot_phase_diagram__mutmut_102,
    "x_plot_phase_diagram__mutmut_103": x_plot_phase_diagram__mutmut_103,
    "x_plot_phase_diagram__mutmut_104": x_plot_phase_diagram__mutmut_104,
    "x_plot_phase_diagram__mutmut_105": x_plot_phase_diagram__mutmut_105,
    "x_plot_phase_diagram__mutmut_106": x_plot_phase_diagram__mutmut_106,
    "x_plot_phase_diagram__mutmut_107": x_plot_phase_diagram__mutmut_107,
    "x_plot_phase_diagram__mutmut_108": x_plot_phase_diagram__mutmut_108,
    "x_plot_phase_diagram__mutmut_109": x_plot_phase_diagram__mutmut_109,
    "x_plot_phase_diagram__mutmut_110": x_plot_phase_diagram__mutmut_110,
    "x_plot_phase_diagram__mutmut_111": x_plot_phase_diagram__mutmut_111,
    "x_plot_phase_diagram__mutmut_112": x_plot_phase_diagram__mutmut_112,
    "x_plot_phase_diagram__mutmut_113": x_plot_phase_diagram__mutmut_113,
    "x_plot_phase_diagram__mutmut_114": x_plot_phase_diagram__mutmut_114,
    "x_plot_phase_diagram__mutmut_115": x_plot_phase_diagram__mutmut_115,
    "x_plot_phase_diagram__mutmut_116": x_plot_phase_diagram__mutmut_116,
    "x_plot_phase_diagram__mutmut_117": x_plot_phase_diagram__mutmut_117,
    "x_plot_phase_diagram__mutmut_118": x_plot_phase_diagram__mutmut_118,
    "x_plot_phase_diagram__mutmut_119": x_plot_phase_diagram__mutmut_119,
    "x_plot_phase_diagram__mutmut_120": x_plot_phase_diagram__mutmut_120,
    "x_plot_phase_diagram__mutmut_121": x_plot_phase_diagram__mutmut_121,
    "x_plot_phase_diagram__mutmut_122": x_plot_phase_diagram__mutmut_122,
    "x_plot_phase_diagram__mutmut_123": x_plot_phase_diagram__mutmut_123,
    "x_plot_phase_diagram__mutmut_124": x_plot_phase_diagram__mutmut_124,
    "x_plot_phase_diagram__mutmut_125": x_plot_phase_diagram__mutmut_125,
    "x_plot_phase_diagram__mutmut_126": x_plot_phase_diagram__mutmut_126,
    "x_plot_phase_diagram__mutmut_127": x_plot_phase_diagram__mutmut_127,
    "x_plot_phase_diagram__mutmut_128": x_plot_phase_diagram__mutmut_128,
    "x_plot_phase_diagram__mutmut_129": x_plot_phase_diagram__mutmut_129,
    "x_plot_phase_diagram__mutmut_130": x_plot_phase_diagram__mutmut_130,
    "x_plot_phase_diagram__mutmut_131": x_plot_phase_diagram__mutmut_131,
    "x_plot_phase_diagram__mutmut_132": x_plot_phase_diagram__mutmut_132,
    "x_plot_phase_diagram__mutmut_133": x_plot_phase_diagram__mutmut_133,
    "x_plot_phase_diagram__mutmut_134": x_plot_phase_diagram__mutmut_134,
    "x_plot_phase_diagram__mutmut_135": x_plot_phase_diagram__mutmut_135,
    "x_plot_phase_diagram__mutmut_136": x_plot_phase_diagram__mutmut_136,
    "x_plot_phase_diagram__mutmut_137": x_plot_phase_diagram__mutmut_137,
    "x_plot_phase_diagram__mutmut_138": x_plot_phase_diagram__mutmut_138,
    "x_plot_phase_diagram__mutmut_139": x_plot_phase_diagram__mutmut_139,
    "x_plot_phase_diagram__mutmut_140": x_plot_phase_diagram__mutmut_140,
    "x_plot_phase_diagram__mutmut_141": x_plot_phase_diagram__mutmut_141,
    "x_plot_phase_diagram__mutmut_142": x_plot_phase_diagram__mutmut_142,
    "x_plot_phase_diagram__mutmut_143": x_plot_phase_diagram__mutmut_143,
    "x_plot_phase_diagram__mutmut_144": x_plot_phase_diagram__mutmut_144,
    "x_plot_phase_diagram__mutmut_145": x_plot_phase_diagram__mutmut_145,
    "x_plot_phase_diagram__mutmut_146": x_plot_phase_diagram__mutmut_146,
    "x_plot_phase_diagram__mutmut_147": x_plot_phase_diagram__mutmut_147,
    "x_plot_phase_diagram__mutmut_148": x_plot_phase_diagram__mutmut_148,
    "x_plot_phase_diagram__mutmut_149": x_plot_phase_diagram__mutmut_149,
    "x_plot_phase_diagram__mutmut_150": x_plot_phase_diagram__mutmut_150,
    "x_plot_phase_diagram__mutmut_151": x_plot_phase_diagram__mutmut_151,
    "x_plot_phase_diagram__mutmut_152": x_plot_phase_diagram__mutmut_152,
    "x_plot_phase_diagram__mutmut_153": x_plot_phase_diagram__mutmut_153,
    "x_plot_phase_diagram__mutmut_154": x_plot_phase_diagram__mutmut_154,
    "x_plot_phase_diagram__mutmut_155": x_plot_phase_diagram__mutmut_155,
    "x_plot_phase_diagram__mutmut_156": x_plot_phase_diagram__mutmut_156,
    "x_plot_phase_diagram__mutmut_157": x_plot_phase_diagram__mutmut_157,
    "x_plot_phase_diagram__mutmut_158": x_plot_phase_diagram__mutmut_158,
    "x_plot_phase_diagram__mutmut_159": x_plot_phase_diagram__mutmut_159,
    "x_plot_phase_diagram__mutmut_160": x_plot_phase_diagram__mutmut_160,
    "x_plot_phase_diagram__mutmut_161": x_plot_phase_diagram__mutmut_161,
    "x_plot_phase_diagram__mutmut_162": x_plot_phase_diagram__mutmut_162,
    "x_plot_phase_diagram__mutmut_163": x_plot_phase_diagram__mutmut_163,
    "x_plot_phase_diagram__mutmut_164": x_plot_phase_diagram__mutmut_164,
    "x_plot_phase_diagram__mutmut_165": x_plot_phase_diagram__mutmut_165,
    "x_plot_phase_diagram__mutmut_166": x_plot_phase_diagram__mutmut_166,
    "x_plot_phase_diagram__mutmut_167": x_plot_phase_diagram__mutmut_167,
    "x_plot_phase_diagram__mutmut_168": x_plot_phase_diagram__mutmut_168,
    "x_plot_phase_diagram__mutmut_169": x_plot_phase_diagram__mutmut_169,
    "x_plot_phase_diagram__mutmut_170": x_plot_phase_diagram__mutmut_170,
    "x_plot_phase_diagram__mutmut_171": x_plot_phase_diagram__mutmut_171,
    "x_plot_phase_diagram__mutmut_172": x_plot_phase_diagram__mutmut_172,
    "x_plot_phase_diagram__mutmut_173": x_plot_phase_diagram__mutmut_173,
    "x_plot_phase_diagram__mutmut_174": x_plot_phase_diagram__mutmut_174,
    "x_plot_phase_diagram__mutmut_175": x_plot_phase_diagram__mutmut_175,
    "x_plot_phase_diagram__mutmut_176": x_plot_phase_diagram__mutmut_176,
    "x_plot_phase_diagram__mutmut_177": x_plot_phase_diagram__mutmut_177,
    "x_plot_phase_diagram__mutmut_178": x_plot_phase_diagram__mutmut_178,
    "x_plot_phase_diagram__mutmut_179": x_plot_phase_diagram__mutmut_179,
    "x_plot_phase_diagram__mutmut_180": x_plot_phase_diagram__mutmut_180,
    "x_plot_phase_diagram__mutmut_181": x_plot_phase_diagram__mutmut_181,
    "x_plot_phase_diagram__mutmut_182": x_plot_phase_diagram__mutmut_182,
    "x_plot_phase_diagram__mutmut_183": x_plot_phase_diagram__mutmut_183,
    "x_plot_phase_diagram__mutmut_184": x_plot_phase_diagram__mutmut_184,
    "x_plot_phase_diagram__mutmut_185": x_plot_phase_diagram__mutmut_185,
    "x_plot_phase_diagram__mutmut_186": x_plot_phase_diagram__mutmut_186,
    "x_plot_phase_diagram__mutmut_187": x_plot_phase_diagram__mutmut_187,
}
x_plot_phase_diagram__mutmut_orig.__name__ = "x_plot_phase_diagram"


def _compute_transition(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    args = [s, gamma, c, trans_type]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x__compute_transition__mutmut_orig,
        x__compute_transition__mutmut_mutants,
        args,
        kwargs,
        None,
    )


def x__compute_transition__mutmut_orig(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_1(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.lower() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_2(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() != "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_3(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "XXLSTARXX":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_4(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "lstar":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_5(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = None
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_6(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma / (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_7(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = +gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_8(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s + c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_9(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = None
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_10(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(None, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_11(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, None, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_12(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, None)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_13(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(-500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_14(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_15(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(
            arg,
            -500,
        )
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_16(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, +500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_17(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -501, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_18(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 501)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_19(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = None
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_20(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 * (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_21(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 2.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_22(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 - np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_23(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (2.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_24(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(None))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_25(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.lower() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_26(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() != "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_27(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "XXESTARXX":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_28(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "estar":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_29(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = None
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_30(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 + np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_31(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 2.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_32(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(None)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_33(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma / (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_34(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(+gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_35(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) * 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_36(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s + c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_37(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 3)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_38(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = None
        raise ValueError(msg)

    return result


def x__compute_transition__mutmut_39(
    s: NDArray[np.float64],
    gamma: float,
    c: float,
    trans_type: str,
) -> NDArray[np.float64]:
    """Compute transition function value.

    Parameters
    ----------
    s : ndarray
        Transition variable values.
    gamma : float
        Transition speed parameter.
    c : float
        Location parameter (threshold).
    trans_type : str
        'LSTAR' or 'ESTAR'.

    Returns
    -------
    ndarray
        Transition function values in [0, 1].
    """
    if trans_type.upper() == "LSTAR":
        # Logistic: G(s) = 1 / (1 + exp(-gamma * (s - c)))
        arg = -gamma * (s - c)
        # Clip to avoid overflow
        arg = np.clip(arg, -500, 500)
        result: NDArray[np.float64] = 1.0 / (1.0 + np.exp(arg))
    elif trans_type.upper() == "ESTAR":
        # Exponential: G(s) = 1 - exp(-gamma * (s - c)^2)
        result = 1.0 - np.exp(-gamma * (s - c) ** 2)
    else:
        msg = f"Unknown transition type: {trans_type}. Use 'LSTAR' or 'ESTAR'."
        raise ValueError(None)

    return result


x__compute_transition__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x__compute_transition__mutmut_1": x__compute_transition__mutmut_1,
    "x__compute_transition__mutmut_2": x__compute_transition__mutmut_2,
    "x__compute_transition__mutmut_3": x__compute_transition__mutmut_3,
    "x__compute_transition__mutmut_4": x__compute_transition__mutmut_4,
    "x__compute_transition__mutmut_5": x__compute_transition__mutmut_5,
    "x__compute_transition__mutmut_6": x__compute_transition__mutmut_6,
    "x__compute_transition__mutmut_7": x__compute_transition__mutmut_7,
    "x__compute_transition__mutmut_8": x__compute_transition__mutmut_8,
    "x__compute_transition__mutmut_9": x__compute_transition__mutmut_9,
    "x__compute_transition__mutmut_10": x__compute_transition__mutmut_10,
    "x__compute_transition__mutmut_11": x__compute_transition__mutmut_11,
    "x__compute_transition__mutmut_12": x__compute_transition__mutmut_12,
    "x__compute_transition__mutmut_13": x__compute_transition__mutmut_13,
    "x__compute_transition__mutmut_14": x__compute_transition__mutmut_14,
    "x__compute_transition__mutmut_15": x__compute_transition__mutmut_15,
    "x__compute_transition__mutmut_16": x__compute_transition__mutmut_16,
    "x__compute_transition__mutmut_17": x__compute_transition__mutmut_17,
    "x__compute_transition__mutmut_18": x__compute_transition__mutmut_18,
    "x__compute_transition__mutmut_19": x__compute_transition__mutmut_19,
    "x__compute_transition__mutmut_20": x__compute_transition__mutmut_20,
    "x__compute_transition__mutmut_21": x__compute_transition__mutmut_21,
    "x__compute_transition__mutmut_22": x__compute_transition__mutmut_22,
    "x__compute_transition__mutmut_23": x__compute_transition__mutmut_23,
    "x__compute_transition__mutmut_24": x__compute_transition__mutmut_24,
    "x__compute_transition__mutmut_25": x__compute_transition__mutmut_25,
    "x__compute_transition__mutmut_26": x__compute_transition__mutmut_26,
    "x__compute_transition__mutmut_27": x__compute_transition__mutmut_27,
    "x__compute_transition__mutmut_28": x__compute_transition__mutmut_28,
    "x__compute_transition__mutmut_29": x__compute_transition__mutmut_29,
    "x__compute_transition__mutmut_30": x__compute_transition__mutmut_30,
    "x__compute_transition__mutmut_31": x__compute_transition__mutmut_31,
    "x__compute_transition__mutmut_32": x__compute_transition__mutmut_32,
    "x__compute_transition__mutmut_33": x__compute_transition__mutmut_33,
    "x__compute_transition__mutmut_34": x__compute_transition__mutmut_34,
    "x__compute_transition__mutmut_35": x__compute_transition__mutmut_35,
    "x__compute_transition__mutmut_36": x__compute_transition__mutmut_36,
    "x__compute_transition__mutmut_37": x__compute_transition__mutmut_37,
    "x__compute_transition__mutmut_38": x__compute_transition__mutmut_38,
    "x__compute_transition__mutmut_39": x__compute_transition__mutmut_39,
}
x__compute_transition__mutmut_orig.__name__ = "x__compute_transition"
