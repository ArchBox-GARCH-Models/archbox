"""SETAR - Self-Exciting Threshold Autoregressive Model (Tong & Lim, 1980).

The SETAR model is a TAR where the threshold variable is the lagged
endogenous variable: s_t = y_{t-d}.

    y_t = phi^{(1)}'x_t * I(y_{t-d} <= c) + phi^{(2)}'x_t * I(y_{t-d} > c) + eps_t

Features:
- Automatic delay (d) selection via AIC/BIC
- Extension to 3 regimes with 2 thresholds

References
----------
- Tong, H. & Lim, K.S. (1980). Threshold Autoregression, Limit Cycles
  and Cyclical Data. JRSS-B, 42(3), 245-292.
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Annotated, Any, ClassVar

import numpy as np
from numpy.typing import NDArray

from archbox.threshold.base import ThresholdModel
from archbox.threshold.results import ThresholdResults

MutantDict = Annotated[dict[str, Callable], "Mutant"]  # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg=None):  # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os  # type: ignore

    mutant_under_test = os.environ["MUTANT_UNDER_TEST"]  # type: ignore
    if mutant_under_test == "fail":  # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException  # type: ignore

        raise MutmutProgrammaticFailException("Failed programmatically")  # type: ignore
    elif mutant_under_test == "stats":  # type: ignore
        from mutmut.__main__ import record_trampoline_hit  # type: ignore

        record_trampoline_hit(orig.__module__ + "." + orig.__name__)  # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    prefix = orig.__module__ + "." + orig.__name__ + "__mutmut_"  # type: ignore
    if not mutant_under_test.startswith(prefix):  # type: ignore
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    mutant_name = mutant_under_test.rpartition(".")[-1]  # type: ignore
    if self_arg is not None:  # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)  # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)  # type: ignore
    return result  # type: ignore


class SETAR(ThresholdModel):
    """Self-Exciting Threshold Autoregressive model (Tong & Lim, 1980).

    Parameters
    ----------
    endog : array-like
        Endogenous time series.
    order : int
        AR order p (default 1).
    delay : int | None
        Delay parameter d (default None = auto-select from 1..d_max).
    n_regimes : int
        Number of regimes: 2 or 3 (default 2).
    d_max : int
        Maximum delay to search when delay=None (default 6).
    grid_points : int
        Number of grid points for threshold search (default 300).
    ic : str
        Information criterion for delay selection: 'aic' or 'bic' (default 'aic').

    Examples
    --------
    >>> import numpy as np
    >>> from archbox.threshold.setar import SETAR
    >>> rng = np.random.default_rng(42)
    >>> n = 500
    >>> y = np.zeros(n)
    >>> for t in range(1, n):
    ...     if y[t-1] <= 0:
    ...         y[t] = 0.5 + 0.3 * y[t-1] + rng.standard_normal() * 0.5
    ...     else:
    ...         y[t] = -0.2 + 0.8 * y[t-1] + rng.standard_normal() * 0.5
    >>> model = SETAR(y, order=1, n_regimes=2)
    >>> results = model.fit()
    >>> print(f"Threshold: {results.threshold}")
    >>> print(f"Delay: {results.delay}")
    """

    model_name: str = "SETAR"

    def __init__(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        args = [endog, order, delay, n_regimes, d_max, grid_points, ic]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁSETARǁ__init____mutmut_orig"),
            object.__getattribute__(self, "xǁSETARǁ__init____mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁSETARǁ__init____mutmut_orig(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_1(
        self,
        endog: Any,
        order: int = 2,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_2(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 3,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_3(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 7,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_4(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 301,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_5(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "XXaicXX",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_6(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "AIC",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_7(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_8(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is not None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_9(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = None
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_10(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = None
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_11(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.upper()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_12(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_13(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("XXaicXX", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_14(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("AIC", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_15(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "XXbicXX"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_16(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "BIC"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_17(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = None
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_18(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(None)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_19(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = None
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_20(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_21(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 2
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_22(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(None, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_23(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=None, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_24(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=None, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_25(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=None)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_26(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_27(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_28(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, n_regimes=n_regimes)
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_29(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(
            endog,
            order=order,
            delay=effective_delay,
        )
        self.grid_points = grid_points

    def xǁSETARǁ__init____mutmut_30(
        self,
        endog: Any,
        order: int = 1,
        delay: int | None = None,
        n_regimes: int = 2,
        d_max: int = 6,
        grid_points: int = 300,
        ic: str = "aic",
    ) -> None:
        """Initialize SETAR model with threshold search configuration."""
        self._auto_delay = delay is None
        self._d_max = d_max
        self._ic = ic.lower()
        if self._ic not in ("aic", "bic"):
            msg = f"ic must be 'aic' or 'bic', got '{ic}'"
            raise ValueError(msg)

        effective_delay = delay if delay is not None else 1
        super().__init__(endog, order=order, delay=effective_delay, n_regimes=n_regimes)
        self.grid_points = None

    xǁSETARǁ__init____mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ__init____mutmut_1": xǁSETARǁ__init____mutmut_1,
        "xǁSETARǁ__init____mutmut_2": xǁSETARǁ__init____mutmut_2,
        "xǁSETARǁ__init____mutmut_3": xǁSETARǁ__init____mutmut_3,
        "xǁSETARǁ__init____mutmut_4": xǁSETARǁ__init____mutmut_4,
        "xǁSETARǁ__init____mutmut_5": xǁSETARǁ__init____mutmut_5,
        "xǁSETARǁ__init____mutmut_6": xǁSETARǁ__init____mutmut_6,
        "xǁSETARǁ__init____mutmut_7": xǁSETARǁ__init____mutmut_7,
        "xǁSETARǁ__init____mutmut_8": xǁSETARǁ__init____mutmut_8,
        "xǁSETARǁ__init____mutmut_9": xǁSETARǁ__init____mutmut_9,
        "xǁSETARǁ__init____mutmut_10": xǁSETARǁ__init____mutmut_10,
        "xǁSETARǁ__init____mutmut_11": xǁSETARǁ__init____mutmut_11,
        "xǁSETARǁ__init____mutmut_12": xǁSETARǁ__init____mutmut_12,
        "xǁSETARǁ__init____mutmut_13": xǁSETARǁ__init____mutmut_13,
        "xǁSETARǁ__init____mutmut_14": xǁSETARǁ__init____mutmut_14,
        "xǁSETARǁ__init____mutmut_15": xǁSETARǁ__init____mutmut_15,
        "xǁSETARǁ__init____mutmut_16": xǁSETARǁ__init____mutmut_16,
        "xǁSETARǁ__init____mutmut_17": xǁSETARǁ__init____mutmut_17,
        "xǁSETARǁ__init____mutmut_18": xǁSETARǁ__init____mutmut_18,
        "xǁSETARǁ__init____mutmut_19": xǁSETARǁ__init____mutmut_19,
        "xǁSETARǁ__init____mutmut_20": xǁSETARǁ__init____mutmut_20,
        "xǁSETARǁ__init____mutmut_21": xǁSETARǁ__init____mutmut_21,
        "xǁSETARǁ__init____mutmut_22": xǁSETARǁ__init____mutmut_22,
        "xǁSETARǁ__init____mutmut_23": xǁSETARǁ__init____mutmut_23,
        "xǁSETARǁ__init____mutmut_24": xǁSETARǁ__init____mutmut_24,
        "xǁSETARǁ__init____mutmut_25": xǁSETARǁ__init____mutmut_25,
        "xǁSETARǁ__init____mutmut_26": xǁSETARǁ__init____mutmut_26,
        "xǁSETARǁ__init____mutmut_27": xǁSETARǁ__init____mutmut_27,
        "xǁSETARǁ__init____mutmut_28": xǁSETARǁ__init____mutmut_28,
        "xǁSETARǁ__init____mutmut_29": xǁSETARǁ__init____mutmut_29,
        "xǁSETARǁ__init____mutmut_30": xǁSETARǁ__init____mutmut_30,
    }
    xǁSETARǁ__init____mutmut_orig.__name__ = "xǁSETARǁ__init__"

    def _transition_function(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        args = [s, params]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁSETARǁ_transition_function__mutmut_orig"),
            object.__getattribute__(self, "xǁSETARǁ_transition_function__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁSETARǁ_transition_function__mutmut_orig(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_1(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes != 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_2(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 3:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_3(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = None
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_4(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[1]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_5(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(None)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_6(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s >= c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_7(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = None
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_8(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[1], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_9(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[2]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_10(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = None
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_11(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(None)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_12(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = None
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_13(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) | (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_14(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s >= c1) & (s <= c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_15(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s < c2)] = 0.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_16(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 1.5
            g[s > c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_17(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = None
            return g

    def xǁSETARǁ_transition_function__mutmut_18(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s >= c2] = 1.0
            return g

    def xǁSETARǁ_transition_function__mutmut_19(
        self, s: NDArray[np.float64], params: NDArray[np.float64]
    ) -> NDArray[np.float64]:
        """Indicator transition: G(s) = I(s > c).

        For 3 regimes, returns values in {0, 0.5, 1}:
        - 0: regime 1 (s <= c1)
        - 0.5: regime 2 (c1 < s <= c2)
        - 1: regime 3 (s > c2)

        Parameters
        ----------
        s : ndarray
            Transition variable values.
        params : ndarray
            [c] for 2 regimes, [c1, c2] for 3 regimes.

        Returns
        -------
        ndarray
            Transition values.
        """
        if self.n_regimes == 2:
            c = params[0]
            return (s > c).astype(np.float64)
        else:
            c1, c2 = params[0], params[1]
            g = np.zeros_like(s)
            g[(s > c1) & (s <= c2)] = 0.5
            g[s > c2] = 2.0
            return g

    xǁSETARǁ_transition_function__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ_transition_function__mutmut_1": xǁSETARǁ_transition_function__mutmut_1,
        "xǁSETARǁ_transition_function__mutmut_2": xǁSETARǁ_transition_function__mutmut_2,
        "xǁSETARǁ_transition_function__mutmut_3": xǁSETARǁ_transition_function__mutmut_3,
        "xǁSETARǁ_transition_function__mutmut_4": xǁSETARǁ_transition_function__mutmut_4,
        "xǁSETARǁ_transition_function__mutmut_5": xǁSETARǁ_transition_function__mutmut_5,
        "xǁSETARǁ_transition_function__mutmut_6": xǁSETARǁ_transition_function__mutmut_6,
        "xǁSETARǁ_transition_function__mutmut_7": xǁSETARǁ_transition_function__mutmut_7,
        "xǁSETARǁ_transition_function__mutmut_8": xǁSETARǁ_transition_function__mutmut_8,
        "xǁSETARǁ_transition_function__mutmut_9": xǁSETARǁ_transition_function__mutmut_9,
        "xǁSETARǁ_transition_function__mutmut_10": xǁSETARǁ_transition_function__mutmut_10,
        "xǁSETARǁ_transition_function__mutmut_11": xǁSETARǁ_transition_function__mutmut_11,
        "xǁSETARǁ_transition_function__mutmut_12": xǁSETARǁ_transition_function__mutmut_12,
        "xǁSETARǁ_transition_function__mutmut_13": xǁSETARǁ_transition_function__mutmut_13,
        "xǁSETARǁ_transition_function__mutmut_14": xǁSETARǁ_transition_function__mutmut_14,
        "xǁSETARǁ_transition_function__mutmut_15": xǁSETARǁ_transition_function__mutmut_15,
        "xǁSETARǁ_transition_function__mutmut_16": xǁSETARǁ_transition_function__mutmut_16,
        "xǁSETARǁ_transition_function__mutmut_17": xǁSETARǁ_transition_function__mutmut_17,
        "xǁSETARǁ_transition_function__mutmut_18": xǁSETARǁ_transition_function__mutmut_18,
        "xǁSETARǁ_transition_function__mutmut_19": xǁSETARǁ_transition_function__mutmut_19,
    }
    xǁSETARǁ_transition_function__mutmut_orig.__name__ = "xǁSETARǁ_transition_function"

    @property
    def start_params(self) -> NDArray[np.float64]:
        """Initial parameters."""
        if self.n_regimes == 2:
            return np.array([np.median(self._s)])
        q1 = np.percentile(self._s, 33)
        q2 = np.percentile(self._s, 67)
        return np.array([q1, q2])

    @property
    def param_names(self) -> list[str]:
        """Parameter names."""
        names = ["delay"]
        if self.n_regimes == 2:
            names.append("c")
        else:
            names.extend(["c_1", "c_2"])
        for regime in range(1, self.n_regimes + 1):
            names.append(f"phi_0_regime{regime}")
            for lag in range(1, self.order + 1):
                names.append(f"phi_{lag}_regime{regime}")
        return names

    def _rebuild_for_delay(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        args = [d]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁSETARǁ_rebuild_for_delay__mutmut_orig"),
            object.__getattribute__(self, "xǁSETARǁ_rebuild_for_delay__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁSETARǁ_rebuild_for_delay__mutmut_orig(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_1(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = None
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_2(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = None
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_3(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(None, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_4(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, None)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_5(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_6(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(
            p,
        )
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_7(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = None

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_8(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs + start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_9(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = None
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_10(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = None
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_11(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones(None)
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_12(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p - 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_13(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 2))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_14(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(None, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_15(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, None):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_16(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_17(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(
            1,
        ):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_18(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(2, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_19(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p - 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_20(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 2):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_21(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = None

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_22(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start + lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_23(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs + lag]

        s = self.endog[start - d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_24(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = None
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_25(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start + d : self.nobs - d]
        return y, x_mat, s

    def xǁSETARǁ_rebuild_for_delay__mutmut_26(
        self, d: int
    ) -> tuple[NDArray[np.float64], NDArray[np.float64], NDArray[np.float64]]:
        """Rebuild matrices for a specific delay d.

        Parameters
        ----------
        d : int
            Delay parameter.

        Returns
        -------
        y, X, s : tuple of ndarrays
        """
        p = self.order
        start = max(p, d)
        t_eff = self.nobs - start

        y = self.endog[start:]
        x_mat = np.ones((t_eff, p + 1))
        for lag in range(1, p + 1):
            x_mat[:, lag] = self.endog[start - lag : self.nobs - lag]

        s = self.endog[start - d : self.nobs + d]
        return y, x_mat, s

    xǁSETARǁ_rebuild_for_delay__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ_rebuild_for_delay__mutmut_1": xǁSETARǁ_rebuild_for_delay__mutmut_1,
        "xǁSETARǁ_rebuild_for_delay__mutmut_2": xǁSETARǁ_rebuild_for_delay__mutmut_2,
        "xǁSETARǁ_rebuild_for_delay__mutmut_3": xǁSETARǁ_rebuild_for_delay__mutmut_3,
        "xǁSETARǁ_rebuild_for_delay__mutmut_4": xǁSETARǁ_rebuild_for_delay__mutmut_4,
        "xǁSETARǁ_rebuild_for_delay__mutmut_5": xǁSETARǁ_rebuild_for_delay__mutmut_5,
        "xǁSETARǁ_rebuild_for_delay__mutmut_6": xǁSETARǁ_rebuild_for_delay__mutmut_6,
        "xǁSETARǁ_rebuild_for_delay__mutmut_7": xǁSETARǁ_rebuild_for_delay__mutmut_7,
        "xǁSETARǁ_rebuild_for_delay__mutmut_8": xǁSETARǁ_rebuild_for_delay__mutmut_8,
        "xǁSETARǁ_rebuild_for_delay__mutmut_9": xǁSETARǁ_rebuild_for_delay__mutmut_9,
        "xǁSETARǁ_rebuild_for_delay__mutmut_10": xǁSETARǁ_rebuild_for_delay__mutmut_10,
        "xǁSETARǁ_rebuild_for_delay__mutmut_11": xǁSETARǁ_rebuild_for_delay__mutmut_11,
        "xǁSETARǁ_rebuild_for_delay__mutmut_12": xǁSETARǁ_rebuild_for_delay__mutmut_12,
        "xǁSETARǁ_rebuild_for_delay__mutmut_13": xǁSETARǁ_rebuild_for_delay__mutmut_13,
        "xǁSETARǁ_rebuild_for_delay__mutmut_14": xǁSETARǁ_rebuild_for_delay__mutmut_14,
        "xǁSETARǁ_rebuild_for_delay__mutmut_15": xǁSETARǁ_rebuild_for_delay__mutmut_15,
        "xǁSETARǁ_rebuild_for_delay__mutmut_16": xǁSETARǁ_rebuild_for_delay__mutmut_16,
        "xǁSETARǁ_rebuild_for_delay__mutmut_17": xǁSETARǁ_rebuild_for_delay__mutmut_17,
        "xǁSETARǁ_rebuild_for_delay__mutmut_18": xǁSETARǁ_rebuild_for_delay__mutmut_18,
        "xǁSETARǁ_rebuild_for_delay__mutmut_19": xǁSETARǁ_rebuild_for_delay__mutmut_19,
        "xǁSETARǁ_rebuild_for_delay__mutmut_20": xǁSETARǁ_rebuild_for_delay__mutmut_20,
        "xǁSETARǁ_rebuild_for_delay__mutmut_21": xǁSETARǁ_rebuild_for_delay__mutmut_21,
        "xǁSETARǁ_rebuild_for_delay__mutmut_22": xǁSETARǁ_rebuild_for_delay__mutmut_22,
        "xǁSETARǁ_rebuild_for_delay__mutmut_23": xǁSETARǁ_rebuild_for_delay__mutmut_23,
        "xǁSETARǁ_rebuild_for_delay__mutmut_24": xǁSETARǁ_rebuild_for_delay__mutmut_24,
        "xǁSETARǁ_rebuild_for_delay__mutmut_25": xǁSETARǁ_rebuild_for_delay__mutmut_25,
        "xǁSETARǁ_rebuild_for_delay__mutmut_26": xǁSETARǁ_rebuild_for_delay__mutmut_26,
    }
    xǁSETARǁ_rebuild_for_delay__mutmut_orig.__name__ = "xǁSETARǁ_rebuild_for_delay"

    def _fit_two_regimes(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        args = [y, x_mat, s]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁSETARǁ_fit_two_regimes__mutmut_orig"),
            object.__getattribute__(self, "xǁSETARǁ_fit_two_regimes__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁSETARǁ_fit_two_regimes__mutmut_orig(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_1(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = None
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_2(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(None)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_3(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = None
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_4(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = None
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_5(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(None)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_6(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 / n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_7(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(1.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_8(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = None
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_9(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(None)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_10(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 / n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_11(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(1.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_12(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo > hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_13(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = None
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_14(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 1
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_15(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = None
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_16(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n + 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_17(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 2
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_18(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = None

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_19(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(None, s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_20(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], None, self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_21(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], None)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_22(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_23(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_24(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(
            s_sorted[lo],
            s_sorted[hi],
        )

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_25(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = None
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_26(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order - 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_27(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 3
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_28(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = None
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_29(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = None

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_30(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(None)

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_31(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(None))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_32(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = None
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_33(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s < c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_34(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = None
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_35(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s >= c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_36(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs and mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_37(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() <= min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_38(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() <= min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_39(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                break
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_40(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = None
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_41(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) - self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_42(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(None, x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_43(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], None) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_44(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_45(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(
                y[mask1],
            ) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_46(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(None, x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_47(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], None)
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_48(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_49(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(
                y[mask2],
            )
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_50(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss <= best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_51(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = None
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_52(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = None

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_53(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(None)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_54(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = None
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_55(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s < best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_56(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = None
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_57(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s >= best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_58(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = None
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_59(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(None, x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_60(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], None)
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_61(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_62(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(
            y[mask1],
        )
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_63(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = None

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_64(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(None, x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_65(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], None)

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_66(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_67(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(
            y[mask2],
        )

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_68(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = None
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_69(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(None), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_70(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(None)
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_71(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = None
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_72(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 * n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_73(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 >= 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_74(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 1 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_75(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1.000001
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_76(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = None

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_77(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 * n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_78(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 >= 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_79(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 1 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_80(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1.000001

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_81(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = None
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_82(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 1.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_83(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 or sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_84(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 >= 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_85(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 1 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_86(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 >= 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_87(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 1:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_88(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll = -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_89(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll -= -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_90(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) + rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_91(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 / (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_92(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 / n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_93(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += +0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_94(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -1.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_95(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) - np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_96(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(None) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_97(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 / np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_98(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(3 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_99(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(None)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_100(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 * (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_101(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 / sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_102(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (3 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_103(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 or sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_104(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 >= 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_105(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 1 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_106(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 >= 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_107(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 1:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_108(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll = -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_109(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll -= -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_110(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) + rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_111(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 / (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_112(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 / n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_113(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += +0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_114(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -1.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_115(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) - np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_116(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(None) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_117(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 / np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_118(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(3 * np.pi) + np.log(sigma2_2)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_119(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(None)) - rss2 / (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_120(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 * (2 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_121(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (2 / sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    def xǁSETARǁ_fit_two_regimes__mutmut_122(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        float,
        float,
        NDArray[np.float64],
        NDArray[np.float64],
        float,
        float,
        float,
    ]:
        """Fit 2-regime SETAR for given y, X, s.

        Returns
        -------
        c, rss_total, beta1, beta2, sigma2_1, sigma2_2, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1
        grid = np.linspace(s_sorted[lo], s_sorted[hi], self.grid_points)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c = float(np.median(s))

        for c in grid:
            mask1 = s <= c
            mask2 = s > c
            if mask1.sum() < min_obs or mask2.sum() < min_obs:
                continue
            rss = self._ols_rss(y[mask1], x_mat[mask1]) + self._ols_rss(y[mask2], x_mat[mask2])
            if rss < best_rss:
                best_rss = rss
                best_c = float(c)

        mask1 = s <= best_c
        mask2 = s > best_c
        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])

        n1, n2 = int(mask1.sum()), int(mask2.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6

        ll = 0.0
        if n1 > 0 and sigma2_1 > 0:
            ll += -0.5 * n1 * (np.log(2 * np.pi) + np.log(sigma2_1)) - rss1 / (2 * sigma2_1)
        if n2 > 0 and sigma2_2 > 0:
            ll += -0.5 * n2 * (np.log(2 * np.pi) + np.log(sigma2_2)) - rss2 / (3 * sigma2_2)

        return best_c, best_rss, beta1, beta2, sigma2_1, sigma2_2, ll

    xǁSETARǁ_fit_two_regimes__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ_fit_two_regimes__mutmut_1": xǁSETARǁ_fit_two_regimes__mutmut_1,
        "xǁSETARǁ_fit_two_regimes__mutmut_2": xǁSETARǁ_fit_two_regimes__mutmut_2,
        "xǁSETARǁ_fit_two_regimes__mutmut_3": xǁSETARǁ_fit_two_regimes__mutmut_3,
        "xǁSETARǁ_fit_two_regimes__mutmut_4": xǁSETARǁ_fit_two_regimes__mutmut_4,
        "xǁSETARǁ_fit_two_regimes__mutmut_5": xǁSETARǁ_fit_two_regimes__mutmut_5,
        "xǁSETARǁ_fit_two_regimes__mutmut_6": xǁSETARǁ_fit_two_regimes__mutmut_6,
        "xǁSETARǁ_fit_two_regimes__mutmut_7": xǁSETARǁ_fit_two_regimes__mutmut_7,
        "xǁSETARǁ_fit_two_regimes__mutmut_8": xǁSETARǁ_fit_two_regimes__mutmut_8,
        "xǁSETARǁ_fit_two_regimes__mutmut_9": xǁSETARǁ_fit_two_regimes__mutmut_9,
        "xǁSETARǁ_fit_two_regimes__mutmut_10": xǁSETARǁ_fit_two_regimes__mutmut_10,
        "xǁSETARǁ_fit_two_regimes__mutmut_11": xǁSETARǁ_fit_two_regimes__mutmut_11,
        "xǁSETARǁ_fit_two_regimes__mutmut_12": xǁSETARǁ_fit_two_regimes__mutmut_12,
        "xǁSETARǁ_fit_two_regimes__mutmut_13": xǁSETARǁ_fit_two_regimes__mutmut_13,
        "xǁSETARǁ_fit_two_regimes__mutmut_14": xǁSETARǁ_fit_two_regimes__mutmut_14,
# type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁSETARǁ_fit_three_regimes__mutmut_orig"),
            object.__getattribute__(self, "xǁSETARǁ_fit_three_regimes__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_orig(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_1(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = None
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_2(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(None)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_3(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = None
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_4(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = None
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_5(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(None)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_6(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 / n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_7(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(1.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_8(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = None
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_9(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(None)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_10(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 / n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_11(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(1.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_12(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo > hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_13(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = None
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_14(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 1
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_15(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = None

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_16(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n + 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_17(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 2

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_18(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = None  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_19(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(None, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_20(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, None)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_21(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_22(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(
            self.grid_points,
        )  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_23(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 101)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_24(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = None

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_25(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(None, s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_26(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], None, grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_27(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], None)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_28(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_29(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_30(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(
            s_sorted[lo],
            s_sorted[hi],
        )

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_31(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = None
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_32(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order - 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_33(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 3
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_34(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = None
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_35(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = None
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_36(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(None)
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_37(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(None, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_38(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, None))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_39(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_40(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(
            np.percentile(
                s,
            )
        )
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_41(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 34))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_42(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = None

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_43(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(None)

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_44(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(None, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_45(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, None))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_46(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_47(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(
            np.percentile(
                s,
            )
        )

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_48(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 68))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_49(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(None):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_50(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i - 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_51(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 2 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_52(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = None
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_53(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s < c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_54(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = None
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_55(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) | (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_56(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s >= c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_57(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s < c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_58(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = None
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_59(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s >= c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_60(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs and mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_61(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs and mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_62(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() <= min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_63(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() <= min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_64(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() <= min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_65(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    break
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_66(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = None
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_67(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    - self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_68(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    - self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_69(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(None, x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_70(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], None)
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_71(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_72(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(
                        y[mask1],
                    )
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_73(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(None, x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_74(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], None)
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_75(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_76(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(
                        y[mask2],
                    )
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_77(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(None, x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_78(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], None)
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_79(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_80(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(
                        y[mask3],
                    )
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_81(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss <= best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_82(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = None
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_83(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = None

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_84(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(None), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_85(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(None)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_86(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = None
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_87(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s < best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_88(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = None
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_89(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) | (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_90(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s >= best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_91(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s < best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_92(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = None

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_93(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s >= best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_94(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = None
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_95(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(None, x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_96(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], None)
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_97(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_98(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(
            y[mask1],
        )
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_99(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = None
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_100(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(None, x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_101(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], None)
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_102(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_103(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(
            y[mask2],
        )
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_104(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = None

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_105(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(None, x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_106(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], None)

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_107(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_108(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(
            y[mask3],
        )

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_109(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = None
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_110(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(None), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_111(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(None), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_112(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(None)
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_113(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = None
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_114(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 * n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_115(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 >= 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_116(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 1 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_117(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1.000001
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_118(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = None
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_119(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 * n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_120(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 >= 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_121(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 1 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_122(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1.000001
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_123(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = None

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_124(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 * n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_125(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 >= 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_126(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 1 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_127(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1.000001

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_128(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = None
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_129(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 1.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_130(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 or s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_131(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni >= 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_132(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 1 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_133(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i >= 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_134(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 1:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_135(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll = -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_136(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll -= -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_137(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) + rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_138(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni / (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_139(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 / ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_140(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += +0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_141(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -1.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_142(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) - np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_143(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(None) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_144(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 / np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_145(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(3 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_146(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(None)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_147(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i * (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_148(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 / s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_149(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (3 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_150(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"XXregime_1XX": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_151(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"REGIME_1": sigma2_1, "regime_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_152(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "XXregime_2XX": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_153(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "REGIME_2": sigma2_2, "regime_3": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_154(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "XXregime_3XX": sigma2_3},
            ll,
        )

    def xǁSETARǁ_fit_three_regimes__mutmut_155(
        self,
        y: NDArray[np.float64],
        x_mat: NDArray[np.float64],
        s: NDArray[np.float64],
    ) -> tuple[
        list[float],
        NDArray[np.float64],
        NDArray[np.float64],
        NDArray[np.float64],
        dict[str, float],
        float,
    ]:
        """Fit 3-regime SETAR.

        Returns
        -------
        thresholds, beta1, beta2, beta3, sigma2_dict, loglike
        """
        s_sorted = np.sort(s)
        n = len(s)
        lo = int(0.15 * n)
        hi = int(0.85 * n)
        if lo >= hi:
            lo = 0
            hi = n - 1

        grid_n = min(self.grid_points, 100)  # coarser grid for 3 regimes
        grid = np.linspace(s_sorted[lo], s_sorted[hi], grid_n)

        min_obs = self.order + 2
        best_rss = np.inf
        best_c1 = float(np.percentile(s, 33))
        best_c2 = float(np.percentile(s, 67))

        for i, c1 in enumerate(grid):
            for c2 in grid[i + 1 :]:
                mask1 = s <= c1
                mask2 = (s > c1) & (s <= c2)
                mask3 = s > c2
                if mask1.sum() < min_obs or mask2.sum() < min_obs or mask3.sum() < min_obs:
                    continue
                rss = (
                    self._ols_rss(y[mask1], x_mat[mask1])
                    + self._ols_rss(y[mask2], x_mat[mask2])
                    + self._ols_rss(y[mask3], x_mat[mask3])
                )
                if rss < best_rss:
                    best_rss = rss
                    best_c1, best_c2 = float(c1), float(c2)

        mask1 = s <= best_c1
        mask2 = (s > best_c1) & (s <= best_c2)
        mask3 = s > best_c2

        beta1, _, rss1 = self._ols_fit(y[mask1], x_mat[mask1])
        beta2, _, rss2 = self._ols_fit(y[mask2], x_mat[mask2])
        beta3, _, rss3 = self._ols_fit(y[mask3], x_mat[mask3])

        n1, n2, n3 = int(mask1.sum()), int(mask2.sum()), int(mask3.sum())
        sigma2_1 = rss1 / n1 if n1 > 0 else 1e-6
        sigma2_2 = rss2 / n2 if n2 > 0 else 1e-6
        sigma2_3 = rss3 / n3 if n3 > 0 else 1e-6

        ll = 0.0
        for ni, rss_i, s2_i in [
            (n1, rss1, sigma2_1),
            (n2, rss2, sigma2_2),
            (n3, rss3, sigma2_3),
        ]:
            if ni > 0 and s2_i > 0:
                ll += -0.5 * ni * (np.log(2 * np.pi) + np.log(s2_i)) - rss_i / (2 * s2_i)

        return (
            [best_c1, best_c2],
            beta1,
            beta2,
            beta3,
            {"regime_1": sigma2_1, "regime_2": sigma2_2, "REGIME_3": sigma2_3},
            ll,
        )

    xǁSETARǁ_fit_three_regimes__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ_fit_three_regimes__mutmut_1": xǁSETARǁ_fit_three_regimes__mutmut_1,
        "xǁSETARǁ_fit_three_regimes__mutmut_2": xǁSETARǁ_fit_three_regimes__mutmut_2,
        "xǁSETARǁ_fit_three_regimes__mutmut_3": xǁSETARǁ_fit_three_regimes__mutmut_3,
        "xǁSETARǁ_fit_three_regimes__mutmut_4": xǁSETARǁ_fit_three_regimes__mutmut_4,
        "xǁSETARǁ_fit_three_regimes__mutmut_5": xǁSETARǁ_fit_three_regimes__mutmut_5,
        "xǁSETARǁ_fit_three_regimes__mutmut_6": xǁSETARǁ_fit_three_regimes__mutmut_6,
        "xǁSETARǁ_fit_three_regimes__mutmut_145": xǁSETARǁ_fit_three_regimes__mutmut_145,
        "xǁSETARǁ_fit_three_regimes__mutmut_146": xǁSETARǁ_fit_three_regimes__mutmut_146,
        "xǁSETARǁ_fit_three_regimes__mutmut_147": xǁSETARǁ_fit_three_regimes__mutmut_147,
        "xǁSETARǁ_fit_three_regimes__mutmut_148": xǁSETARǁ_fit_three_regimes__mutmut_148,
        "xǁSETARǁ_fit_three_regimes__mutmut_149": xǁSETARǁ_fit_three_regimes__mutmut_149,
        "xǁSETARǁ_fit_three_regimes__mutmut_150": xǁSETARǁ_fit_three_regimes__mutmut_150,
        "xǁSETARǁ_fit_three_regimes__mutmut_151": xǁSETARǁ_fit_three_regimes__mutmut_151,
        "xǁSETARǁ_fit_three_regimes__mutmut_152": xǁSETARǁ_fit_three_regimes__mutmut_152,
        "xǁSETARǁ_fit_three_regimes__mutmut_153": xǁSETARǁ_fit_three_regimes__mutmut_153,
        "xǁSETARǁ_fit_three_regimes__mutmut_154": xǁSETARǁ_fit_three_regimes__mutmut_154,
        "xǁSETARǁ_fit_three_regimes__mutmut_155": xǁSETARǁ_fit_three_regimes__mutmut_155,
    }
    xǁSETARǁ_fit_three_regimes__mutmut_orig.__name__ = "xǁSETARǁ_fit_three_regimes"

    def _fit_cls(self) -> ThresholdResults:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁSETARǁ_fit_cls__mutmut_orig"),
            object.__getattribute__(self, "xǁSETARǁ_fit_cls__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁSETARǁ_fit_cls__mutmut_orig(self) -> ThresholdResults:
        """Fit SETAR via Conditional Least Squares.

        Returns
        -------
        ThresholdResults
        """
        if self._auto_delay:
            return self._fit_with_delay_selection()

        if self.n_regimes == 2:
            return self._fit_2regime_fixed_delay()
        return self._fit_3regime_fixed_delay()

    def xǁSETARǁ_fit_cls__mutmut_1(self) -> ThresholdResults:
        """Fit SETAR via Conditional Least Squares.

        Returns
        -------
        ThresholdResults
        """
        if self._auto_delay:
            return self._fit_with_delay_selection()

        if self.n_regimes != 2:
            return self._fit_2regime_fixed_delay()
        return self._fit_3regime_fixed_delay()

    def xǁSETARǁ_fit_cls__mutmut_2(self) -> ThresholdResults:
        """Fit SETAR via Conditional Least Squares.

        Returns
        -------
        ThresholdResults
        """
        if self._auto_delay:
            return self._fit_with_delay_selection()

        if self.n_regimes == 3:
            return self._fit_2regime_fixed_delay()
        return self._fit_3regime_fixed_delay()

    xǁSETARǁ_fit_cls__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ_fit_cls__mutmut_1": xǁSETARǁ_fit_cls__mutmut_1,
        "xǁSETARǁ_fit_cls__mutmut_2": xǁSETARǁ_fit_cls__mutmut_2,
    }
    xǁSETARǁ_fit_cls__mutmut_orig.__name__ = "xǁSETARǁ_fit_cls"

    def _fit_with_delay_selection(self) -> ThresholdResults:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁSETARǁ_fit_with_delay_selection__mutmut_orig"),
            object.__getattribute__(self, "xǁSETARǁ_fit_with_delay_selection__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁSETARǁ_fit_with_delay_selection__mutmut_orig(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_1(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = None
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_2(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = None
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_3(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 2
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_4(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = ""

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_5(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(None, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_6(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, None):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_7(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_8(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(
            1,
        ):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_9(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(2, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_10(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max - 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_11(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 2):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_12(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = None
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_13(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(None)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_14(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                break

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_15(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) <= 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_16(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) - 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_17(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 / (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_18(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 3 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_19(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order - d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_20(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 11:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_21(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                break

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_22(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = None
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_23(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = None
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_24(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = None
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_25(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = None

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_26(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes != 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_27(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 3:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_28(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = None
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_29(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = None

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_30(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = None
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_31(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic != "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_32(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "XXaicXX" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_33(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "AIC" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_34(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val <= best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_35(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = None
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_36(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = None
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_37(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = None
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_38(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = None
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_39(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = None

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_40(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is not None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_41(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = None
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_42(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 2
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_43(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = None
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_44(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(None)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_45(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(2)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_46(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes != 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_47(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 3:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_48(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = None
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_49(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = None
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_50(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = None
            self._y, self._X, self._s = self._rebuild_for_delay(best_delay)

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_51(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = None

        return best_results

    def xǁSETARǁ_fit_with_delay_selection__mutmut_52(self) -> ThresholdResults:
        """Select optimal delay via AIC/BIC, then fit."""
        best_ic = np.inf
        best_delay = 1
        best_results: ThresholdResults | None = None

        for d in range(1, self._d_max + 1):
            try:
                y, x_mat, s = self._rebuild_for_delay(d)
            except Exception:  # noqa: BLE001
                continue

            if len(y) < 2 * (self.order + d) + 10:
                continue

            # Save and restore state
            old_delay = self.delay
            old_y, old_x, old_s = self._y, self._X, self._s
            self.delay = d
            self._y, self._X, self._s = y, x_mat, s

            try:
                if self.n_regimes == 2:
                    result = self._fit_2regime_fixed_delay()
                else:
                    result = self._fit_3regime_fixed_delay()

                ic_val = result.aic if self._ic == "aic" else result.bic
                if ic_val < best_ic:
                    best_ic = ic_val
                    best_delay = d
                    best_results = result
            except Exception:  # noqa: BLE001
                pass
            finally:
                self.delay = old_delay
                self._y, self._X, self._s = old_y, old_x, old_s

        if best_results is None:
            # Fallback to delay=1
            self.delay = 1
            self._y, self._X, self._s = self._rebuild_for_delay(1)
            if self.n_regimes == 2:
                best_results = self._fit_2regime_fixed_delay()
            else:
                best_results = self._fit_3regime_fixed_delay()
        else:
            # Set final delay
            self.delay = best_delay
            self._y, self._X, self._s = self._rebuild_for_delay(None)

        return best_results

    xǁSETARǁ_fit_with_delay_selection__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ_fit_with_delay_selection__mutmut_1": xǁSETARǁ_fit_with_delay_selection__mutmut_1,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_2": xǁSETARǁ_fit_with_delay_selection__mutmut_2,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_3": xǁSETARǁ_fit_with_delay_selection__mutmut_3,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_4": xǁSETARǁ_fit_with_delay_selection__mutmut_4,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_5": xǁSETARǁ_fit_with_delay_selection__mutmut_5,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_6": xǁSETARǁ_fit_with_delay_selection__mutmut_6,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_7": xǁSETARǁ_fit_with_delay_selection__mutmut_7,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_8": xǁSETARǁ_fit_with_delay_selection__mutmut_8,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_9": xǁSETARǁ_fit_with_delay_selection__mutmut_9,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_10": xǁSETARǁ_fit_with_delay_selection__mutmut_10,
        "xǁSETARǁ_fit_with_delay_selection__mutmut_11": xǁSETARǁ_fit_with_delay_selection__mutmut_11,
        "xǁSETARǁ_fit_with_delay_selection__mutmut


        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_6(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_7(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_8(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(
            y,
            x_mat,
        )

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_9(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = None
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_10(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s < best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_11(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = None
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_12(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s >= best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_13(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = None
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_14(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(None)
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_15(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = None
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_16(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] + x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_17(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = None

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_18(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] + x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_19(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = None
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_20(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(None)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_21(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = None
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_22(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = None  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_23(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 - 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_24(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 - 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_25(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) / 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_26(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order - 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_27(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 2) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_28(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 3 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_29(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 2 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_30(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 2  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_31(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = None
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_32(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll - 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_33(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 / ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_34(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = +2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_35(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -3 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_36(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 / n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_37(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 3 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_38(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = None

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_39(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll - np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_40(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 / ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_41(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = +2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_42(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -3 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_43(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) / n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_44(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(None) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_45(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=None,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_46(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params=None,
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_47(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=None,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_48(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=None,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_49(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params=None,
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_50(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=None,
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_51(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=None,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_52(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=None,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_53(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=None,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_54(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=None,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_55(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=None,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_56(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=None,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_57(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=None,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_58(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=None,
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_59(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=None,
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_60(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=None,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_61(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=None,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_62(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=None,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_63(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=None,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_64(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=None,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_65(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_66(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_67(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_68(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_69(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_70(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_71(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_72(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_73(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_74(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_75(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_76(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_77(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_78(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_79(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_80(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_81(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_82(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_83(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_84(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_85(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"XXregime_1XX": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_86(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"REGIME_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_87(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "XXregime_2XX": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_88(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "REGIME_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_89(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"XXcXX": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_90(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"C": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_91(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array(None),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_92(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"XXregime_1XX": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_93(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"REGIME_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_94(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "XXregime_2XX": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_95(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "REGIME_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_96(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(None),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_2regime_fixed_delay__mutmut_97(self) -> ThresholdResults:
        """Fit 2-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        best_c, _, beta1, beta2, sigma2_1, sigma2_2, ll = self._fit_two_regimes(y, x_mat, s)

        mask1 = s <= best_c
        mask2 = s > best_c
        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2

        g = mask2.astype(np.float64)
        t = len(y)
        n_params = (self.order + 1) * 2 + 1 + 1  # +1 for delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2},
            threshold=best_c,
            delay=self.delay,
            transition_params={"c": best_c},
            transition_params_array=np.array([best_c]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2={"regime_1": sigma2_1, "regime_2": sigma2_2},
            loglike=ll,
            aic=float(aic),
            bic=float(None),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    xǁSETARǁ_fit_2regime_fixed_delay__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_1": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_1,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_2": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_2,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_3": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_3,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_4": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_4,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_5": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_5,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_6": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_6,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_7": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_7,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_8": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_8,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_9": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_9,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_10": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_10,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_11": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_11,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_12": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_12,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_13": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_13,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_14": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_14,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_15": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_15,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_16": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_16,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_17": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_17,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_18": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_18,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_19": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_19,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_20": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_20,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_21": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_21,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_22": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_22,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_23": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_23,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_24": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_24,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_25": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_25,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_26": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_26,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_27": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_27,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_28": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_28,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_29": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_29,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_30": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_30,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_31": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_31,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_32": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_32,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_33": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_33,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_34": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_34,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_35": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_35,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_36": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_36,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_37": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_37,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_38": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_38,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_39": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_39,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_40": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_40,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_41": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_41,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_42": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_42,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_43": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_43,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_44": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_44,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_45": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_45,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_46": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_46,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_47": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_47,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_48": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_48,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_49": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_49,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_50": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_50,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_51": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_51,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_52": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_52,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_53": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_53,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_54": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_54,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_55": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_55,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_56": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_56,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_57": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_57,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_58": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_58,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_59": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_59,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_60": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_60,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_61": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_61,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_62": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_62,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_63": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_63,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_64": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_64,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_65": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_65,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_66": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_66,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_67": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_67,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_68": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_68,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_69": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_69,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_70": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_70,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_71": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_71,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_72": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_72,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_73": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_73,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_74": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_74,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_75": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_75,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_76": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_76,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_77": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_77,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_78": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_78,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_79": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_79,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_80": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_80,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_81": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_81,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_82": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_82,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_83": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_83,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_84": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_84,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_85": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_85,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_86": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_86,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_87": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_87,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_88": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_88,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_89": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_89,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_90": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_90,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_91": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_91,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_92": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_92,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_93": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_93,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_94": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_94,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_95": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_95,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_96": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_96,
        "xǁSETARǁ_fit_2regime_fixed_delay__mutmut_97": xǁSETARǁ_fit_2regime_fixed_delay__mutmut_97,
    }
    xǁSETARǁ_fit_2regime_fixed_delay__mutmut_orig.__name__ = "xǁSETARǁ_fit_2regime_fixed_delay"

    def _fit_3regime_fixed_delay(self) -> ThresholdResults:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_orig"),
            object.__getattribute__(self, "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_orig(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_1(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = None
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_2(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = None

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_3(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(None, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_4(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, None, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_5(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, None)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_6(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_7(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_8(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(
            y,
            x_mat,
        )

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_9(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = None
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_10(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = None
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_11(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s < c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_12(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = None
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_13(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) | (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_14(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s >= c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_15(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s < c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_16(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = None

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_17(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s >= c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_18(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = None
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_19(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(None)
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_20(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = None
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_21(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] + x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_22(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = None
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_23(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] + x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_24(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = None

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_25(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] + x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_26(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = None
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_27(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(None)
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_28(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = None
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_29(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 1.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_30(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = None

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_31(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 2.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_32(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = None
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_33(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = None  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_34(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 - 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_35(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 - 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_36(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) / 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_37(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order - 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_38(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 2) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_39(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 4 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_40(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 3 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_41(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 2  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_42(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = None
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_43(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll - 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_44(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 / ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_45(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = +2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_46(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -3 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_47(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 / n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_48(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 3 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_49(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = None

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_50(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll - np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_51(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 / ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_52(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = +2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_53(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -3 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_54(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) / n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_55(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(None) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_56(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=None,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_57(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params=None,
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_58(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=None,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_59(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=None,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_60(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params=None,
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_61(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=None,
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_62(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=None,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_63(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=None,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_64(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=None,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_65(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=None,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_66(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=None,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_67(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=None,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_68(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=None,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_69(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=None,
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_70(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=None,
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_71(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=None,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_72(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=None,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_73(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=None,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_74(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=None,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_75(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=None,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_76(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_77(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_78(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_79(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_80(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_81(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_82(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_83(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_84(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_85(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_86(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_87(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_88(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_89(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_90(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_91(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_92(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_93(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_94(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_95(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_96(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"XXregime_1XX": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_97(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"REGIME_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_98(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "XXregime_2XX": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_99(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "REGIME_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_100(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "XXregime_3XX": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_101(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "REGIME_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_102(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"XXc_1XX": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_103(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"C_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_104(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "XXc_2XX": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_105(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "C_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_106(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array(None),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_107(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(None),
            bic=float(bic),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    def xǁSETARǁ_fit_3regime_fixed_delay__mutmut_108(self) -> ThresholdResults:
        """Fit 3-regime SETAR with current delay."""
        y, x_mat, s = self._y, self._X, self._s
        thresholds, beta1, beta2, beta3, sigma2_dict, ll = self._fit_three_regimes(y, x_mat, s)

        c1, c2 = thresholds
        mask1 = s <= c1
        mask2 = (s > c1) & (s <= c2)
        mask3 = s > c2

        resid = np.empty(len(y))
        resid[mask1] = y[mask1] - x_mat[mask1] @ beta1
        resid[mask2] = y[mask2] - x_mat[mask2] @ beta2
        resid[mask3] = y[mask3] - x_mat[mask3] @ beta3

        g = np.zeros(len(y))
        g[mask2] = 0.5
        g[mask3] = 1.0

        t = len(y)
        n_params = (self.order + 1) * 3 + 2 + 1  # 3 regimes + 2 thresholds + delay
        aic = -2 * ll + 2 * n_params
        bic = -2 * ll + np.log(t) * n_params

        return ThresholdResults(
            model_name=self.model_name,
            params={"regime_1": beta1, "regime_2": beta2, "regime_3": beta3},
            threshold=thresholds,
            delay=self.delay,
            transition_params={"c_1": c1, "c_2": c2},
            transition_params_array=np.array([c1, c2]),
            params_regime1=beta1,
            params_regime2=beta2,
            regime_assignments=g,
            transition_values=g,
            resid=resid,
            sigma2=sigma2_dict,
            loglike=ll,
            aic=float(aic),
            bic=float(None),
            nobs=t,
            order=self.order,
            n_regimes=self.n_regimes,
            endog=self.endog,
            _model=self,
        )

    xǁSETARǁ_fit_3regime_fixed_delay__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_1": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_1,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_2": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_2,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_3": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_3,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_4": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_4,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_5": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_5,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_6": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_6,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_7": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_7,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_8": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_8,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_9": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_9,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_10": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_10,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_11": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_11,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_12": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_12,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_13": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_13,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_14": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_14,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_15": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_15,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_16": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_16,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_17": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_17,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_18": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_18,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_19": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_19,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_20": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_20,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_21": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_21,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_22": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_22,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_23": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_23,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_24": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_24,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_25": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_25,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_26": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_26,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_27": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_27,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_28": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_28,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_29": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_29,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_30": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_30,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_31": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_31,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_32": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_32,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_33": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_33,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_34": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_34,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_35": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_35,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_36": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_36,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_37": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_37,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_38": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_38,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_39": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_39,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_40": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_40,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_41": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_41,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_42": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_42,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_43": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_43,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_44": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_44,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_45": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_45,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_46": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_46,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_47": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_47,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_48": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_48,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_49": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_49,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_50": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_50,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_51": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_51,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_52": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_52,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_53": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_53,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_54": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_54,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_55": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_55,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_56": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_56,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_57": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_57,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_58": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_58,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_59": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_59,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_60": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_60,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_61": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_61,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_62": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_62,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_63": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_63,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_64": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_64,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_65": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_65,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_66": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_66,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_67": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_67,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_68": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_68,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_69": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_69,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_70": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_70,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_71": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_71,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_72": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_72,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_73": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_73,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_74": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_74,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_75": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_75,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_76": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_76,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_77": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_77,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_78": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_78,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_79": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_79,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_80": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_80,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_81": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_81,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_82": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_82,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_83": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_83,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_84": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_84,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_85": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_85,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_86": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_86,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_87": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_87,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_88": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_88,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_89": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_89,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_90": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_90,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_91": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_91,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_92": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_92,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_93": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_93,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_94": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_94,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_95": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_95,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_96": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_96,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_97": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_97,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_98": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_98,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_99": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_99,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_100": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_100,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_101": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_101,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_102": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_102,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_103": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_103,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_104": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_104,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_105": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_105,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_106": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_106,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_107": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_107,
        "xǁSETARǁ_fit_3regime_fixed_delay__mutmut_108": xǁSETARǁ_fit_3regime_fixed_delay__mutmut_108,
    }
    xǁSETARǁ_fit_3regime_fixed_delay__mutmut_orig.__name__ = "xǁSETARǁ_fit_3regime_fixed_delay"
